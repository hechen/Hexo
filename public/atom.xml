<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Writing, Thinking and Coding.</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://hechen.xyz/"/>
  <updated>2019-01-31T02:30:21.000Z</updated>
  <id>http://hechen.xyz/</id>
  
  <author>
    <name>Chen</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>关于 SS 流量丢失的原因探索</title>
    <link href="http://hechen.xyz/2019/01/28/%E5%85%B3%E4%BA%8E-SS-%E6%B5%81%E9%87%8F%E4%B8%A2%E5%A4%B1%E7%9A%84%E5%8E%9F%E5%9B%A0%E6%8E%A2%E7%B4%A2/"/>
    <id>http://hechen.xyz/2019/01/28/关于-SS-流量丢失的原因探索/</id>
    <published>2019-01-28T09:51:35.000Z</published>
    <updated>2019-01-31T02:30:21.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="2019-01-29-更新"><a href="#2019-01-29-更新" class="headerlink" title="2019-01-29 更新"></a>2019-01-29 更新</h3><p>早上一到办公室连接上公司网络，网速直接就飚到了 600KB/s，甚至一度有过 M 的趋势，果不其然，还是 com.apple.Safari.SafeBrowsing.Service 这个服务进程。这就尴尬了，看来并不是说你不用 Safari 就不会触发。</p><p><img src="https://i.imgur.com/yifgIEZ.png" alt="Surge Dashboard"></p><p>直接把 Surge 规则中的 Rule 由 Direct 改成了 Reject。</p><h1 id="问题追踪"><a href="#问题追踪" class="headerlink" title="问题追踪"></a>问题追踪</h1><p>在 2018 年 12 月底的时候，一大早打开电脑查询刚买的 SS 服务。</p><p>刚刚在前几天购买的 SS 流量，每个月 150GB 的限额在短短的 4 天之内就耗了将近 95GB，就在 1 月 1 日元旦当天就耗了接近 75GB，要知道，我并不只有 AgentNEO 一家的流量服务，相对来说在 ping test 上其还不如 Shadowsocks 家的好，所以理论上规则命中也应该是后者的，而且就算我用 A 服务看小电影也不至于这么多流量消耗。</p><p><img src="https://i.imgur.com/9tRs9vh.png" alt="AgentNEO 流量面板"></p><p>在最初看到这个统计数据之后，我一度怀疑是他们网站的流量统计有问题。</p><p>后续几天，我实际上对流量耗损比较关注，有一天注意到，Surge 的流量监控速率一直持续不断的显示达 五六百 KB/s 的下载流量，但是我知道自己当时并没有下载任何东西。</p><p><img src="https://i.imgur.com/nWz8JAt.png" alt="Surge Menu Bar"></p><p>打开 Surge Dashboard，看到如下的情形：</p><p><img src="https://i.imgur.com/9kVX27V.png" alt="Surge Dashboard"></p><p>可以看到当时在持续不断的进行下载动作的进程名称是 <code>com.apple.Safari.SafeBrowsing.Service</code>，看请求地址是 safebrowsing.googleapis.com。</p><p>从进程名字来看应该是 Safari 和安全流量相关的服务，通过 Snitch 的数据库查到对其的说明：</p><blockquote><p>Safari has built-in support for Google’s Safe Browsing service to identify fraudulent and unsafe websites. Right before Safari navigates to a certain website, the website gets checked for possible security concerns using Google’s Safe Browsing online database. Accessing the online database requires connections to Google servers.</p></blockquote><p>说明该服务是针对 Safari 浏览器启用的，我们在如下图所示的 Safari 的 Security 菜单中可以找到启用关于欺诈网站的检测功能的开关。</p><blockquote><p>Safari uses Tencent Safe Browsing and Google Safe Browsing to identify fraudulent websites.</p></blockquote><p>大概意思就是 Safari 使用腾讯的安全浏览服务和 Google 的安全浏览服务来鉴别欺诈网站，</p><p><img src="https://i.imgur.com/wEhv8iu.png" alt="Little Snitch Network Monitor"></p><p>通过针对该进程的出口请求 host 也可以验证这一点，该进程发起的网络请求会有如下两个 host 出口：</p><ol><li>safebrowsing.googleapis.com </li><li>safebrowsing.urlsec.qq.com</li></ol><p>查了一下知道腾讯为 Apple 提供了本地化的欺诈网站特征库，而我们出问题的就是 1 的 google 提供的欺诈网站特征库。</p><p><img src="https://i.imgur.com/BtfvMIV.png" alt="Little Snitch Network Monitor"></p><p>尝试关闭 Surge 作为系统流量代理之后，看到 Activity Monitor 中进程又开始了下载，如下图所示，只是连接请求主体从原来 Surge 切换到了独立进程而已（Surge 会接管网络流量，因此之前该任务的下载会算到 Surge 的头上），但是针对该 host 的下载任务一致持续不断。</p><blockquote><p>说起来，Little Snitch Network Monitor 这款软件也是当时为了查流量丢失问题，才买的。</p></blockquote><p><img src="https://i.imgur.com/SUT5qQC.png" alt="Little Snitch Network Monitor"></p><p>而在我关闭 Surge 作为代理之前，可以看到该进程大概在 7 个小时之内耗了 12.4 GB 的流量。</p><p><img src="https://i.imgur.com/Gnxc4bB.png" alt="Little Snitch Network Monitor"></p><p>而在网络上目前未看到针对 safebrowsing 进程的大量讨论，国内论坛 <a href="https://www.v2ex.com/t/404025" target="_blank" rel="noopener">V2EX</a> 里也看到有人遇到该问题。</p><p>目前暂时不清楚是官方 Bug 还是我电脑安装了什么插件或者软件导致。暂时先停掉使用 Safari 了，用 Chrome 用上一段时间之后再用 Snitch 看下情况吧。</p><p>另一方面，因为不放心，在 Surge 的自定义规则中加了一条：<br>    NAME,com.apple.Safari.SafeBrowsing.Service,DIRECT<br>针对该进程的所有流量都直连，不用代理了。 后续有任何进展会更新到 Blog 中。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3 id=&quot;2019-01-29-更新&quot;&gt;&lt;a href=&quot;#2019-01-29-更新&quot; class=&quot;headerlink&quot; title=&quot;2019-01-29 更新&quot;&gt;&lt;/a&gt;2019-01-29 更新&lt;/h3&gt;&lt;p&gt;早上一到办公室连接上公司网络，网速直接就飚到了 60
      
    
    </summary>
    
      <category term="Mac" scheme="http://hechen.xyz/categories/Mac/"/>
    
    
      <category term="Safari" scheme="http://hechen.xyz/tags/Safari/"/>
    
      <category term="Traffic" scheme="http://hechen.xyz/tags/Traffic/"/>
    
  </entry>
  
  <entry>
    <title>在 Swift Framework 中使用 C 文件的过程探索</title>
    <link href="http://hechen.xyz/2019/01/03/Swift-and-Modules/"/>
    <id>http://hechen.xyz/2019/01/03/Swift-and-Modules/</id>
    <published>2019-01-03T13:19:24.000Z</published>
    <updated>2019-01-07T04:12:30.428Z</updated>
    
    <content type="html"><![CDATA[<p>Swift 真香！</p><h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>我们在开发线上诊断工具需求的时候，是以单个 Pod 的形式提供支持，并且代码文件中只有纯 Swift 文件，但是其中需要用到系统的 C 库的一些功能，本次就是使用了系统 C 库中 <code>resolv.h</code> 这个文件来进行 DNS 解析所用。</p><p>当后期 Pod 功能完善之后，在 Example 工程中也已经编译通过之后，接入主项目中之后遇到了下面这个编译错误：</p><p><img src="https://i.imgur.com/ADh0sy6.png" alt="Error When Compile"></p><p>具体文字错误信息如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/Users/chen/Repos/Work/ZHDiagnosisTool/ZHDiagnosisTool/Classes/Core/Network/ZHDiagnosisTool-Network-Header.h:8:10: </span><br><span class="line">    Include of non-modular header inside framework module &apos;Diagnosis.ZHDiagnosisTool_Network_Header&apos;: &apos;/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS12.1.sdk/usr/include/resolv.h&apos;</span><br></pre></td></tr></table></figure><p>大概的意思就是在 Umbrella Header 中加入的 Briding header 指定了 include 一个 C 文件，而该 C 文件本身并不是  Modular Header， 因此编译无法通过。这就涉及了目前 iOS 生态中普遍使用的 Modules 概念。</p><h2 id="几个概念"><a href="#几个概念" class="headerlink" title="几个概念"></a>几个概念</h2><h3 id="Modules"><a href="#Modules" class="headerlink" title="Modules"></a>Modules</h3><p>Modules 的概念是 XCode 5 带到 iOS 开发者面前的，从那时起 LLVM 编译器就已经内在支持了，具体关于其历史在 <a href="https://onevcat.com/2013/06/new-in-xcode5-and-objc/" target="_blank" rel="noopener">WWDC 2013 Session笔记 - Xcode5和ObjC新特性</a> 这篇文章中已经讲解的非常详细了，不再赘述，总结一句话就是：</p><blockquote><p>Modules 是被引入 LLVM 层面的，用以解决之前 C/C++ 系中 #include 和 #import 带来的引用泛滥以及编译时间过长的问题的一种手段。</p></blockquote><p>尤其是在 Swift 引入之后，Module 的概念应该已经深入人心。大家可能已经习惯直接使用 @import 来引用某个 Module，或者其中某个逻辑单元。尤其是使用 CocoaPods 集成开发的时候，其也帮你底层做了一些 Modules 的工作， module 的属性是由定义它的 <code>.modulemap</code> 文件来决定的，在 Pods 的工程目录结构中充斥着 <code>.modulemap</code> 的身影，其语法大概如下：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">module module_name [system] &#123;</span><br><span class="line">    header "header.h"</span><br><span class="line">    link "linked_library"</span><br><span class="line">    export *</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果对其中 <a href="https://clang.llvm.org/docs/Modules.html#module-map-language" target="_blank" rel="noopener">Modules 的语法</a>感兴趣，可以到 Clang 的官方文档下通读下，你肯定会对 Modules 这一套有更清楚的认识。</p><h3 id="Umbrella-Header"><a href="#Umbrella-Header" class="headerlink" title="Umbrella Header"></a>Umbrella Header</h3><p>说起来，Umbrella Header 是在 Framework 的概念被引入的，你可以理解为一个模块均存在一个 Umbrella Header 用来将那些你想暴露给模块外界调用的头文件包裹在一起。避免使用者在使用该模块的时候需要手动输入多个 Header 的一种解决方案。 iOS 中特指 iOS 8 系统官方加入 Dynamic Framework 之后引入的。</p><p>如下所示，没有 Umbrella Header 的情况下你需要将所有需要引入的头文件依次写出。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;XYZModule/XYZCustomCell.h&gt;</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;XYZModule/XYZCustomView.h&gt;</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;XYZModule/XYZCustomViewController.h&gt;</span></span></span><br></pre></td></tr></table></figure><p>在使用了 Umbrella Header 之后，你只需要下面一行即可。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;XYZModule/XYZModule.h&gt;</span></span></span><br></pre></td></tr></table></figure><p>当然，还存在 umbrella framework，感兴趣大家可以到<a href="https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPFrameworks/Concepts/FrameworkAnatomy.html#//apple_ref/doc/uid/20002253-97623-BAJJHAJC" target="_blank" rel="noopener">官方文档</a>下观看。</p><ul><li>Bridging-Header</li></ul><p>桥接文件是在 Swift 推出之后，Apple 引入目前iOS 生态的一个概念用以桥接 Swift 和 Objective-C 的一种方式 （Mix and Match），如下图所示，如果在 Swift 中想使用 Objective-C 类定义的内容，就需要建立 Bridging Header，然后在其中定义你想要暴露的 OC 头文件。</p><p><img src="https://i.imgur.com/YwqHEKJ.png" alt="Bridging Header"></p><p>具体的操作在<a href="https://developer.apple.com/documentation/swift/imported_c_and_objective-c_apis/importing_objective-c_into_swift" target="_blank" rel="noopener">官方文档</a>有针对在同一个 App Target 和在同一个 Framework 内两种情况均有说明。</p><blockquote><p>在梳理这些概念的过程中，发现自己之前对桥接文件有个误区，如果你在 App 的主 Target 中需要进行 OC 和 Swift 的混编，使用 Bridging Header 是必选，如果你是在同一个 Framework 中进行的混编，Bridging-Header 是并不需要的，你需要做的只是在 Umbrella Header 里加入你想暴露给 Swift 文件使用的 OC 头文件即可，之前自己一直是显式的建立一个 Bridging-Header，然后在 Umbrella Header 中引入该头文件，想来这种方式只是将你想暴露在外的头文件进行了二次包裹而已，因为 Cocoapods 是将所有 public header 均加入到 umbrella header 里了，因此单个 framework 内部开发，即使没有 bridge header 也是能够将符号暴露给 Swift 的。</p></blockquote><h3 id="Cocoapods"><a href="#Cocoapods" class="headerlink" title="Cocoapods"></a>Cocoapods</h3><p>我们每次执行 <code>pod install</code> 的时候，如果你使用 —verbose 来看详细安装过程就能看到，针对每一个将要被安装的 Pod 均会执行生成 module map 和 umbrella header 这两个阶段，如下所示部分 Install 指令的代码，代码位于 <code>lib/cocoapods/installer/xcode/pods_project_generator/aggregate_target_installer.rb</code> </p><figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">install!</span></span></span><br><span class="line">       UI.message <span class="string">"- Installing target `<span class="subst">#&#123;target.name&#125;</span>` <span class="subst">#&#123;target.platform&#125;</span>"</span> <span class="keyword">do</span></span><br><span class="line">       native_target = add_target</span><br><span class="line">       create_support_files_dir</span><br><span class="line">       create_support_files_group</span><br><span class="line">       create_xcconfig_file(native_target)</span><br><span class="line">       <span class="keyword">if</span> target.host_requires_frameworks?</span><br><span class="line">          create_info_plist_file(target.info_plist_path, native_target, target.version, target.platform)</span><br><span class="line">        create_module_map(native_target)</span><br><span class="line">          create_umbrella_header(native_target)</span><br><span class="line">       <span class="keyword">elsif</span> target.uses_swift?</span><br><span class="line">          create_module_map(native_target)</span><br><span class="line">    create_umbrella_header(native_target)</span><br><span class="line">       <span class="keyword">end</span></span><br><span class="line">      </span><br><span class="line">     <span class="comment"># Some Code</span></span><br><span class="line">     <span class="comment"># Some Code</span></span><br><span class="line">     <span class="comment"># Some Code      </span></span><br><span class="line">    <span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>很明确，Cocoapods 在 install 某个 Pod 的时候会执行创建 module map 文件以及 umbrella header 文件的工作。详细的代码大家可以直接到 Cocoapods 源码下  <code>lib/cocoapods/generator</code> 目录下看，分别是 <code>module_map.rb</code> 和 <code>umbrella_header.rb</code> 文件。</p><p>其中在各自文件的注释部分也有针对生成文件的说明，比如 <code>module_map.rb</code> 中说明了 module map 的作用，</p><blockquote><p>Generates LLVM module map files. A module map file is generated for each Pod and for each Pod target definition that is built as a framework. It specifies a different umbrella header than usual to avoid name conflicts with existing headers of the podspec.</p></blockquote><p>在 <code>umbrella_header.rb</code> 中说明了 umbrella header 的作用，</p><blockquote><p>Generates an umbrella header file for clang modules, which are used by dynamic frameworks on iOS 8 and OSX 10.10 under the hood. If the target is a +PodTarget+, then the umbrella header is required to make all public headers in a convenient manner available without the need to write out header declarations for every library header.</p></blockquote><h2 id="问题原因"><a href="#问题原因" class="headerlink" title="问题原因"></a>问题原因</h2><p>首先先解答：为什么 Example 工程能够编译通过 而用了主工程是无法编译通过？</p><p>因为目前主工程都要求我们自行开发的 pod 以动态 framework 的形式参与，但是在 Example 工程开发的时候我们的 <code>podfile</code> 并未指定 <code>use_frameworks!</code> 用以产出动态 framework，因此未暴露出该问题，也就是说只有以 framework 的形式的 module 才会有该问题，准确的说，应该是 <code>Dynamic Framework</code></p><p>了解了以上概念之后，我们再来看我们的问题，首先我们的问题是在 framework 内部调用系统 C 库代码出现的问题，从 Xcode 的报错，我们知道我们通过 bridging header 引入的 C 文件并不以 module 的形式存在，因此编译器报错。</p><h2 id="解决过程"><a href="#解决过程" class="headerlink" title="解决过程"></a>解决过程</h2><p>其实吧，大家都能想到，使用 Objective-C 做个封装，或者干脆直接调用 C 文件的类用 OC 重写不就完了么，可是如果下次你再遇到了呢？ 或者要改造了一个纯 Swift 库呢？知其所以然，才能避免再次落坑吧。</p><blockquote><p>因为 Objective-C 自身编译器是帮你做了 modular 化的，当然，如果你选择了前者，还有个限制，你并不能把上面 C 的头文件放到你的 Objective-C 的头文件中，因为本质上，最后这个头文件还是要暴露给 Umbrella Header 的。</p></blockquote><p>首先，能否允许编译器支持在 module 中引入非 modular 的头文件呢？ </p><p>Xcode 在 build setting 中提供了  <code>Allow Non-modular Includes In Framework Modules</code> 来控制是否允许在当前 framework 中支持非 modular 头文件引入，其并不适用于纯 Swift 项目，而且即使适用，其会导致所有用到该 framework 的地方也不能再适用 module 形式引入头文件了，也就是必须适用平坦式的 <code>#include &quot;&quot;</code></p><p><img src="https://i.imgur.com/S4i2chx.png" alt="Build Setting"></p><p>最后，通过只能从根源上来解决该问题了，既然需要引入的文件是 modular header，我们就要想办法来将其包裹为 module。 在查找解决方案的过程中发现，就在 Swift 刚推出的时候，大家已经遇到这个问题了，遇到最多的就是<code>CommonCrypto</code> 经常被使用到的 C 库，常见的 MD5 计算就是其提供的 API，但是在前几年官方并未将该动态库 module 化，因此导致你无法在 Swift 文件中直接使用类似 <code>#import &lt;CommonCrypto/CommonCrypto.h&gt;</code> 这种写法。自然而言多了很多解决方案，如下出自 StackOverflow 上 <a href="https://stackoverflow.com/questions/25248598/importing-commoncrypto-in-a-swift-framework/37125785#37125785" target="_blank" rel="noopener">Importing CommonCrypto in a Swift framework</a> 的帖子下面就提供了多种解决方案：</p><blockquote><p>还是那句话，你是可以通过 Objective-C 做桥接来达到同样效果的。毕竟 Swift 本身就是一门心思想抛开 C 的历史包袱，因此想拥有一个纯纯的 Swift 代码不沾染一点 C 文件气息，你就要做一些工作。</p></blockquote><p>其中根本问题就是为 <code>CommonCrypto</code> 这个 C 编译单元定义 module，上面也提到了 LLVM 是通过 <code>modulemap</code> 文件来识别的，所以只要通过 <code>.modulemap</code> 来定义即可，</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module CommonCrypto [system] &#123;</span><br><span class="line">    header &quot;/usr/include/CommonCrypto/CommonCrypto.h&quot;</span><br><span class="line">    export *</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>定义的 modulemap 自然可以放到任意目录，只要让编译单元在编译的时候能够搜索的到即可，Xcode 选项的 Build Settings 下的 Swift Compiler - Search Paths 。添加 .modulemap 文件所在路径即可。在编译的时候 LLVM 自然会查找到 .modulemap 文件自动生成 Module 信息。你此时就可以在使用  CommonCrypto 的地方使用 modular header 了。</p><p>过了几年之后，官方才将该库定义为 module，在 Xcode 自带的 iOS SDK 中 <code>/usr/include/CommonCrypto</code> 下可以看到 <code>module.modulemap</code> 文件：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module CommonCrypto [system] [extern_c] &#123;</span><br><span class="line">      umbrella header &quot;CommonCrypto.h&quot;</span><br><span class="line">      export *</span><br><span class="line">      module * &#123; export * &#125;</span><br><span class="line">      </span><br><span class="line">      module Error &#123;</span><br><span class="line">          header &quot;CommonCryptoError.h&quot;</span><br><span class="line">          export *</span><br><span class="line">      &#125;</span><br><span class="line">      </span><br><span class="line">      module Random &#123;</span><br><span class="line">          header &quot;CommonRandom.h&quot;</span><br><span class="line">          export *</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>大家也注意到了在 CommonCrypto 的同级目录中实际上还有很多的系统 C 库代码，并且也有一个 module.modulemap 文件，我裁剪一段代码大家看下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module Compression [system] [extern_c] &#123;</span><br><span class="line">    header &quot;compression.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    link &quot;compression&quot;</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line">module Darwin [system] [extern_c] [no_undeclared_includes] &#123;</span><br><span class="line">    // Headers that are repeatedly included, and therefore should not be</span><br><span class="line">    // assigned to any given module.</span><br><span class="line">    exclude header &quot;_structs.h&quot;</span><br><span class="line">    exclude header &quot;sys/_structs.h&quot;</span><br><span class="line">    </span><br><span class="line">    // C standard library</span><br><span class="line">    module C &#123;</span><br><span class="line">    textual header &quot;assert.h&quot;</span><br><span class="line">    </span><br><span class="line">      module setjmp &#123;</span><br><span class="line">    header &quot;setjmp.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    module signal &#123;</span><br><span class="line">    header &quot;signal.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    module stdio &#123;</span><br><span class="line">    header &quot;stdio.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    module zlib [system] [extern_c] &#123;</span><br><span class="line">    header &quot;zlib.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    link &quot;z&quot;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    module SQLite3 [system] [extern_c] &#123;</span><br><span class="line">    header &quot;sqlite3.h&quot;</span><br><span class="line">    link &quot;sqlite3&quot;</span><br><span class="line">    explicit module Ext &#123;</span><br><span class="line">    header &quot;sqlite3ext.h&quot;</span><br><span class="line">    export *</span><br><span class="line">    &#125;</span><br><span class="line">    export *</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>这也是为什么我们在 Swift 代码里可以直接使用类似 <code>import Darwin.C.stdio</code> 写法的原因。但是还是存在一些 C 库并没有被定义为 module，而本次用来做 DNS 解析的 <code>resolv.h</code> 就是其中一员。</p><p>具体到我们开发流程里说，是要解决 modulemap 如何定义，如何和我们目前使用的 Cocoapods 整合的问题了。既然是在作为 Pod 进行开发，自然是需要将 module map 文件加入到 pod 的代码管理中去。比如将其放置于单个 pod 根目录下，然后在 podspec 中配置好路径以便编译的时候 Swift Search Paths 中有它。自然，我们可以建立 resolv.modulemap 如下：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">module Resolv [system] &#123;</span><br><span class="line">    header "/usr/include/resolv.h"</span><br><span class="line">    export *</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后在对应的 podspec 文件中配置 build setting 中的参数，如下所示</p><figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">s.subspec <span class="string">'Core'</span> <span class="keyword">do</span> <span class="params">|core|</span></span><br><span class="line">    core.source_files   = [<span class="string">'ZHDiagnosisTool/Classes/Core/**/*'</span>] </span><br><span class="line">    core.preserve_paths = <span class="string">'ZHDiagnosisTool/Classes/Core/ModuleMap'</span></span><br><span class="line">    core.pod_target_xcconfig = &#123;</span><br><span class="line">        <span class="string">'SWIFT_INCLUDE_PATHS[sdk=macosx*]'</span>           =&gt; <span class="string">'$(PODS_ROOT)/ZHDiagnosisTool/Classes/Core/ModuleMap'</span>,</span><br><span class="line">        <span class="string">'SWIFT_INCLUDE_PATHS[sdk=iphoneos*]'</span>         =&gt; <span class="string">'$(PODS_ROOT)/ZHDiagnosisTool/Classes/Core/ModuleMap'</span>,</span><br><span class="line">        <span class="string">'SWIFT_INCLUDE_PATHS[sdk=iphonesimulator*]'</span>  =&gt; <span class="string">'$(PODS_ROOT)/ZHDiagnosisTool/Classes/Core/ModuleMap'</span>,</span><br><span class="line">      &#125;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>可是，问题出在 PODS_ROOTS 这个路径上，如果我们进行本地开发，该路径就是 Cocoapods 执行完毕之后生成的临时 Pods 目录，并不是原始 podspec 文件所在的地方，而官方也表明了，不会对这种 Local Pods 特定提供一个环境变量来获取， 如下链接可以看到：</p><p><a href="https://github.com/CocoaPods/CocoaPods/issues/809" target="_blank" rel="noopener">local pod development on a project that includes libraries · Issue #809 · CocoaPods/CocoaPods</a></p><p>如图中，指定 $(PODS_ROOT) 路径实际上在开发 Local Pod 的时候就会找不到，所以就需要按照上面链接中的方式自行手动拼接路径，所以，我们尽可能不用 PODS_ROOT 这个路径，需要另外找一个不会因为 Pod 位置而变化的路径。</p><p>最终参考了以下 repo 中工程的解决方案，</p><p><a href="https://github.com/onmyway133/Arcane" target="_blank" rel="noopener">onmyway133/Arcane</a></p><p>其中作者使用 Pod 的 script phase 来完成 .modulemap 的操作，这样会更加灵活。</p><p>这样，针对我们自己本次的需求来看，.podspec 中的 script 如下所写即可：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span> Create</span><br><span class="line">FRAMEWORK_DIR="$&#123;BUILT_PRODUCTS_DIR&#125;/RESOLV.framework"</span><br><span class="line">          </span><br><span class="line">if [ -d "$&#123;FRAMEWORK_DIR&#125;" ]; then</span><br><span class="line">    echo "$&#123;FRAMEWORK_DIR&#125; already exists, so skipping the rest of the script."</span><br><span class="line">    exit 0</span><br><span class="line">fi</span><br><span class="line">          </span><br><span class="line">mkdir -p "$&#123;FRAMEWORK_DIR&#125;/Modules"</span><br><span class="line">          </span><br><span class="line">echo "module RESOLV [system] &#123;</span><br><span class="line">    header \"$&#123;SDKROOT&#125;/usr/include/resolv.h\"</span><br><span class="line">    export *</span><br><span class="line">&#125;" &gt; "$&#123;FRAMEWORK_DIR&#125;/Modules/module.modulemap"</span><br><span class="line">               </span><br><span class="line">    </span><br><span class="line"><span class="meta">#</span> Generate fake header...</span><br><span class="line">[ -d "$&#123;FRAMEWORK_DIR&#125;/Headers" ] || mkdir "$&#123;FRAMEWORK_DIR&#125;/Headers"</span><br><span class="line">touch "$&#123;FRAMEWORK_DIR&#125;/Headers/resolv.h"</span><br><span class="line">    </span><br><span class="line"><span class="meta">#</span> Soft link C header to local framework Headers</span><br><span class="line">ln -sf "$&#123;SDKROOT&#125;/usr/include/resolv.h" "$&#123;FRAMEWORK_DIR&#125;/Headers/resolv.h"</span><br></pre></td></tr></table></figure><p>每次在编译运行前，我们会自行创建一个 <code>Resolv.framework</code>，其中 Header 目录中我们放一个空白文件并且软链接到真正想要链接的头文件上，然后在该 framework 中创建 modulemap 文件。</p><p>之后，我们在 .podspec 中指定 script phase 为编译前即可，如下所示，</p><figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">s.script_phase = &#123;</span><br><span class="line">    <span class="symbol">:name</span> =&gt; <span class="string">'Resolv'</span>,</span><br><span class="line">    <span class="symbol">:script</span> =&gt; script_above,</span><br><span class="line">    <span class="symbol">:execution_position</span> =&gt; <span class="symbol">:before_compile</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，通过 <code>Resolv.framework</code> 的桥接，Swift 代码中就可以直接通过 <code>import RESOLV</code> 来使用了。当然，链接这一环你可以通过以下两种形式达到：</p><ol><li>直接在 podspec 文件中指定依赖，如 <code>core.library = &quot;resolv&quot;</code></li><li>在 <code>.modulemap</code> 文件中显式的 link，如下所示：</li></ol><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">module Resolv [system] &#123;</span><br><span class="line">    header "/usr/include/resolv.h"</span><br><span class="line">    link "resolv"</span><br><span class="line">    export *</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而且通过将 module map 文件放置于 product 产出目录这里也解决了路径指定的问题，因为 product folder 是默认在编译的搜索路径下的。</p><h3 id="问题解惑"><a href="#问题解惑" class="headerlink" title="问题解惑"></a>问题解惑</h3><h4 id="关于某些"><a href="#关于某些" class="headerlink" title="关于某些"></a>关于某些</h4><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>总结来看，就是想要在纯 Swift 的项目中引入系统 C 库文件（这里指的是未被 modular 化的文件，因为有些 C 文件已经被系统默认封装成了 module 了）。以上只是一些简单的概念讲解，整个编译系统以及套件都是长时间不断演化的结果，这里也只是简单的讲述，有一些概念实际上也只是点到为止，大家如果感兴趣可以多找一些相关资料阅读下，亲自试一试。当然，当然刚开始直接用 Objective-C 来写不就好了，确实，不过通过本次解决这个问题，对 iOS 开发过程中遇到的某些司空见惯的概念（充斥在整个开发周期中）有了更清楚的了解，不是更好。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li><a href="http://llvm.org/devmtg/2012-11/Gregor-Modules.pdf" target="_blank" rel="noopener">Modules</a></li><li><a href="https://zh.wikipedia.org/wiki/LLVM" target="_blank" rel="noopener">LLVM - 维基百科，自由的百科全书</a></li><li><a href="https://clang.llvm.org/docs/Modules.html#introduction" target="_blank" rel="noopener">Modules - Clang 8 documentation</a></li><li><a href="https://onevcat.com/2013/06/new-in-xcode5-and-objc/" target="_blank" rel="noopener">WWDC 2013 Session笔记 - Xcode5和ObjC新特性</a></li><li><a href="https://stackoverflow.com/questions/25248598/importing-commoncrypto-in-a-swift-framework/37125785#37125785" target="_blank" rel="noopener">Importing CommonCrypto in a Swift framework</a></li><li><a href="https://forums.developer.apple.com/thread/46477" target="_blank" rel="noopener">Adding CommonCrypto to custom Swift framework |Apple Developer Forums</a></li><li><a href="https://theswiftdev.com/2018/01/15/how-to-call-c-code-from-swift/" target="_blank" rel="noopener">How to call C code from Swift - The.Swift.Dev.</a></li><li><a href="https://medium.com/swift-and-ios-writing/using-a-c-library-inside-a-swift-framework-d041d7b701d9/" target="_blank" rel="noopener">Using a C library inside a Swift framework - Swift and iOS Writing - Medium</a></li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Swift 真香！&lt;/p&gt;
&lt;h2 id=&quot;问题描述&quot;&gt;&lt;a href=&quot;#问题描述&quot; class=&quot;headerlink&quot; title=&quot;问题描述&quot;&gt;&lt;/a&gt;问题描述&lt;/h2&gt;&lt;p&gt;我们在开发线上诊断工具需求的时候，是以单个 Pod 的形式提供支持，并且代码文件中只有纯 
      
    
    </summary>
    
      <category term="iOS" scheme="http://hechen.xyz/categories/iOS/"/>
    
    
      <category term="Swift, Module, CocoaPods" scheme="http://hechen.xyz/tags/Swift-Module-CocoaPods/"/>
    
  </entry>
  
  <entry>
    <title>UserDefaults and Keychain</title>
    <link href="http://hechen.xyz/2018/12/21/UserDefaults-and-Keychain/"/>
    <id>http://hechen.xyz/2018/12/21/UserDefaults-and-Keychain/</id>
    <published>2018-12-21T06:30:50.000Z</published>
    <updated>2019-01-03T14:26:07.844Z</updated>
    
    <content type="html"><![CDATA[<p>Apple 提供了几种持久化方案，其中 UserDefaults 和 Keychain 是 App 开发过程中使用频率最高的方案，而且从以往和同事的探讨过程中发现对这两个概念中有一些细节还是理解不太透彻，因此本文会针对这二者展开讲一讲。</p><h2 id="UserDefaults"><a href="#UserDefaults" class="headerlink" title="UserDefaults"></a>UserDefaults</h2><p>首先，阅读完 Apple 关于 UserDefaults 一节的文档描述之后，我觉得有两个需要注意的点：</p><ol><li>UserDefaults 的构成</li><li>UserDefaults 的目的</li></ol><h3 id="UserDefaults-构成"><a href="#UserDefaults-构成" class="headerlink" title="UserDefaults 构成"></a>UserDefaults 构成</h3><p>我们可以看看具体 UserDefaults 存储的地方，如下图，我们在应用中写入 Demo 数据：</p><p><img src="https://i.imgur.com/wq9onOa.png" alt="Write Key-Value to UserDefaults"></p><p>打印 Library 路径，在 Finder 中打开路径:</p><p><img src="https://i.imgur.com/65ybYX6.png" alt="Library"></p><p>如下展示内容，可以看到在 Preferences 目录中存在某个文件，例如该 App Library 目录下存放的文件为 <code>app.chen.ios.PersistenceDemo.plist</code></p><p><img src="https://i.imgur.com/ChqnDJa.png" alt="Cache"><br><img src="https://i.imgur.com/cPvlFdR.png" alt="Preferences"></p><p>该 plist 内容如图所示，即为我们之前在 App 中写入的 Key-Value。</p><p><img src="https://i.imgur.com/shdXoZN.png" alt="plist"></p><p>所以这也证明了，我们针对 UserDefaults 的读写实质上是针对 plist 文件的读写。</p><p>iOS 系统会自动帮你做 <code>plist</code> 文件的读入以及 Cache，根据情况会把你在内存中所做的操作同步到 <code>plist</code> 文件（UserDefaults 同步到内存是同步的，同步到 Database 是异步的， iOS 8 开始，会有一个常驻进程 <code>cfprefsd</code> 来负责同步）。</p><p>所以你会看有iOS 面试题目会问题： 系统的 <code>UserDefaults</code> 的本质以及和 plist 文件的直接读写的区别？（这题目太 TM 偏了。。。）</p><blockquote><p>UserDefaults caches the information to avoid having to open the user’s defaults database each time you need a default value. When you set a default value, it’s changed synchronously within your process, and asynchronously to persistent storage and other processes.</p></blockquote><p>所以，实质上，你是可以直接针对 UserDefaults 的最终产物 plist 文件进行操作的，当然，这是有风险的，而且无法保障正常使用的。官方在文档中也提醒了开发者。</p><blockquote><p>Don’t try to access the preferences subsystem directly. Modifying preference property list files may result in loss of changes, delay of reflecting changes, and app crashes. To configure preferences, use the defaults command-line utility in macOS instead.</p></blockquote><p>那既然本质上 UserDefaults 是使用 plist 文件进行存储，那也要求了我们能存储的 Value 只能支持 <code>plist</code> 所支持的格式，例如 <code>String</code>，<code>Number</code>，<code>Array</code>，<code>Data</code>，<code>Date</code> 等，当然如果你要存储自定义的类，其需要遵守 <code>Codable</code> 协议（实质也是要归档为 <code>Data</code>）</p><p><img src="https://i.imgur.com/cxWHfvS.png" alt="plist"></p><h3 id="UserDefaults-目的"><a href="#UserDefaults-目的" class="headerlink" title="UserDefaults 目的"></a>UserDefaults 目的</h3><blockquote><p>The defaults system allows an app to customize its behavior to match a user’s preferences. For example, you can allow users to specify their preferred units of measurement or media playback speed. Apps store these preferences by assigning values to a set of parameters in a user’s defaults database. </p></blockquote><p>一般我们在 <code>UserDefaults</code> 存储的数据都是用户的某些配置项，不因为用户使用过程中出现意外而丢失，比如是否开启了日夜间模式了，是否开启了大图模式了等等。或者存储一些对安全方面不敏感的数据</p><p>不建议往 <code>UserDefaults</code> 里存储较大的数据，例如直接存储一张图片。而对于这种需要存储较大文件的需求，你可以将文件本身存储到本地，而 <code>UserDefaults</code> 里只存储该文件的路径。</p><p>毕竟在 App 启动之后，UserDefaults 会进行 IO ，读取本地 plist 文件，因此一定程度上，也会较少 App 启动之后 UserDefaults API 针对 <code>plist</code> 文件 IO 的时间，纯属个人揣测，没有经过实验验证。</p><h3 id="关于-Extension-中-UserDefaults-的应用"><a href="#关于-Extension-中-UserDefaults-的应用" class="headerlink" title="关于 Extension 中 UserDefaults 的应用"></a>关于 Extension 中 UserDefaults 的应用</h3><p>iOS 上 Extension 和 Host App 之间做数据共享也是通过 UserDefaults，开启 App Group 之后，这二者就可以修改同一份配置。具体 Extension 和 Host App 之间的内在关系可以先看下 Apple 文档。本质上如下所示，</p><p><img src="https://i.imgur.com/cZFlz6o.png" alt="app_extensions_container_restrictions"></p><p>Extension 和 Host App 之间通过 Shared Container 来做数据共享，该 SharedContainer 私有，因此不存在于单一 App 的沙盒内。应该是系统会单独开辟一块空间用以做共享数据的存储。</p><blockquote><p>After you enable app groups, an app extension and its containing app can both use the NSUserDefaults API to share access to user preferences. To enable this sharing, use the initWithSuiteName: method to instantiate a new NSUserDefaults object, passing in the identifier of the shared group.</p></blockquote><p>因为 UserDefaults 既然是暴露在本地能够访问的文件当中的，因此不要在 UserDefaults 里存储任何 Security-Sensitive 的数据。 如果要存储保密级别较高的数据，就要用到另外一种持久化方案 ── Keychain</p><h2 id="Keychain"><a href="#Keychain" class="headerlink" title="Keychain"></a>Keychain</h2><p>关于 Keychain，是 Apple 提供给开发者用来存储 Security-Sensitive 的数据了，比如登录密码，用户标识，加密数据等等。 官方示意图如下，其实中间 Keychain 的 API 还进行了 Decrypt 和 Encrypt 的动作。</p><p><img src="https://i.imgur.com/tvA4lV3.png" alt="Keychain services API"></p><p>Apple 提供的 Keychain API 大部分都是 C 语言写成，使用起来相对不便，因此基本上我们都会使用二次加工过的库，比如知乎用的就是 SMKeychain。</p><p>Keychain 中的数据完全交由系统保管并加密过( AES 128 in GCM (Galois/Counter Mode))的，因此能够保证安全性。</p><p>如果对加密这一块感兴趣，可以看下<a href="https://www.apple.com/business/site/docs/iOS_Security_Guide.pdf" target="_blank" rel="noopener">苹果的白皮书</a>中 Keychain data protection 这一节。</p><p>另外，Keychain 的数据并不存放在 App 的 Sanbox 中，即使删除了 App，资料依然保存在 keychain 中。如果重新安装了app，还可以从 keychain 获取数据。</p><h3 id="同一个-App-内部共享"><a href="#同一个-App-内部共享" class="headerlink" title="同一个 App 内部共享"></a>同一个 App 内部共享</h3><p>类似 UserDefaults ，Keychain 也支持 Extension 和 host app 之间的共享，需要在每个 Target 的 Capability 下开启 Keychain Sharing 功能，并且设置为同一个 Group。</p><h3 id="不同-App-之间共享"><a href="#不同-App-之间共享" class="headerlink" title="不同 App 之间共享"></a>不同 App 之间共享</h3><p>和 UserDefaults 不同的是，Keychain 的数据是可以跨 App 获取的，但是限于一个开发者的 App，也就是需要确保这些 App 所属的 Team ID 是相同的。</p><p>这个也是App 进行 SSO 登录的基础，比如知乎日报使用知乎主 App 登录也是基于此原理。</p><p><img src="https://i.imgur.com/0L2MA23.png" alt="shared items"></p><p>在需要进行 Keychain 共享的 App 内开启 Keychain Sharing 的能力，所属同一个 Team ID 下的 App 本身都可以读取该 Keychain 的内容，类似于 AppGroup，这里也会产生 Keychain Group。</p><blockquote><p>Allows this application to share passwords from its keychain with other applications made by your team.</p></blockquote><p><img src="https://i.imgur.com/DJsInJb.png" alt="Keychain Sharing"></p><h2 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h2><ol><li><a href="https://developer.apple.com/documentation/foundation/userdefaults" target="_blank" rel="noopener">https://developer.apple.com/documentation/foundation/userdefaults</a></li><li><a href="https://www.reddit.com/r/jailbreak/comments/2qpqi9/ios_812_has_protection_against_plist_file_editing/" target="_blank" rel="noopener">https://www.reddit.com/r/jailbreak/comments/2qpqi9/ios_812_has_protection_against_plist_file_editing/</a></li><li><a href="http://iphonedevwiki.net/index.php/PreferenceBundles" target="_blank" rel="noopener">http://iphonedevwiki.net/index.php/PreferenceBundles</a></li><li><a href="https://developer.apple.com/library/archive/documentation/General/Conceptual/ExtensibilityPG/ExtensionScenarios.html" target="_blank" rel="noopener">https://developer.apple.com/library/archive/documentation/General/Conceptual/ExtensibilityPG/ExtensionScenarios.html</a></li><li><a href="https://developer.apple.com/documentation/security/keychain_services" target="_blank" rel="noopener">https://developer.apple.com/documentation/security/keychain_services</a></li><li><a href="https://github.com/soffes/SAMKeychain" target="_blank" rel="noopener">https://github.com/soffes/SAMKeychain</a></li><li><a href="https://developer.apple.com/documentation/security/keychain_services/keychain_items/sharing_access_to_keychain_items_among_a_collection_of_apps" target="_blank" rel="noopener">https://developer.apple.com/documentation/security/keychain_services/keychain_items/sharing_access_to_keychain_items_among_a_collection_of_apps</a></li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Apple 提供了几种持久化方案，其中 UserDefaults 和 Keychain 是 App 开发过程中使用频率最高的方案，而且从以往和同事的探讨过程中发现对这两个概念中有一些细节还是理解不太透彻，因此本文会针对这二者展开讲一讲。&lt;/p&gt;
&lt;h2 id=&quot;UserDe
      
    
    </summary>
    
      <category term="iOS" scheme="http://hechen.xyz/categories/iOS/"/>
    
    
      <category term="UserDefaults,Keychain" scheme="http://hechen.xyz/tags/UserDefaults-Keychain/"/>
    
  </entry>
  
  <entry>
    <title>如何做 Send to 2Do 的书签</title>
    <link href="http://hechen.xyz/2018/11/26/Send-to-2Do/"/>
    <id>http://hechen.xyz/2018/11/26/Send-to-2Do/</id>
    <published>2018-11-26T06:59:50.000Z</published>
    <updated>2018-12-02T11:52:17.000Z</updated>
    
    <content type="html"><![CDATA[<p>使用鼠标拖拽下面这个链接到你的 Favorites Bar 上，</p><p><a href="javascript:window.location=&#39;&#39;" target="_blank" rel="noopener">Send to 2Do</a></p><p>然后编辑地址，将其替换为如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">javascript:window.location=&apos;twodo://x-callback-url/add?task=&apos;+encodeURIComponent(document.title)+&apos;&amp;note=&apos;+encodeURIComponent(window.location)+&apos;&amp;action=url:&apos;+encodeURIComponent(window.location)</span><br></pre></td></tr></table></figure><p><img src="https://i.imgur.com/7M0xN1g.jpg" alt="Change Value"></p><p>所以，其实还是在触发调用 URL Scheme，其中 </p><ol><li><code>document.title</code> 为标题</li><li><code>window.location</code> 为当前页面链接</li></ol><p>我们知道 Things 的 URL Scheme 为 </p><p><code>things:///add?title=iamtitle&amp;notes=iamnotes&amp;when=tomorrow</code></p><p>比如你可以把链接内容改为支持 Send To Things：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">javascript:window.location=&apos;things:///add?title=&apos;+encodeURIComponent(document.title)+&apos;&amp;notes=&apos;+encodeURIComponent(window.location)+&apos;&amp;when=today&apos;</span><br></pre></td></tr></table></figure><p>以及 Send To OmniFocus ，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">javascript:window.location=&apos;omnifocus:///add?note=&apos;+encodeURIComponent(window.location)+&apos;&amp;name=&apos;+encodeURIComponent(document.title)</span><br></pre></td></tr></table></figure><p>或者直接去 <a href="http://people.omnigroup.com/kc/OmniFocus/SendToOmniFocusBookmarklet.html" target="_blank" rel="noopener">OmniFocus 页面</a> 用相同方式把起已经设置好正确 Value 的链接拖到上方工具条上即可。</p><p>如果你找不到 Favorites Bar，点击 Safari 菜单栏上的 View 菜单就能看到（中文系统叫视图）</p><p><img src="https://i.imgur.com/ZBqkn4Z.jpg" alt="Favorites Bar"></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;使用鼠标拖拽下面这个链接到你的 Favorites Bar 上，&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;javascript:window.location=&amp;#39;&amp;#39;&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Send to 2Do&lt;/a&gt;&lt;/p
      
    
    </summary>
    
      <category term="Productivity" scheme="http://hechen.xyz/categories/Productivity/"/>
    
    
      <category term="Bookmark,2Do,Productivity,Things" scheme="http://hechen.xyz/tags/Bookmark-2Do-Productivity-Things/"/>
    
  </entry>
  
  <entry>
    <title>阅读流程</title>
    <link href="http://hechen.xyz/2018/10/16/MyReadingProcess/"/>
    <id>http://hechen.xyz/2018/10/16/MyReadingProcess/</id>
    <published>2018-10-16T09:01:44.000Z</published>
    <updated>2018-10-21T06:07:01.000Z</updated>
    
    <content type="html"><![CDATA[<p>最近统一了一下自己的阅读流程，更新一下，具体用到的工具有</p><ol><li>DEVONThink Pro</li><li>PDFExpert</li><li>MarginNote</li></ol><p>其中 1 是输入和归档源，3 是输出源。 大概就是如下所示：</p><p><img src="https://i.imgur.com/SrcVtP1.png" alt="Reading Process"></p><h2 id="采集素材"><a href="#采集素材" class="headerlink" title="采集素材"></a>采集素材</h2><p>在平时浏览网页的时候，看到自己喜欢的文章之后，想进行一些标记或者做笔记，大概有几个选择：</p><ol><li>印象笔记直接 clip 当前网页，之后在印象笔记里进行阅读或者标记</li><li>放进稍后读服务中，比如 Pocket 或者 Instapaper 等；</li><li>用 Safari 自带的 Save as PDF 之后再用单独的 PDF 工具查看；</li></ol><p>而最近几个月，因为 MarginNote 3 的发布，由于其提供的大纲脑图的自动生成，给我提供了一种便利的拆书功能，因此会把平时看到的文章都归集到一处来看。</p><p>借助 DEVONthink 的 Clip To DEVONthink 的插件可以很方便的把文章归集到 DEONThink 中，最主要的是因为它能保存 WebArchive，即使后续文章被删除也没关系。</p><p><img src="https://i.imgur.com/Kw7CoBl.png" alt="Clip To DEVONthink"></p><p>当按下 Cmmd + S 进行归档之后，DEVONThink 对应 Group 里就会把该原始页面保存起来。</p><p><img src="https://i.imgur.com/m3jM9kx.png" alt="DEVONthink"></p><p>由于 WebArchive 无法做 Highlight 标记，这时候就用到了 DEVONthink 提供的 OCR 功能，将 WebArchive 提取为 PDF 文件。</p><p><img src="https://i.imgur.com/YxEsN2G.png" alt="WebArchive To PDF"></p><h2 id="二次加工处理"><a href="#二次加工处理" class="headerlink" title="二次加工处理"></a>二次加工处理</h2><p>当你使用 DEVONthink 生成 PDF 之后，就可以直接用其自带的 PDF 阅读工具进行阅读了，它默认提供工具已经完全满足，不过我想利用 MarginNote 来阅读，因此这时候你可以选择直接发送到 MarginNote 来阅读。</p><p>其实更多时候，我会选择先使用 PDF Expert 进行 PDF 文件的裁剪，因为大多数时候网页正文内容两侧留白太多，想裁剪掉。</p><p><img src="https://i.imgur.com/8I8UcFY.png" alt="PDF With Margin"></p><p>裁剪掉多余的部分之后，pdf 文件就可读性也会极大提高。<br><img src="https://i.imgur.com/1CwG33x.png" alt="Final PDF"></p><h2 id="阅读输出"><a href="#阅读输出" class="headerlink" title="阅读输出"></a>阅读输出</h2><p>到了这一步，我们就可以把文档导入到 MarginNote 中，如图所示：<br><img src="https://i.imgur.com/DmADrxk.png" alt="MarginNote Import"></p><p>使用 <strong>Add to Study</strong>，将自己的标记和笔记生成笔记本，MarginNote 强大就在于能够很好的自动帮你把重点归集到一起，生成脑图关联，你还可以针对某个部分做笔记。</p><p><img src="https://i.imgur.com/9uvu95H.png" alt="MarginNote Add To Study"></p><p>而且很多时候，我们看一篇文章很大可能会展开这个主题，比如看下该文章下给的参考链接，这些往往都是同样主题的内容，我们希望相关的文章能够整体归集到一个笔记本下。MarginNote 可以很方便做到，比如下图，我在阅读 Apple 官方 URL Session Programming Guide 的时候会查看很多相关文章，也都是相关内容，我会把笔记整理在一起。</p><p><img src="https://i.imgur.com/kIvn6v8.png" alt="MarginNote Note Group"></p><p>我单独在 MarginNote 里建了一个 Folder ，叫 Article。专门阅读我从 DEVONthink 中采集到的文章。阅读完毕，还可以将阅读笔记归档，MarginNote 支持分享到 印象笔记，Word 或者 DEVONthink 进行归档。</p><p><img src="https://i.imgur.com/yyQ3pOr.png" alt="Export"></p><p>到了这里，基本上对我来讲一个 输入 ── 处理 ── 阅读 ── 输出 的整个流程就结束了。</p><p>上面提到的三个工具，均有 iOS 版本，也就意味着即使拿着手机也能够不破坏整个阅读环节，不过其中 DEVONthink 提供的 iOS 版本是没办法进行 WebArchive 向 PDF 的转换的，处理这一步最后还是得放在 Mac 端处理，不过解决了输入和输出也就意味着随时随地可以捕获想看的内容放到稍后读列表，然后自动同步到 Mac 端进行处理，然后在 MarginNote 对应的 iOS 版本，平时在家的时候还是习惯用 iPad Pro 查看，体验也不错。</p><p><img src="https://i.imgur.com/ZJsdxyS.jpg" alt="iPad MarginNote"></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;最近统一了一下自己的阅读流程，更新一下，具体用到的工具有&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;DEVONThink Pro&lt;/li&gt;
&lt;li&gt;PDFExpert&lt;/li&gt;
&lt;li&gt;MarginNote&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;其中 1 是输入和归档源，3 是输出源。 大概就是如下
      
    
    </summary>
    
      <category term="Reading" scheme="http://hechen.xyz/categories/Reading/"/>
    
    
      <category term="DEVONThink, MarginNote, PDFExpert" scheme="http://hechen.xyz/tags/DEVONThink-MarginNote-PDFExpert/"/>
    
  </entry>
  
  <entry>
    <title>三个简单步骤让你测试使用系统单例的代码</title>
    <link href="http://hechen.xyz/2018/07/17/Testing-Swift-code-that-uses-system-singletons-in-3-easy-steps/"/>
    <id>http://hechen.xyz/2018/07/17/Testing-Swift-code-that-uses-system-singletons-in-3-easy-steps/</id>
    <published>2018-07-17T09:09:57.000Z</published>
    <updated>2018-07-17T09:54:43.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.swiftbysundell.com/posts/testing-swift-code-that-uses-system-singletons-in-3-easy-steps" target="_blank" rel="noopener">Testing Swift code that uses system singletons in 3 easy steps</a><br>原作者 &amp; Copyright <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">@johnsundell</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>大部分在 Apple 平台开发的 App 都会依赖基于单例的 API。从 UIScreen 到 UIApplication，再到 NSBundle，而 Foundation，UIKit 以及 AppKit 里到处充斥着静态的 API。</p><p>尽管单例非常方便，并且随时随地都可以很轻易的获取到特定的 API，但是当它们一旦要面临代码解耦和测试的时候就会出现挑战。单例同时也是平时遇到 Bug 里最常见的一种，在单例中状态会以共享的方式终结，那些针对状态做的变更并不能很好的广播到整个系统层面。</p><p>然而，尽管我们能够重构自己的代码，让它们只在真正需求的地方使用单例，我们针对系统 API 也做不了太多。但是，好消息是，这里有一些技巧让你使用系统单例的代码依然变得容易管理和测试。</p><p>让我们看一些使用了 URLSession.shared 单例的代码：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DataLoader</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">enum</span> <span class="title">Result</span> </span>&#123;</span><br><span class="line">        <span class="keyword">case</span> data(<span class="type">Data</span>)</span><br><span class="line">        <span class="keyword">case</span> error(<span class="type">Error</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">load</span><span class="params">(from url: URL, completionHandler: @escaping <span class="params">(Result)</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">        <span class="keyword">let</span> task = <span class="type">URLSession</span>.shared.dataTask(with: url) &#123; (data, response, error) <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">                <span class="keyword">return</span> completionHandler(.error(error))</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            completionHandler(.data(data ?? <span class="type">Data</span>()))</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        task.resume()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面这个 <strong>DataLoader</strong> 现在就变得很难测试，因为它内部会自主调用共享的 URL Session 来执行一个网络调用。这就需要我们为测试代码中增加一些等待和超时代码，然后很快这部分代码就会变得糟糕和不稳定。</p><h3 id="抽象成一个协议"><a href="#抽象成一个协议" class="headerlink" title="抽象成一个协议"></a>抽象成一个协议</h3><p>我们第一个任务就是把我们需要的 URLSession 的部分移到一个协议里，这样我们在测试中也能够很容易的进行 mock。在作者的<a href="http://www.ustream.tv/recorded/101118612" target="_blank" rel="noopener">一次 talk</a>中作者建议尽可能的避免 mock，尽管它是一种很好的策略。但是当你和系统的单例打交道的时候，mock 就是一个增加可预测性的重要工具。</p><p>让我们创建一个 <strong>NetworkEngine</strong> 协议，然后让 URLSession 符合它:</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">protocol</span> <span class="title">NetworkEngine</span> </span>&#123;</span><br><span class="line">    <span class="keyword">typealias</span> <span class="type">Handler</span> = (<span class="type">Data</span>?, <span class="type">URLResponse</span>?, <span class="type">Error</span>?) -&gt; <span class="type">Void</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">performRequest</span><span class="params">(<span class="keyword">for</span> url: URL, completionHandler: @escaping Handler)</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">URLSession</span>: <span class="title">NetworkEngine</span> </span>&#123;</span><br><span class="line">    <span class="keyword">typealias</span> <span class="type">Handler</span> = <span class="type">NetworkEngine</span>.<span class="type">Handler</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">performRequest</span><span class="params">(<span class="keyword">for</span> url: URL, completionHandler: @escaping Handler)</span></span> &#123;</span><br><span class="line">        <span class="keyword">let</span> task = dataTask(with: url, completionHandler: completionHandler)</span><br><span class="line">        task.resume()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上所见，我们使得 <strong>URLSessionDataTask</strong> 成了 <strong>URLSession</strong> 的一个实现细节。这样，我们就避免了在测试代码中不得不创建不同的 mock ，而只需要关注 NetworkEngine 就行。</p><h3 id="协议中把单例作为默认值"><a href="#协议中把单例作为默认值" class="headerlink" title="协议中把单例作为默认值"></a>协议中把单例作为默认值</h3><p>现在，让我们更新我们的 <strong>DataLoader</strong> 来使用新的 NetworkEngine 协议，把它作为依赖项注入。我们把 URLSession.shared 作为默认参数传递，以此做到了向后兼容并且和之前一样方便。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DataLoader</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">enum</span> <span class="title">Result</span> </span>&#123;</span><br><span class="line">        <span class="keyword">case</span> data(<span class="type">Data</span>)</span><br><span class="line">        <span class="keyword">case</span> error(<span class="type">Error</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> engine: <span class="type">NetworkEngine</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span>(engine: <span class="type">NetworkEngine</span> = <span class="type">URLSession</span>.shared) &#123;</span><br><span class="line">        <span class="keyword">self</span>.engine = engine</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">load</span><span class="params">(from url: URL, completionHandler: @escaping <span class="params">(Result)</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">        engine.performRequest(<span class="keyword">for</span>: url) &#123; (data, response, error) <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">                <span class="keyword">return</span> completionHandler(.error(error))</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            completionHandler(.data(data ?? <span class="type">Data</span>()))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过使用默认参数，我们依然可以像之前一样很容易的生成 <strong>DataLoader</strong>，而不需要提供一个 <strong>NetworkEngine</strong>。</p><h3 id="在你的测试中-Mock-协议"><a href="#在你的测试中-Mock-协议" class="headerlink" title="在你的测试中 Mock 协议"></a>在你的测试中 Mock 协议</h3><p>最后，让我们写个测试，在这里我们通过 mock <strong>NetworkEngine</strong> 使得我们的测试快速，可预测并且容易维护。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">testLoadingData</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">NetworkEngineMock</span>: <span class="title">NetworkEngine</span> </span>&#123;</span><br><span class="line">        <span class="keyword">typealias</span> <span class="type">Handler</span> = <span class="type">NetworkEngine</span>.<span class="type">Handler</span> </span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> requestedURL: <span class="type">URL</span>?</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">func</span> <span class="title">performRequest</span><span class="params">(<span class="keyword">for</span> url: URL, completionHandler: @escaping Handler)</span></span> &#123;</span><br><span class="line">            requestedURL = url</span><br><span class="line"></span><br><span class="line">            <span class="keyword">let</span> data = “<span class="type">Hello</span> world”.data(using: .utf8)</span><br><span class="line">            completionHandler(data, <span class="literal">nil</span>, <span class="literal">nil</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> engine = <span class="type">NetworkEngineMock</span>()</span><br><span class="line">    <span class="keyword">let</span> loader = <span class="type">DataLoader</span>(engine: engine)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> result: <span class="type">DataLoader</span>.<span class="type">Result</span>?</span><br><span class="line">    <span class="keyword">let</span> url = <span class="type">URL</span>(string: “my/<span class="type">API</span>”)!</span><br><span class="line">    loader.load(from: url) &#123; result = $<span class="number">0</span> &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">XCTAssertEqual</span>(engine.requestedURL, url)</span><br><span class="line">    <span class="type">XCTAssertEqual</span>(result, .data(“<span class="type">Hello</span> world”.data(using: .utf8)!))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上所见，作者试图让 mock 尽可能的简单。不需要创建包含大量逻辑的复杂 mock，你只需要让自己的代码返回一些 hardcode 的值通常是一个好主意，这样，你能够在自己的测试中进行断言。然而，风险就是你可能最后测试的是自己的 mock 而不是生产环境的代码。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>我们现在通过以下三个步骤让这些依然方便的使用了系统的单例可被测试：</p><ol><li>抽象协议</li><li>协议中把单例作为默认值</li><li>在你的测试中 Mock 该协议</li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.swiftbysundell.com/posts/testing-swift-code-that-uses-system-singletons-in-3-easy-steps&quot; target=&quot;_bl
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="XCode" scheme="http://hechen.xyz/tags/XCode/"/>
    
      <category term="Singleton" scheme="http://hechen.xyz/tags/Singleton/"/>
    
  </entry>
  
  <entry>
    <title>避免在 Swift 中使用单例</title>
    <link href="http://hechen.xyz/2018/07/16/Avoiding-singletons-in-Swift/"/>
    <id>http://hechen.xyz/2018/07/16/Avoiding-singletons-in-Swift/</id>
    <published>2018-07-16T09:34:36.000Z</published>
    <updated>2018-10-07T11:17:57.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://ppinera.es/2017/09/29/modular-xcode-projects.html" target="_blank" rel="noopener">Modular Xcode projects</a><br>原作者 &amp; Copyright <a href="http://twitter.com/pepibumur" target="_blank" rel="noopener">@pepibumur</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>“我知道单例不好，但是…”，这是开发者常常在讨论代码的时候会提到的。貌似社区大家有共识 ── 单例不好。但是同时，包括 Apple 和第三方的 Swift 开发者还是在 App 内部或者共享的 frameworks 里不断在用它们。</p><p>这周，让我们好好看一看单例的问题到底在哪里，探索更多的技巧以便我们日常能够避免使用它们。这就开始吧！</p><h3 id="为什么单例这么流行？"><a href="#为什么单例这么流行？" class="headerlink" title="为什么单例这么流行？"></a>为什么单例这么流行？</h3><p>首先一点我们要想的是问问自己，为什么单例这么流行？热过大多数开发者都认同，单例应该被避免的话，为什么它们还不断的出现呢？</p><p>我认为有两部分原因。</p><p>我觉得日常开发 Apple 平台上的 App 时候大量使用单例，最大的原因是苹果自己也在经常的用。作为第三方开发者来讲，我们常常会把 Apple 的做法作为最佳实践来用，任何 Apple 自己常常使用的模式在社区也会发展的很迅速。</p><p>这个谜题的第二个原因是单例非常方便。单例因为它可以在任何地方获取到，因而经常会扮演一个获取特定核心值或者对象的快捷方式。可以看下下面这个例子，在这个例子中，我们想在 <strong>ProfileViewController</strong> 中展示当前登录用户的名字，同时当某个按钮按下的时候，退登当前用户。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProfileViewController</span>: <span class="title">UIViewController</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">lazy</span> <span class="keyword">var</span> nameLabel = <span class="type">UILabel</span>()</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.viewDidLoad()</span><br><span class="line">        nameLabel.text = <span class="type">UserManager</span>.shared.currentUser?.name</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">func</span> <span class="title">handleLogOutButtonTap</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="type">UserManager</span>.shared.logOut()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>按照上面这样，把一个 UserManager 单例的用户和账户处理的功能封装起来，确实很方便（而且很常见！），那这种模式的使用到底不好在哪里呢？</p><h3 id="为什么单例会很糟糕？"><a href="#为什么单例会很糟糕？" class="headerlink" title="为什么单例会很糟糕？"></a>为什么单例会很糟糕？</h3><p>当讨论类似模式和架构这些东西的时候，很容易陷入一个特别理论化的圈套。虽然让我们的代码理论上“正确”，遵循最佳实践和原则是很美好的，但是现实经常会很受打击，我们需要寻找一些中间方案。</p><p>所以，关于单例引起的实质性问题是什么呢？为什么它们要应该被避免呢？这里我倾向于三个原因：</p><ol><li>这些单例是全局的可变的共享状态。它们的状态是在整个 App 里自动共享的，因此当其状态发生不可预期的变化的时候，Bug 也就可能开始发生了；</li><li>单例之间的关系以及依赖这些单例的代码常常不那么好定义。因为单例是如此方便和容易获得，大量的使用它们常常会导致很难维护<a href="https://zh.wikipedia.org/wiki/%E9%9D%A2%E6%9D%A1%E5%BC%8F%E4%BB%A3%E7%A0%81" target="_blank" rel="noopener">面条式代码</a>，这些代码中对象和对象之间没有清晰的隔离；</li><li>管理这些单例的生命周期非常的难。因为单例本身会在应用的整个生命周期内存活，管理它们就变得异常困难，因此常常会不得不依赖可选值来跟踪值的变化。这也使得依赖单例的代码变得很难测试，因为你没法在一个测试 case 里把状态重置。</li></ol><p>在我们前面的 <strong>ProfileViewController</strong> 的例子中，我们已经看到了有这三个问题的信号出现。首先它依赖 UserManager 导致这二者关系很不清晰，其次，它不得不让 currentUser 作为可选值出现，因为我们没办法在编译期就能够确定视图控制器实际上出现的时候这个数据一定在。听起来就感觉要有 bug 发生的感觉 😬!</p><h3 id="依赖注入"><a href="#依赖注入" class="headerlink" title="依赖注入"></a>依赖注入</h3><p>相对于使用 <strong>ProfileViewController</strong> 通过单例来获取它所需的依赖项，我们要在其初始化方法中将依赖项传入。这里我们是将当前的 User 作为非可选传入的，同样，传入一个 <strong>LogOutService</strong> 来进行登出操作：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProfileViewController</span>: <span class="title">UIViewController</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> user: <span class="type">User</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> logOutService: <span class="type">LogOutService</span></span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">lazy</span> <span class="keyword">var</span> nameLabel = <span class="type">UILabel</span>()</span><br><span class="line">    <span class="keyword">init</span>(user: <span class="type">User</span>, logOutService: <span class="type">LogOutService</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.user = user</span><br><span class="line">        <span class="keyword">self</span>.logOutService = logOutService</span><br><span class="line">        <span class="keyword">super</span>.<span class="keyword">init</span>(nibName: <span class="literal">nil</span>, bundle: <span class="literal">nil</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.viewDidLoad()</span><br><span class="line">        nameLabel.text = user.name</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">func</span> <span class="title">handleLogOutButtonTap</span><span class="params">()</span></span> &#123;</span><br><span class="line">        logOutService.logOut()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样的话，代码就会变得更加清晰，并且容易管理。我们的代码现在安全的依赖其 Model，并且有一个清晰的 API 进行登出的交互。一般情况下，将各种单例以及管理器重构成清晰分离的服务能够使得一个 App 的核心对象之间的关系更加清晰。</p><h3 id="Services"><a href="#Services" class="headerlink" title="Services"></a>Services</h3><p>举个例子，让我们更近一些来看下 <strong>LogOutService</strong> 这个类是如何实现的。它内部也对其依赖的服务使用了依赖注入，并且提供了一个优雅的，定义清晰的 API 来只做一件事情 ── 登出。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LogOutService</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> user: <span class="type">User</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> networkService: <span class="type">NetworkService</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> navigationService: <span class="type">NavigationService</span></span><br><span class="line">    <span class="keyword">init</span>(user: <span class="type">User</span>,</span><br><span class="line">         networkService: <span class="type">NetworkService</span>,</span><br><span class="line">         navigationService: <span class="type">NavigationService</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.user = user</span><br><span class="line">        <span class="keyword">self</span>.networkService = networkService</span><br><span class="line">        <span class="keyword">self</span>.navigationService = navigationService</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">logOut</span><span class="params">()</span></span> &#123;</span><br><span class="line">        networkService.request(.logout(user)) &#123; [<span class="keyword">weak</span> <span class="keyword">self</span>] <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">self</span>?.navigationService.showLoginScreen()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="翻新"><a href="#翻新" class="headerlink" title="翻新"></a>翻新</h3><p>从一个重度使用单例的设置到完全使用服务，依赖注入以及本地状态来改造会非常的困难，会花费大量时间。而且，会很难认为花大量时间在这上面是合理的，而且有些时候可能会需要一次更大规模的重构才行。</p><p>谢天谢地，我们可以使用相近的技术，在<a href="https://www.swiftbysundell.com/posts/testing-swift-code-that-uses-system-singletons-in-3-easy-steps" target="_blank" rel="noopener">Testing Swift code that uses system singletons in 3 easy steps</a>这篇文章中也使用到过，其允许我们可以以一种非常容易的方式单例开始。和很多其他解决方案类似 ── 协议来救场！</p><p>我们不是一次性重构所有的单例，而是创建 Service 类，我们可以很简单的把我们的 Service 定义成 Protocol，如下所示：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">protocol</span> <span class="title">LogOutService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">logOut</span><span class="params">()</span></span></span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">protocol</span> <span class="title">NetworkService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">request</span><span class="params">(<span class="number">_</span> endpoint: Endpoint, completionHandler: @escaping <span class="params">()</span></span></span> -&gt; <span class="type">Void</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">protocol</span> <span class="title">NavigationService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">showLoginScreen</span><span class="params">()</span></span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">showProfile</span><span class="params">(<span class="keyword">for</span> user: User)</span></span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后我们就能通过将单例符合我们新创建的 Service 协议来使其翻新为一堆 Service。在很多情况下，我们甚至不需要改变任何实现，只是简单的传递它们 shared 实例作为一个 Service 即可。</p><p>同样的技巧也能够被用作重构我们 app 中其他核心对象，那些对象我们也许也都正在以某种单例形式在使用着，例如使用 AppDelegate 来做导航。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">UserManager</span>: <span class="title">LoginService</span>, <span class="title">LogOutService</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">AppDelegate</span>: <span class="title">NavigationService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">showLoginScreen</span><span class="params">()</span></span> &#123;</span><br><span class="line">        navigationController.viewControllers = [</span><br><span class="line">            <span class="type">LoginViewController</span>(</span><br><span class="line">                loginService: <span class="type">UserManager</span>.shared,</span><br><span class="line">                navigationService: <span class="keyword">self</span></span><br><span class="line">            )</span><br><span class="line">        ]</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">showProfile</span><span class="params">(<span class="keyword">for</span> user: User)</span></span> &#123;</span><br><span class="line">        <span class="keyword">let</span> viewController = <span class="type">ProfileViewController</span>(</span><br><span class="line">            user: user,</span><br><span class="line">            logOutService: <span class="type">UserManager</span>.shared</span><br><span class="line">        )</span><br><span class="line">        navigationController.pushViewController(viewController, animated: <span class="literal">true</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们现在通过使用依赖注入以及 Service 的方式，开始使得我们工程处在 Singleton free 的状态，而不需要进行特别巨大的工程改造和重写。接下来我们就能够使用 Service 或者其他类型的 API 逐个替换掉单例，比如说使用<a href="https://www.swiftbysundell.com/posts/replacing-legacy-code-using-swift-protocols" target="_blank" rel="noopener">这篇博文</a>中的技巧。</p><h3 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h3><p>单例也不是都是不好的，只是在很多情况下，它们会引发一系列问题，这些问题可以通过使用依赖注入的方式为你程序里的对象建立良好的关系来解决。</p><p>如果你现在正在开发的 App 里在使用大量的单例，那你一定也在经历或者已经经历过它们所带来的 bug 了吧。希望这篇文章能够给你一些灵感让你没有那么慌乱的开始远离它们。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://ppinera.es/2017/09/29/modular-xcode-projects.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Modular Xcode projects
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="XCode" scheme="http://hechen.xyz/tags/XCode/"/>
    
      <category term="Singleton" scheme="http://hechen.xyz/tags/Singleton/"/>
    
  </entry>
  
  <entry>
    <title>Swift 中几种不同的依赖注入方式</title>
    <link href="http://hechen.xyz/2018/07/16/Different-flavors-of-dependency-injection-in-Swift/"/>
    <id>http://hechen.xyz/2018/07/16/Different-flavors-of-dependency-injection-in-Swift/</id>
    <published>2018-07-16T05:57:24.000Z</published>
    <updated>2018-07-17T04:01:34.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.swiftbysundell.com/posts/different-flavors-of-dependency-injection-in-swift" target="_blank" rel="noopener">Different flavors of dependency injection in Swift</a><br>原作者 &amp; Copyright <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">@johnsundell</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>在之前的几篇博客中，我们已经了解了几种使用依赖注入方式使得某个 swift app 拥有一个更加解耦可测试的架构。比如在 <a href="http://hechen.xyz/2017/11/19/Dependency-injection-using-factories-in-Swift/">在 Swift 中使用工厂模式进行依赖注入</a> 中和工厂模式结合，以及在<a href="http://hechen.xyz/2018/07/16/Avoiding-singletons-in-Swift/">避免在 Swift 中使用单例</a>中替换程序中的单例对象等方式进行依赖注入。</p><p>到目前为止，大部分我的博文以及例子中都使用了基于初始化的依赖注入方式。然而，就像大部分的编程技巧一样，还有很多 “口味” 的进行依赖注入的方式 ── 每一种都有其优缺点。 这周，让我们来看看其中三种方式以及如何在 Swift 中使用它们。</p><h3 id="基于初始化方法"><a href="#基于初始化方法" class="headerlink" title="基于初始化方法"></a>基于初始化方法</h3><p>让我们快速回顾一下用的最为广泛的依赖注入方式 ── initializer-based。这个想法是在某个类被初始化的时候传入其所需的依赖的方式。这种形式最大的好处就是，它保证了能够立即满足该类完成其功能所需要的所有东西。</p><p>让我们建一个类 ── FileLoader 来从磁盘加载文件。为了实现该 Loader 的功能，需要两个依赖项，系统所提供的实例对象 ── <strong>FileManager</strong> 和 <strong>Cache</strong>。使用基于初始化的依赖注入的执行代码如下所示：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FileLoader</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> fileManager: <span class="type">FileManager</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> cache: <span class="type">Cache</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span>(fileManager: <span class="type">FileManager</span> = .<span class="keyword">default</span>,</span><br><span class="line">         cache: <span class="type">Cache</span> = .<span class="keyword">init</span>()) &#123;</span><br><span class="line">        <span class="keyword">self</span>.fileManager = fileManager</span><br><span class="line">        <span class="keyword">self</span>.cache = cache</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以关注下初始化方法里默认参数的设置，其可以避免每次都需要自行创建的问题。这样能够简化我们在生产环境下使用 <strong>FileLoader</strong> 类创建文件加载器的工作，而且也能够能够传入 mock 数据或者我们的测试代码中的创建的实例进行测试。</p><h3 id="基于属性"><a href="#基于属性" class="headerlink" title="基于属性"></a>基于属性</h3><p>虽然前一种基于初始化的依赖注入形式常常给我们自定义类带来很多好处，但是有时候，当你不得不继承自某个系统类的时候会遇到一些困难。其中一个例子就是，当我们构建 view controller，尤其是你在使用 XIB 以及 Storyboard 类定义它们的时候，因为此时你不再能够掌控它们的初始化方法了。</p><p>对于这些情况，基于属性的依赖注入方式是一个更好的选择。相对于初始化的时候进行对象的依赖项注入，基于属性的形式可以在之后通过简单的赋值来做到，其能够让你减少模板代码的书写，尤其是在你确实没有必要进行注入的时候能够有更好的默认实现。</p><p>让我们再来看一个例子，在这个例子中，我们在构建一个 <strong>PhotoEditorViewController</strong> ，这个视图控制器使得用户编辑它们照片库中的图片。为了达到这个功能，该视图控制器需要用到系统提供的 PHPhotoLibrary 类型的一个实例（该类本身是一个单例）以及我们自己实现的类 <strong>PhotoEditorEngine</strong> 的一个实例。那为了不通过自定义初始化方法进行依赖注入的话，我们可以创建一些具有默认值的可变属性，例如下面这样：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PhotoEditorViewController</span>: <span class="title">UIViewController</span> </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> library: <span class="type">PhotoLibrary</span> = <span class="type">PHPhotoLibrary</span>.shared()</span><br><span class="line">    <span class="keyword">var</span> engine = <span class="type">PhotoEditorEngine</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 <a href="https://www.swiftbysundell.com/posts/testing-swift-code-that-uses-system-singletons-in-3-easy-steps" target="_blank" rel="noopener">Testing Swift code that uses system singletons in 3 easy steps</a> 这篇文章中的手法，通过使用一个协议提供一个更为抽象的 <strong>PhotoLibrary</strong> 接口来获取系统的图片库。这样会使得测试和 Mock 数据特别的容易。</p><p>上面这些工作比较好的是我们依然能够通过简单的给视图控制器来赋值，从而在我们的测试中简单的注入 Mock 代码：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PhotoEditorViewControllerTests</span>: <span class="title">XCTestCase</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">testApplyingBlackAndWhiteFilter</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">let</span> viewController = <span class="type">PhotoEditorViewController</span>()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Assign a mock photo library to gain complete control over</span></span><br><span class="line">        <span class="comment">// what photos are stored in it</span></span><br><span class="line">        <span class="keyword">let</span> library = <span class="type">PhotoLibraryMock</span>()</span><br><span class="line">        library.photos = [<span class="type">TestPhotoFactory</span>.photoWithColor(.red)]</span><br><span class="line">        viewController.library = library</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Run our testing commands</span></span><br><span class="line">        viewController.selectPhoto(atIndex: <span class="number">0</span>)</span><br><span class="line">        viewController.apply(<span class="built_in">filter</span>: .blackAndWhite)</span><br><span class="line">        viewController.savePhoto()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Assert that the outcome is correct</span></span><br><span class="line">        <span class="type">XCTAssertTrue</span>(photoIsBlackAndWhite(library.photos[<span class="number">0</span>]))</span><br><span class="line">    &#125;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="基于参数"><a href="#基于参数" class="headerlink" title="基于参数"></a>基于参数</h3><p>最后一种，让我们来看下基于参数的依赖注入形式。这种方式尤其在你想让旧有代码更加可测试，而不需要对现存的结构进行更多改动的时候尤其有效。</p><p>许多时候，我们仅需要某个特定的依赖项一次，或者我们仅仅需要在某些特定条件下进行 mock。不需要改变某个对象的初始化方法或者暴露可变属性（通常都不是一个好主意），我们可以开发出一个接受一个依赖项作为参数的特定 API。</p><p>让我们看一个 <strong>NoteManager</strong> 类，该类是某个笔记类应用的一部分。它的工作就是管理所有用户已经书写的笔记，提供一个 API 让用户能够查询笔记。考虑到这个操作可能耗时（如果用户有许多笔记，通常情况下是这样），我们把该动作正常放置于一个后台线程执行，如下：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NoteManager</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">loadNotes</span><span class="params">(matching query: String,</span></span></span><br><span class="line"><span class="function"><span class="params">                   completionHandler: @escaping <span class="params">([Note])</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">        <span class="type">DispatchQueue</span>.global(qos: .userInitiated).async &#123;</span><br><span class="line">            <span class="keyword">let</span> database = <span class="keyword">self</span>.loadDatabase()</span><br><span class="line">            <span class="keyword">let</span> notes = database.<span class="built_in">filter</span> &#123; note <span class="keyword">in</span></span><br><span class="line">                <span class="keyword">return</span> note.matches(query: query)</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            completionHandler(notes)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>尽管上面的代码在我们的生产环境里也算是一个很好的解决方案，但在测试中我们正常情况下是想避免一些异步代码的，尽可能的平行化以避免 <a href="https://www.swiftbysundell.com/posts/reducing-flakiness-in-swift-tests" target="_blank" rel="noopener">flakiness</a>. 如果要类似于基于初始化或者基于属性的依赖注入能够指定一个显式的队列提供给 NoteManager 来用的方式，需要对该类进行很多改变，这是我们在当下无法做或者不愿意做的。</p><p>这时候，基于参数的依赖注入方式的实现就能够达到。相对于不得不对整个类进行重构，我们通过插入队列相关代码使得 <code>loadNotes</code> 方法在该队列上执行：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NoteManager</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">loadNotes</span><span class="params">(matching query: String,</span></span></span><br><span class="line"><span class="function"><span class="params">                   on queue: DispatchQueue = .global<span class="params">(qos: .userInitiated)</span></span></span>,</span><br><span class="line">                   completionHandler: @escaping ([<span class="type">Note</span>]) -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">        queue.async &#123;</span><br><span class="line">            <span class="keyword">let</span> database = <span class="keyword">self</span>.loadDatabase()</span><br><span class="line">            <span class="keyword">let</span> notes = database.<span class="built_in">filter</span> &#123; note <span class="keyword">in</span></span><br><span class="line">                <span class="keyword">return</span> note.matches(query: query)</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            completionHandler(notes)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样就使得我们能够很容易的在测试代码中使用某个定制的我们可等待的队列。这样也几乎使得我们把上面的接口转变成了一个同步接口，使得一切变得更加容易和可预测。</p><p>另外一个基于参数的依赖注入的使用案例是当你想测试某个静态的 API 的时候。因为静态的 API 我们是不存在初始化方法的，同时我们理想情况下也是不应该维护任何静态的状态的，那基于参数的依赖注入方式就是一个很好的选择。让我们看一个静态的类 <strong>MessageSender</strong>， 该类当前依赖某个单例：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MessageSender</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="function"><span class="keyword">func</span> <span class="title">send</span><span class="params">(<span class="number">_</span> message: Message, to user: User)</span></span> <span class="keyword">throws</span> &#123;</span><br><span class="line">        <span class="type">Database</span>.shared.insert(message)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">let</span> data: <span class="type">Data</span> = <span class="keyword">try</span> wrap(message)</span><br><span class="line">        <span class="keyword">let</span> endpoint = <span class="type">Endpoint</span>.sendMessage(to: user)</span><br><span class="line">        <span class="type">NetworkManager</span>.shared.post(data, to: endpoint.url)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，一个长远的解决方案更可能是重构 <strong>MessageSender</strong> 这个类，让它变成一个非静态的，能够在其所有被使用的地方被正确的注入依赖。但是基于我们能够更容易的测试它（比如，重现或者验证某个 Bug），我们能简单的使用参数作为依赖项进行注入，而不是基于某个单例：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MessageSender</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="function"><span class="keyword">func</span> <span class="title">send</span><span class="params">(<span class="number">_</span> message: Message,</span></span></span><br><span class="line"><span class="function"><span class="params">                     to user: User,</span></span></span><br><span class="line"><span class="function"><span class="params">                     database: Database = .shared,</span></span></span><br><span class="line"><span class="function"><span class="params">                     networkManager: NetworkManager = .shared)</span></span> <span class="keyword">throws</span> &#123;</span><br><span class="line">        database.insert(message)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">let</span> data: <span class="type">Data</span> = <span class="keyword">try</span> wrap(message)</span><br><span class="line">        <span class="keyword">let</span> endpoint = <span class="type">Endpoint</span>.sendMessage(to: user)</span><br><span class="line">        networkManager.post(data, to: endpoint.url)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们又再次使用了缺省的参数，不仅仅更加方便而已，更重要的是，能够在 100% 向后兼容的情况下为我们的代码增加测试支持。</p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>所以，哪一种依赖注入的方案是最好的呢？就像大部分时候一样，我的答案很无聊的： 看情况！我在该博客中所视图去做的一件事情就是针对某个特定问题给出需要不同的解决方案。原因很简单，我真的不相信又所谓银弹的存在。我认为，按照我们意愿具备多个工具或者对于特定技巧的多种解决方案能够使我们变得更好，在写代码的时候也能够游刃有余。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.swiftbysundell.com/posts/different-flavors-of-dependency-injection-in-swift&quot; target=&quot;_blank&quot; rel=&quot;no
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="XCode" scheme="http://hechen.xyz/tags/XCode/"/>
    
  </entry>
  
  <entry>
    <title>那些我恢复 Mac 系统之后要安装的 Apps</title>
    <link href="http://hechen.xyz/2018/07/11/My-Favorite-Mac-Apps/"/>
    <id>http://hechen.xyz/2018/07/11/My-Favorite-Mac-Apps/</id>
    <published>2018-07-11T04:58:37.000Z</published>
    <updated>2018-10-07T11:09:23.000Z</updated>
    
    <content type="html"><![CDATA[<p>记录一下自己常用的 Mac Apps，每次重装之后到了用的时候发现忘记安装了(不是每次都用 TimeMachine 恢复)，而且强烈简易大家如果某个 App 有对应的独立安装版本的话，购买其独立安装版本为好，沙盒环境以及和 Apple ID 绑定的特性就使得功能性以及灵活度大减。</p><a id="more"></a><blockquote><p>更新时间 2018-10-07<br>更换图床，更新图片</p></blockquote><blockquote><p>更新时间 2018-08-10<br>不久前，已经订阅了一年的 Setapp，发现上面好多 App 自己都之前买过了，Sigh！<br>如果你使用 Ulysses 或者 MoneyWiz 3.0 的话，订阅 Setapp 就会非常的值得！</p></blockquote><p><img src="https://i.imgur.com/q3yToeZ.png" alt="Mac Apps"></p><h2 id="办公工具"><a href="#办公工具" class="headerlink" title="办公工具"></a>办公工具</h2><p>离开这些工具，貌似无法办公的。</p><h3 id="AirMail"><a href="#AirMail" class="headerlink" title="AirMail"></a>AirMail</h3><p><a href="https://itunes.apple.com/cn/app/airmail-3/id918858936?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/19ADIxn.jpg" alt="AirMai"></p><p>邮箱客户端，看中 AirMail 主要是因为其强大的第三方服务集成。</p><h3 id="Slack"><a href="#Slack" class="headerlink" title="Slack"></a>Slack</h3><p><a href="https://itunes.apple.com/cn/app/slack/id803453959?mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/W1wtsje.jpg" alt="Slack"></p><p>公司的日常沟通交流主要战场，其实整个操作体验我是不太适应的，不过它强大在第三方服务的集成上，可以做很多自动化的事情。</p><h3 id="Fantastical-2"><a href="#Fantastical-2" class="headerlink" title="Fantastical 2"></a>Fantastical 2</h3><p><a href="https://flexibits.com/fantastical" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/1naxpaM.jpg" alt="Fantastical"></p><p>日历工具，查看满满的一屏幕日程，你会感觉很舒服的。</p><h4 id="Tunnelblick"><a href="#Tunnelblick" class="headerlink" title="Tunnelblick"></a>Tunnelblick</h4><p><a href="https://tunnelblick.net/downloads.html" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/ghIEHNQ.png" alt="Tunnelblick-menu"></p><p>公司需要使用该 App 来接入办公室网络。远程办公必备。</p><h4 id="Office-三件套"><a href="#Office-三件套" class="headerlink" title="Office 三件套"></a>Office 三件套</h4><p>微软的 Office 三件套，Word，Excel，PowerPoint。 用的频率不高，但是用的时候得有。</p><h2 id="GTD"><a href="#GTD" class="headerlink" title="GTD"></a>GTD</h2><p>日常 GTD 工具，基本就是各家评测常提起的几款 App。</p><h3 id="2Do"><a href="#2Do" class="headerlink" title="2Do"></a>2Do</h3><p><a href="https://www.2doapp.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/st89Smb.jpg" alt="2Do on Mac"></p><p>最近才完全转向 2Do，这个 GTD 工具是我最早买入的，但是后来因为其自动化太弱，被打入冷宫。最近几个月又开始捡起来了，吸引我的几个点：</p><ol><li>Colorful Tab</li><li>Mail AutoCapture</li><li>Smart List</li></ol><p>还有一点就是，其同步目前稳定的就只能使用 Dropbox 了，在国内基本对大部分人就是个障碍。</p><h3 id="Things-3"><a href="#Things-3" class="headerlink" title="Things 3"></a>Things 3</h3><p><img src="https://i.imgur.com/5ifAVwf.jpg" alt="Things3"></p><p>Things 2 之前也有，不过因为功能太简单，基本处于买了看看的状态，后来 Things 3 的出现主要看中它的设计元素。</p><h3 id="OmniFocus-3"><a href="#OmniFocus-3" class="headerlink" title="OmniFocus 3"></a>OmniFocus 3</h3><p><img src="https://i.imgur.com/D72LM50.jpg" alt="OmniFocus 3"></p><p>使用它最主要的原因是它支持 AppleScript 比较方便。尤其是结合 Keyboard Maestro 来用太方便。比如我会建立一些定时任务，每周五下午下班前自动生成一张周报表单，列出来本周完成的 Task 等。</p><p>最近刚又从 2 升级到 3 代，半价折扣 39.99$</p><p>3 主要将之前 Context 更换为支持多属性的 Tag，也算补充了一个竞争点吧。</p><h2 id="系统工具"><a href="#系统工具" class="headerlink" title="系统工具"></a>系统工具</h2><p>基本是系统缺少的或者功能难以满足日常使用的一些 App。</p><h3 id="1-Blocker"><a href="#1-Blocker" class="headerlink" title="1 Blocker"></a>1 Blocker</h3><p><a href="https://itunes.apple.com/app/id1107421413" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/l3aGI1P.jpg" alt="1 Blocker"></p><h3 id="iStat-Menus"><a href="#iStat-Menus" class="headerlink" title="iStat Menus"></a>iStat Menus</h3><p><a href="http://download.bjango.com/istatmenus/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/PtO8FNP.jpg" alt="iStat Menus"></p><h3 id="Bartender-3"><a href="#Bartender-3" class="headerlink" title="Bartender 3"></a>Bartender 3</h3><p><a href="https://www.macbartender.com/Demo/Bartender%203.zip" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/EnE2fyQ.png" alt="Bartender"></p><h3 id="Surge"><a href="#Surge" class="headerlink" title="Surge"></a>Surge</h3><p><a href="https://nssurge.com/" target="_blank" rel="noopener">Surge</a></p><p>最近 Yachen Liu 发布了 Surge 3 的 Mac 版本预览版本</p><p><img src="https://i.imgur.com/CNc6AIK.png" alt="Surge 3"></p><p>实质上是个网络调试工具，但是大部分情况下被用来作为爱国上网工具了。 而且定价策略也是引发了很多口水战，不过商品嘛，如果你觉得不值就不买就行了，不必要想用还骂他定价高，对吧。</p><p><img src="https://i.imgur.com/gkhCynx.png" alt="Surge Dashboard"></p><h3 id="Wallpaper-Wizard"><a href="#Wallpaper-Wizard" class="headerlink" title="Wallpaper Wizard"></a>Wallpaper Wizard</h3><p><a href="https://itunes.apple.com/cn/app/wallpaper-wizard-2/id1266674560?mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/lYnieJQ.jpg" alt="Wallpaper Wizard"></p><p>MacPaw 家的壁纸应用，限时免费的时候入的。</p><h3 id="TinkerTool"><a href="#TinkerTool" class="headerlink" title="TinkerTool"></a>TinkerTool</h3><p><a href="https://www.bresink.com/osx/TinkerTool.html" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/YqvFP1Y.png" alt="TinkerTool"></p><p>一个系统增强工具，可以对 Dashboard 和 Docker 等进行定制化修改。</p><h3 id="Capto"><a href="#Capto" class="headerlink" title="Capto"></a>Capto</h3><p><a href="https://www.globaldelight.com/capto/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/9QeUc6Z.jpg" alt="Capto"></p><p>截屏和录制视频工具。</p><h2 id="效率工具"><a href="#效率工具" class="headerlink" title="效率工具"></a>效率工具</h2><h3 id="1Password-7-for-Mac"><a href="#1Password-7-for-Mac" class="headerlink" title="1Password 7 for Mac"></a>1Password 7 for Mac</h3><p><a href="https://app-updates.agilebits.com/download/OPM7" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/Bb13IZD.jpg" alt="1Password 7"></p><h3 id="Alfred-3"><a href="#Alfred-3" class="headerlink" title="Alfred 3"></a>Alfred 3</h3><p><a href="https://cachefly.alfredapp.com/Alfred_3.6.2_922.dmg" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/WeFkSYh.jpg" alt="Alfred"></p><h3 id="LaunchBar-6"><a href="#LaunchBar-6" class="headerlink" title="LaunchBar 6"></a>LaunchBar 6</h3><p><a href="https://www.obdev.at/products/launchbar/download.html" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/FVgOB3g.png" alt="LaunchBar"></p><h3 id="BetterTouchTool"><a href="#BetterTouchTool" class="headerlink" title="BetterTouchTool"></a>BetterTouchTool</h3><p><a href="https://bettertouchtool.net/releases/BetterTouchTool.zip" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/4RfNX0T.jpg" alt="BetterTouchTool"></p><h3 id="Keyboard-Maestro"><a href="#Keyboard-Maestro" class="headerlink" title="Keyboard Maestro"></a>Keyboard Maestro</h3><p><a href="https://www.keyboardmaestro.com/action/download?km-kmi-7-b3" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/IOZeiaH.png" alt="Keyboard Maestro"></p><h3 id="Yoink"><a href="#Yoink" class="headerlink" title="Yoink"></a>Yoink</h3><p><a href="https://eternalstorms.at/yoink/mac/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/oMcckyM.jpg" alt="Yoink"></p><h3 id="Unclutter"><a href="#Unclutter" class="headerlink" title="Unclutter"></a>Unclutter</h3><p><a href="https://unclutterapp.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/SunNE17.png" alt="Unclutter"></p><h3 id="ToothFairy"><a href="#ToothFairy" class="headerlink" title="ToothFairy"></a>ToothFairy</h3><p><a href="https://itunes.apple.com/cn/app/toothfairy/id1191449274?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/2crElmV.jpg" alt="ToothFairy"></p><h3 id="PopClip"><a href="#PopClip" class="headerlink" title="PopClip"></a>PopClip</h3><p><a href="https://itunes.apple.com/cn/app/popclip/id445189367?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/qytMbLT.jpg" alt="PopClip"></p><h3 id="Copied"><a href="#Copied" class="headerlink" title="Copied"></a>Copied</h3><p><a href="https://geo.itunes.apple.com/us/app/copied/id1026349850?ls=1&amp;mt=12&amp;at=1010l8Ju&amp;ct=web&amp;uo=8&amp;at=1010l8Ju" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/hRW1Xoo.jpg" alt="Copied"></p><p>一款剪贴板工具，应该是目前 Mac 上最强大的剪贴板工具了。</p><h2 id="写作"><a href="#写作" class="headerlink" title="写作"></a>写作</h2><h3 id="Day-One"><a href="#Day-One" class="headerlink" title="Day One"></a>Day One</h3><p><a href="https://itunes.apple.com/cn/app/day-one/id1055511498?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/fKmOZa9.jpg" alt="Day One"></p><p>日记应用，之前买了 iOS 和 Mac 双版本，改成订阅之后自动升级为 Plus 会员了，功能已经完全足够。最近的 Premium 会员增加了音频日记以及 Dark Theme 的主题了。</p><h3 id="Ulysses"><a href="#Ulysses" class="headerlink" title="Ulysses"></a>Ulysses</h3><p><a href="https://itunes.apple.com/cn/app/ulysses/id1225570693?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/GBaAJW2.jpg" alt="Ulysses"></p><p>这个完全是冲动消费，就是想看看大家捧杀这么狠到底优秀在哪里，说实话其自定义的 Markdown 格式我还是用不太习惯，不过用了每几次。从非订阅用到了订阅</p><h3 id="MWeb"><a href="#MWeb" class="headerlink" title="MWeb"></a>MWeb</h3><p>推荐安装其非 MAS 版本，<a href="https://zh.mweb.im/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/fMxRIDB.jpg" alt="MWeb"></p><p>前一阵子刚又升级到了 3.0 版本，虽然 UI 上和 Ulysses 差得远，不过因为本地化做的好，各项服务接入的比较完整，比如图床，静态网站自动生成等都是比较吸引人的地方。</p><h3 id="Notability"><a href="#Notability" class="headerlink" title="Notability"></a>Notability</h3><p><a href="https://itunes.apple.com/cn/app/notability/id736189492?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/GPiorsk.jpg" alt="Notability"></p><h3 id="GoodNotes"><a href="#GoodNotes" class="headerlink" title="GoodNotes"></a>GoodNotes</h3><p><a href="https://itunes.apple.com/cn/app/goodnotes/id1026566364?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-07-13-2400x2400bb -2-.jpg" alt="GoodNotes"></p><h3 id="MindNode-5"><a href="#MindNode-5" class="headerlink" title="MindNode 5"></a>MindNode 5</h3><p><a href="https://itunes.apple.com/cn/app/mindnode-5/id1289197285?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/1er4hI3.jpg" alt="MindNode 5"></p><h2 id="阅读"><a href="#阅读" class="headerlink" title="阅读"></a>阅读</h2><h3 id="DEVONThink-Pro-Office"><a href="#DEVONThink-Pro-Office" class="headerlink" title="DEVONThink Pro Office"></a>DEVONThink Pro Office</h3><p><a href="https://www.devontechnologies.com/download/products.html" target="_blank" rel="noopener">下载地址</a></p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-07-13-42007085dd.png" alt="DEVONThink Pro Office"></p><p>已经完全把 DENONThink 家的产品作为构建我知识图谱的工具了。必备。</p><h3 id="Reeder-3"><a href="#Reeder-3" class="headerlink" title="Reeder 3"></a>Reeder 3</h3><p><a href="https://itunes.apple.com/us/app/reeder-3/id880001334?mt=12&amp;uo=4&amp;at=1001lsTF&amp;ct=iOS_detail_share_880001334" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/NPLz9Wl.jpg" alt="Reeder 3"></p><h3 id="MarginNote-3"><a href="#MarginNote-3" class="headerlink" title="MarginNote 3"></a>MarginNote 3</h3><p><a href="https://marginnote.com/?lang=zh-hans" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/kPu5B0k.jpg" alt="MarginNote 3"></p><h2 id="开发工具"><a href="#开发工具" class="headerlink" title="开发工具"></a>开发工具</h2><h3 id="IDE"><a href="#IDE" class="headerlink" title="IDE"></a>IDE</h3><h4 id="Xcode"><a href="#Xcode" class="headerlink" title="Xcode"></a>Xcode</h4><p>话不多说，老老实实的从 App Store 下载吧。</p><p><img src="https://i.imgur.com/NSW9lFo.jpg" alt="Xcode"></p><h4 id="AppCode"><a href="#AppCode" class="headerlink" title="AppCode"></a>AppCode</h4><p><a href="https://www.jetbrains.com/objc/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/pdXy6FK.png" alt="AppCode"></p><p>AppCode 强大就强大在 Jetbrain 团队一贯的 Refactor 功能。不过在 15寸 中配 MacBook Pro 上依然会时不时卡顿，不清楚时不时因为其基于 JVM 那一套所致。推荐个人开发者或者项目不太庞大的团队或者个人使用。</p><h3 id="编辑器"><a href="#编辑器" class="headerlink" title="编辑器"></a>编辑器</h3><h4 id="Visual-Studio-Code"><a href="#Visual-Studio-Code" class="headerlink" title="Visual Studio Code"></a>Visual Studio Code</h4><p><a href="https://code.visualstudio.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/ExXn0QN.png" alt="Visual Studio Code"></p><p>目前当之无愧最好使的编辑器了，当然可以作为大量脚本语言的轻量 IDE 使用了。</p><h3 id="代码管理"><a href="#代码管理" class="headerlink" title="代码管理"></a>代码管理</h3><h4 id="Tower"><a href="#Tower" class="headerlink" title="Tower"></a>Tower</h4><p>Tower 是一款多平台支持的 Git GUI，使用命令行虽然某些程度上比较快，也显得 Geek 范儿，但是在查看 Diff 等功能上确实不太直观，因此一款 Git 的 UI 工具显得很重要。Tower 是一款收费软件，最近 3.0 转为了订阅式。刚订阅了一年基础版再接着用，毕竟切换工具的代价有时候蛮大的。</p><p><a href="https://www.git-tower.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/r4FdJaF.jpg" alt="Microsoft.VisualStudio.Services.Screenshots"></p><h4 id="OhMyStar"><a href="#OhMyStar" class="headerlink" title="OhMyStar"></a>OhMyStar</h4><p><a href="https://itunes.apple.com/us/app/ohmystar2/id1218642292?ls=1&amp;mt=8" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/3NNd0ct.jpg" alt="OhMyStar 2"></p><h4 id="SnippetsLab"><a href="#SnippetsLab" class="headerlink" title="SnippetsLab"></a>SnippetsLab</h4><p><a href="https://itunes.apple.com/cn/app/snippetslab/id1006087419?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/qd4LQGz.jpg" alt="SnippetsLab"></p><h3 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h3><h4 id="Sketch"><a href="#Sketch" class="headerlink" title="Sketch"></a>Sketch</h4><p><a href="https://www.sketchapp.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/NXGXH8X.jpg" alt="Sketch"></p><h4 id="Sip-for-Mac"><a href="#Sip-for-Mac" class="headerlink" title="Sip for Mac"></a>Sip for Mac</h4><p><a href="https://sipapp.io/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/f3NUIHy.jpg" alt="sip-color-format-mac-2016"></p><h4 id="PaintCode"><a href="#PaintCode" class="headerlink" title="PaintCode"></a>PaintCode</h4><p><a href="https://www.paintcodeapp.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/E7UILLI.png" alt="PaintCode 3"></p><p>主要针对一些复杂的设计图形，用代码绘制会非常复杂，比如一个需要使用 Bezier 绘制复杂的图形，如果人工画，费时费力。使用 PaintCode 能够解决这个痛点，支持 Objective-C 和 Swift 两种语言。</p><p>结合 Playground 的话你感受一下。</p><h4 id="Eagle"><a href="#Eagle" class="headerlink" title="Eagle"></a>Eagle</h4><p><img src="https://i.imgur.com/4kEFUi4.png" alt="Eagle"></p><p>设计师用来收集素材所用</p><h3 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h3><h4 id="Charles"><a href="#Charles" class="headerlink" title="Charles"></a>Charles</h4><p><a href="https://www.charlesproxy.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/s7LX5NO.png" alt="Charles"></p><p>使用公司的序列号，手机版本我也入了，不过在 iOS 生态里其并没有 Mac 上在同等功能上的地位，在 iOS 上我更喜欢使用 surge 或者 Thor 来抓包，而后者的功能基本上目前是市面上最完整的 App 了。 但是在 Mac 平台，Charles 的地位依然无人撼动。</p><h4 id="Paw"><a href="#Paw" class="headerlink" title="Paw"></a>Paw</h4><p><a href="https://paw.cloud/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/zt7Up5u.jpg" alt="paw-lg"></p><p>强大的 API 调试工具</p><h4 id="Reveal"><a href="#Reveal" class="headerlink" title="Reveal"></a>Reveal</h4><p><a href="https://revealapp.com/features/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/ITkfXe6.jpg" alt="Reveal"></p><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><h4 id="Monodraw"><a href="#Monodraw" class="headerlink" title="Monodraw"></a>Monodraw</h4><p><a href="https://monodraw.helftone.com/" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/M20TPBn.jpg" alt="Monodraw"></p><p>代码注释加强工具，可以用来对代码进行注释。</p><h4 id="CuteBaby"><a href="#CuteBaby" class="headerlink" title="CuteBaby"></a>CuteBaby</h4><p><img src="https://i.imgur.com/GyIzViM.jpg" alt="CuteBaby"></p><p>Model 序列化工具，解决的痛点就是后端给你一个 JSON 结构，你需要自行映射成工程中的类，这个重复工作有时候会让您炸掉，因为有的接口返回的结构复杂，字段繁杂。不过网络上有很多开源的项目其实也可以做到一定程度。</p><h2 id="社交"><a href="#社交" class="headerlink" title="社交"></a>社交</h2><h4 id="Telegram"><a href="#Telegram" class="headerlink" title="Telegram"></a>Telegram</h4><p><a href="https://telegram.org/dl/macos" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/cWw9qpK.jpg" alt="Telegram"></p><h4 id="Tweetbot-3"><a href="#Tweetbot-3" class="headerlink" title="Tweetbot 3"></a>Tweetbot 3</h4><p><a href="https://itunes.apple.com/cn/app/tweetbot-3-for-twitter/id1384080005?l=en&amp;mt=12" target="_blank" rel="noopener">下载地址</a></p><p><img src="https://i.imgur.com/yK4hhTr.jpg" alt="Tweetbot"></p><h2 id="其他-1"><a href="#其他-1" class="headerlink" title="其他"></a>其他</h2><h4 id="RescueTime"><a href="#RescueTime" class="headerlink" title="RescueTime"></a>RescueTime</h4><p>买了 Premium 会员，记录我日常在 Mac 上的工作记录。已经记录了大概有 2 年多的数据了，很直观。</p><h4 id="PCalc"><a href="#PCalc" class="headerlink" title="PCalc"></a>PCalc</h4><p><img src="https://i.imgur.com/PRMDseI.jpg" alt="PCalc"></p><p>也算是老牌的计算器了</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录一下自己常用的 Mac Apps，每次重装之后到了用的时候发现忘记安装了(不是每次都用 TimeMachine 恢复)，而且强烈简易大家如果某个 App 有对应的独立安装版本的话，购买其独立安装版本为好，沙盒环境以及和 Apple ID 绑定的特性就使得功能性以及灵活度大减。&lt;/p&gt;
    
    </summary>
    
      <category term="Application" scheme="http://hechen.xyz/categories/Application/"/>
    
    
      <category term="App" scheme="http://hechen.xyz/tags/App/"/>
    
      <category term="Mac" scheme="http://hechen.xyz/tags/Mac/"/>
    
  </entry>
  
  <entry>
    <title>What is LLVM</title>
    <link href="http://hechen.xyz/2018/07/10/What-is-LLVM/"/>
    <id>http://hechen.xyz/2018/07/10/What-is-LLVM/</id>
    <published>2018-07-10T04:05:58.000Z</published>
    <updated>2018-07-17T04:08:39.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.infoworld.com/article/3247799/development-tools/what-is-llvm-the-power-behind-swift-rust-clang-and-more.html" target="_blank" rel="noopener">What is LLVM? The power behind Swift, Rust, Clang, and more</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><h1 id="什么是-LLVM？隐藏在-Swift，Rust，Clang-等语言背后的奥秘"><a href="#什么是-LLVM？隐藏在-Swift，Rust，Clang-等语言背后的奥秘" class="headerlink" title="什么是 LLVM？隐藏在 Swift，Rust，Clang 等语言背后的奥秘"></a>什么是 LLVM？隐藏在 Swift，Rust，Clang 等语言背后的奥秘</h1><blockquote><p>了解编译器是如何生成机器原生代码会使得倒腾新语言或者加强已经存在的编程语言变得比以往更加容易了。</p></blockquote><p>新的编程语言，针对现存语言的优化如同雨后春笋般产生。 Mozilla 的 <strong>Rust</strong>， Apple 的 <strong>Swift</strong>，JetBrain 的 <strong>Kotlin</strong>等语言给开发者提供了在速度，安全性，便捷性，可移植性以及性能等方面更多的选择。</p><p>为什么是这个时间点呢？一个重大的原因是那些构建具体语言相关的新式工具的出现。在这一堆工具中，最重要的就是 LLVM，其全称是 Low-Level Virtual Machine，它是一个开源项目，其最早是由 Swift 语言的发明人 Chris Lattner 在伊利诺斯州州立大学期间的一个研究项目而来。</p><p>LLVM 不仅仅使得创建新式语言更加容易，也使得针对现存编程语言进行增强完善更加便捷。它提供了一堆工具使得创造语言的过程中需要的那些令人头疼的事情变得自动化：创建一个编译器，输出代码需要适配多平台和架构，编写代码处理语言当中通常都存在的那些比较晦涩的部分，比如异常。LLVM 的自由开发许可使得它可以被自由的作为一个软件组件或者被部署成服务来使用。</p><p>使用了 LLVM 的编程语言花名册种有很多熟悉的名字。Apple 的 Swift 语言使用 LLVM 作为其编译器框架，Rust 语言使用 LLVM 作为其工具链中的一个核心组件。同样，许多编译器也有其 LLVM 版本，例如 Clang，一个 C/C++ 编译器，它是和 LLVM 功能特别相近的一个项目。还有 Kotlin 语言，其名义上是一门 JVM 语言，也正在开发该语言的一个版本： <a href="https://www.infoworld.com/article/3187370/application-development/kotlin-compiles-directly-to-native-code-via-llvm.html" target="_blank" rel="noopener">Kotlin Native</a>，其使用 LLVM 编译成机器原生代码。</p><h2 id="LLVM-是什么"><a href="#LLVM-是什么" class="headerlink" title="LLVM 是什么"></a>LLVM 是什么</h2><p>LLVM 最核心的功能就是，可以通过编码方式创造机器原生代码。一个开发者可以使用其 API 创造指令，该指令是一种 intermediate representation 格式，简称 IR。之后，LLVM 能够将 IR 编译成独立的二进制文件，或者对其执行即时编译以运行在另外一个程序，例如某编程语言的解释器的上下文中。</p><p>很多编程语言中很常见的结构体或者模式，LLVM 的 API 都提供了原始语义的支持。例如，几乎所有的语言都有函数或者全局变量的概念，LLVM 在其 IR 中把函数和全局变量作为独立元素定义出来，因此你不需要花太多时间和精力来重复造这些特定的轮子，你只需要使用 LLVM 的实现，专注于你的语言最需要关注的地方即可。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-07-13-direct.jpeg" alt="一个 LLVM 的 IR 的例证. 右侧是一个简单的 C 程序; 左侧是同样的代码被 Clang 编译器翻译成的 LLVM IR"></p><h2 id="LLVM-专为移植性而设计"><a href="#LLVM-专为移植性而设计" class="headerlink" title="LLVM: 专为移植性而设计"></a>LLVM: 专为移植性而设计</h2><p>涉及 LLVM 的一点就是，经常在讨论 C 语言的时候被提及： C 语言有时候会被描述为一门可移植的高阶汇编语言，就是因为其具备能被映射成很接近硬件设备的指令，而且它已经被移植到几乎所有的系统架构上。但是 C 语言只能作为一种可移植的汇编语言实际上也有其副作用，这个确实也不是其设计目标。</p><p>作为对比，LLVM 的 IR 从最初就是被设计作为一个可移植的汇编。体现的第一点就是其提供独立于特定机器架构的语义，例如，integer 类型并不会被限制在特定硬件设备上的最大位数（比如 32 位 或者 64 位）。你可以按照你的需要的位数来创建你所需要的 integer 类型，比如 一个 128 位的 integer。你也不需要担心针对特定处理器指令集来再加工，LLVM 已经为了考虑了这些。</p><p>如果你想看到 LLVM IR 长的什么样子，可以去看一下 <a href="http://ellcc.org/" target="_blank" rel="noopener">ELLCC 项目</a>，尝试一下 <a href="http://ellcc.org/demo/index.cgi" target="_blank" rel="noopener">Demo</a> 直接使用浏览器将 C 代码转换为 LLVM IR。</p><h2 id="编程语言是如何使用-LLVM-的"><a href="#编程语言是如何使用-LLVM-的" class="headerlink" title="编程语言是如何使用 LLVM 的"></a>编程语言是如何使用 LLVM 的</h2><p>最常用的场景就是作为某一种语言的编译器前端（ahead-of-time (AOT) compiler），除了如此，LLVM 还有其他很多可能性。</p><h3 id="使用-LLVM-进行即时编译"><a href="#使用-LLVM-进行即时编译" class="headerlink" title="使用 LLVM 进行即时编译"></a>使用 LLVM 进行即时编译</h3><p>一些情形需要我们的代码在运行时即时生成，而不是提前编译好。比如 <a href="https://www.infoworld.com/article/3241107/python/julia-vs-python-julia-language-rises-for-data-science.html?nsdr=true" target="_blank" rel="noopener">Julia 语言</a>，JIT 编译其代码，因为其需要运行很快，能够使得用户通过交互式解析器（<a href="https://en.wikipedia.org/wiki/Read–eval–print_loop" target="_blank" rel="noopener">REPL</a>）或者可交互弹窗来交互。Mono，使用 .Net 实现，能够利用 <a href="http://www.mono-project.com/docs/advanced/runtime/docs/llvm-backend/" target="_blank" rel="noopener">LLVM 后端编译为机器原生代码</a>。</p><p><a href="https://www.infoworld.com/article/2880767/python/5-projects-push-python-performance.html" target="_blank" rel="noopener">Numba</a>，一个为 Python 提供的数学计算加速包，JIT 将选择的 Python 函数编译为机器代码。它也能提前编译 Numba 修饰的代码，但是（例如 Julia），Python 由于其解释性语言的特性能够提供快速的开发工作。使用 JIT 编译能够产出比提前编译更好的 Python 的交互工作流。</p><p>其他的语言还有用非传统方式把 LLVM 作为 JIT 来实验的语言，比如 <a href="https://www.pgcon.org/2017/schedule/events/1092.en.html" target="_blank" rel="noopener">JIT-Compiling SQL Queries in PostgreSQL Using LLVM</a>，能够达到高达 5 倍的性能提升。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-07-13-numba-llvm-100747598-large.jpg" alt="Numba 使用 LLVM 即时编译其数值代码从而加速其执行速度。JIT 加速的 sum2d 函数完成执行比使用常规 Python 代码的实现快大约 139 倍"></p><h3 id="利用-LLVM-进行自动化的代码优化"><a href="#利用-LLVM-进行自动化的代码优化" class="headerlink" title="利用 LLVM 进行自动化的代码优化"></a>利用 LLVM 进行自动化的代码优化</h3><p>LLVM 不仅仅是将 IR 编译成原生机器代码。你也可以通过编码引导其进行更大粒度的优化工作，整个工作会贯穿整个链接过程。优化工作可以非常激进，包括对函数进行内联，移除死代码（包含无用的类型声明以及函数参数）或者<a href="https://en.wikipedia.org/wiki/Loop_unrolling" target="_blank" rel="noopener">展开循环</a>等。</p><p>这次依然，上面这些你并不需要都自己来做。 LLVM 能够帮你处理这些事情，你也可以按需关闭这些功能。举例来讲，如果你想牺牲一部分性能来减小二进制的大小，你可以使用自己的编译器前端来告知 LLVM 来关闭循环展开。</p><h3 id="使用-LLVM-的领域特定语言"><a href="#使用-LLVM-的领域特定语言" class="headerlink" title="使用 LLVM 的领域特定语言"></a>使用 LLVM 的领域特定语言</h3><p>LLVM 已经被用以为许多跨领域通用计算机语言产生编译器，但是它在生成非常垂直或者解决某些具体问题的语言方面也非常有用。在某些程度上，这才是 LLVM 最闪光的点所在，因为它移除了一大堆创建该语言过程中的单调烦躁的工作，并且使得它表现的更好。</p><p>比如，<a href="https://github.com/kripken/emscripten" target="_blank" rel="noopener">Emscripten</a> 项目，采用了 LLVM IR 代码，将其转换成 JavaScript，理论上允许任何具备 LLVM 后端的语言可以导出在浏览器中运行的代码。虽然 Emscripten 的长期计划是能够拥有可生成 WebAssembly 的基于 LLVM 的后端，但是它是一个展示 LLVM 灵活性的很好的例证。</p><p>另外一种 LLVM 可以被使用的方式就是为已存在的编程语言增加特定领域的扩展。Nvidia 使用 LLVM 创造了<a href="https://developer.nvidia.com/cuda-llvm-compiler" target="_blank" rel="noopener">Nvidia CUDA Compiler</a>，其能够让编程语言增加针对 CUDA 的原生支持，而不是通过加载某个库来唤起。</p><h2 id="在多种语言中使用-LLVM"><a href="#在多种语言中使用-LLVM" class="headerlink" title="在多种语言中使用 LLVM"></a>在多种语言中使用 LLVM</h2><p>和 LLVM 打交道的一种典型方式就是找到你很舒服的一种编程语言的代码来体会，当然，这种编程语言要支持 LLVM。</p><p>两种常见选择的语言是 C 和 C++。许多 LLVM 的开发者默认选择这二者之一有以下几个原因：</p><ol><li>LLVM 本身就是 C++ 写的；</li><li>LLVM 的 API 能够在 C 和 C++ 中无缝使用；</li><li>需要语言都倾向于基于 C 和 C++ 进行开发。</li></ol><p>尽管，这两种语言并不是唯一的选择。许多语言能够原生调用 C 库的方法，所以理论上是可以使用这些语言来进行 LLVM 开发的，但是其也有助于在某种语言中实际存在一个库，能够优雅的封装 LLVM 的 API。幸运的是，许多语言和运行时都有这些库，包括 <a href="https://github.com/Microsoft/LLVMSharp" target="_blank" rel="noopener">C#/.Net/Mono</a>, <a href="https://crates.io/crates/llvm-sys" target="_blank" rel="noopener">Rust</a>, <a href="https://hackage.haskell.org/package/llvm" target="_blank" rel="noopener">Haskell</a>, <a href="https://opam.ocaml.org/packages/llvm/" target="_blank" rel="noopener">OCAML</a>, <a href="https://www.npmjs.com/package/llvm-node" target="_blank" rel="noopener">Node.js</a>, <a href="https://llvm.org/svn/llvm-project/llgo/trunk/README.TXT" target="_blank" rel="noopener">Go</a>, 和 <a href="https://github.com/numba/llvmlite" target="_blank" rel="noopener">Python</a>。</p><p>不过，你需要注意的一点是，这些语言中有一些针对 LLVM 的绑定支持并不完整。以 Python 语言举例，虽然存在多种选择，但是它们都会在完整性和功效性上有差别：</p><ul><li><p>LLVM 项目下<a href="https://github.com/llvm-mirror/llvm/tree/master/bindings/python" target="_blank" rel="noopener">维护有针对 LLVM 的 C 版 API 的绑定集合</a>，但是它们已经不再被维护了；</p></li><li><p><a href="https://www.infoworld.com/article/3247799/development-tools/what-is-llvm-the-power-behind-swift-rust-clang-and-more.html" target="_blank" rel="noopener">llvmpy</a> 在 2015 年停止的维护，这对于任何的软件项目来讲都不是一件好事。在使用 LLVM 的时候更是如此，考虑到每一个版本的 LLVM 所带来的变化数量。</p></li><li><p><a href="https://github.com/numba/llvmlite" target="_blank" rel="noopener">llvmlite</a> 是由 Numba 的开发团队创建的，已经成为了当下在 Python 中做 LLVM 开发的有力竞争者。它基于 Numba 项目的需求，只实现了一小部分 LLVM 的功能。但是这个功能子集已经能够满足大部分 LLVM 开发者使用了。</p></li><li><p><a href="https://github.com/revng/llvmcpy" target="_blank" rel="noopener">llvmcpy</a> 旨在更新 Python 为 C 库提供的绑定，使得它们能够以自动化的形式保持更新，并且 Python 的原生语法使得它们能够很方便的使用。llvmcpy 依然处于初级阶段，但是已经做了很多和 LLVM API 打交道的基础工作。</p></li></ul><p>If you’re curious about how to use LLVM libraries to build a language, LLVM’s own creators have a tutorial, using either C++ or OCAML, that steps you through creating a simple language called Kaleidoscope. It’s since been ported to other languages:</p><p>如果你很好奇如何使用 LLVM 库构建一门语言的话，LLVM 的原创者们提供了<a href="https://llvm.org/docs/tutorial/index.html" target="_blank" rel="noopener">教程</a>，该教程使用 C++ 或者 OCAML ，指导你从头到尾创造一门简单的语言 <strong>Kaleidoscope</strong>，而这个示例已经被移植到其他语言上了：</p><ul><li><p><a href="http://www.stephendiehl.com/llvm/" target="_blank" rel="noopener">Haskell</a>: 和原始教程最接近的移植；</p></li><li><p>Python：<a href="https://github.com/eliben/pykaleidoscope" target="_blank" rel="noopener">一个版本</a>和该教程非常接近，<a href="https://github.com/frederickjeanguerin/pykaleidoscope" target="_blank" rel="noopener">另外一个版本</a> 更激进一些，重写了一个交互命令行。两者都使用了 llvmlite 作为和 LLVM 的绑定；</p></li><li><p><a href="https://github.com/jauhien/iron-kaleidoscope" target="_blank" rel="noopener">Rust</a> 和 <a href="https://harlanhaskins.com/2017/01/08/building-a-compiler-with-swift-in-llvm-part-1-introduction-and-the-lexer.html" target="_blank" rel="noopener">Swift</a>：似乎不可避免，我们会得到这两个语言版本的移植版本教程，因为 LLVM 本身更就是因为这二者才得以创造的。</p></li></ul><p>最后，这个教程也有其他人类语言的版本。它已经被翻译成中文了，有使用 <a href="https://github.com/liancheng/llvm-tutorial-cn" target="_blank" rel="noopener">C++</a> 的，也有 <a href="https://github.com/moevis/Kaleidoscope-LLVM-tutorial-zh-cn" target="_blank" rel="noopener">Python</a>。</p><h2 id="LLVM-做不到哪些"><a href="#LLVM-做不到哪些" class="headerlink" title="LLVM 做不到哪些"></a>LLVM 做不到哪些</h2><p>以上提到的都是 LLVM 能够提供的功能，它还有一些无法做到的事情，了解一下会比较有用。</p><p>举例而言，LLVM 不会进行语法解析。许多工具，比如 <a href="http://dinosaur.compilertools.net/" target="_blank" rel="noopener">lex/yacc</a>，<a href="http://aquamentus.com/flex_bison.html" target="_blank" rel="noopener">ﬂex/bison</a> 以及 <a href="http://www.antlr.org/" target="_blank" rel="noopener">ANTLR</a> 等都已经做了这些工作。解析语法就意味着要和编译行为进行解耦，所以，LLVM 没有试图去做这部分工作也不意外。</p><p>LLVM 也不会试图解决围绕某种特定语言更大范围的周边行为。你需要自行安装编译器二进制，管理安装过程中的包以及升级工具链。</p><p>最后一点，也是最重要的是，依然又很多语言通用的部分 LLVM 没有原生支持的。许多编程语言都存在垃圾回收内存管理的行为，要么作为管理内存的主要方式，要么作为某种策略，例如 RAII（C++ 和 Rust 语言中使用的）的辅助。LLVM 不提供垃圾回收机制，但是它确实<a href="https://llvm.org/docs/GarbageCollection.html" target="_blank" rel="noopener">提供一些工具</a>，能够允许代码可以使用元数据来做标记使得编写垃圾收集器更加容易。</p><p>尽管如此，LLVM 还是存在最终添加原生支持执行垃圾回收机制的可能性。LLVM 正在以每 6 个月一个大版本更新在快速发展。而这个发展步速很大程度上因为目前很多主流编程语言已经把其作为它们开发过程的核心一环。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.infoworld.com/article/3247799/development-tools/what-is-llvm-the-power-behind-swift-rust-clang-and-m
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="LLVM" scheme="http://hechen.xyz/tags/LLVM/"/>
    
      <category term="Compiler" scheme="http://hechen.xyz/tags/Compiler/"/>
    
      <category term="Swfit" scheme="http://hechen.xyz/tags/Swfit/"/>
    
      <category term="IR" scheme="http://hechen.xyz/tags/IR/"/>
    
  </entry>
  
  <entry>
    <title>In App Purchase</title>
    <link href="http://hechen.xyz/2018/05/24/In-App-Purchase/"/>
    <id>http://hechen.xyz/2018/05/24/In-App-Purchase/</id>
    <published>2018-05-24T02:40:39.000Z</published>
    <updated>2018-10-07T11:09:32.000Z</updated>
    
    <content type="html"><![CDATA[<p>对于在 App Store 中上架的应⽤来说，应⽤内购买(In-App Purchase，简称 IAP) 应该是一个避不开的话题，尤其是去年微信打赏和 Apple 之间的争执更让 IAP 火了一把，不仅仅大公司，作为个人开发者来讲，IAP 也是非常重要的，说不定就是你养家糊口的工具呢。</p><a id="more"></a><p>整个 IAP 的过程，在客户端的实现依赖于 <code>StoreKit</code> 这个框架，所有的支付相关操作都是交由 StoreKit 来完成的。</p><p>首先，我们先复习一下 IAP 的整个流程。</p><h2 id="In-App-Purchase-的整体流程"><a href="#In-App-Purchase-的整体流程" class="headerlink" title="In App Purchase 的整体流程"></a>In App Purchase 的整体流程</h2><h3 id="阶段-1"><a href="#阶段-1" class="headerlink" title="阶段 1"></a>阶段 1</h3><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-09-03-4DDE8387-EDB1-42CC-B18C-3DC6CA1DC688.png" alt="In App Purchase 过程"></p><ol><li>加载 In-App Identifier</li><li>客户端从 AppStore 中获取本地化的商品信息。</li><li>把 IAP 的购买界⾯面展示给⽤用户，⽤用户可以同意购买并点击购买按钮。</li><li>⽤用户授权购买，客户端向服务器器发送购买请求。</li><li>服务器器处理理购买请求，并把结果返回给 StoreKit。</li><li>如果购买请求验证通过，客户端此时解锁内容或者提供⾦金金币。</li><li>至此，整个交易易流程结束。</li></ol><h3 id="阶段-2"><a href="#阶段-2" class="headerlink" title="阶段 2"></a>阶段 2</h3><p>具体到和 Apple Store 打交道的话，如下所示：</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-08-27-intro_2x.png" alt="IAP With App Store"></p><h3 id="阶段-3"><a href="#阶段-3" class="headerlink" title="阶段 3"></a>阶段 3</h3><p>如果涉及到自己 App 端 Server 的参与，基本如下图所示：</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-08-29-1*MwLeHUGSupbb9j-oYQyflw.png" alt="IAP With Business Server"></p><p>正常多出的几步：</p><ol><li>获取 Product 标识列表；</li><li>客户端上传 Receipt 数据给 Server，Server 自行校验或者使用 Apple 的接口校验；</li><li>Server 端告知给客户端付费内容</li></ol><h3 id="注册商品"><a href="#注册商品" class="headerlink" title="注册商品"></a>注册商品</h3><p>商品注册是通过 iTunes Connect 后台进行的，其中商品类型需要提前明确好。</p><h4 id="商品类型"><a href="#商品类型" class="headerlink" title="商品类型"></a>商品类型</h4><p>参与 App 内支付动作的商品有四种类型：</p><ul><li>Consumable products （消耗型商品）</li><li>Non-consumable products （非消耗型商品）</li><li>Auto-renewable subscriptions （自动续约订阅）</li><li>Non-renewable subscriptions （非自动续约订阅）</li></ul><p>其中关于非自动续约订阅，App 的开发者有义务同步服务到用户的所有设备上。</p><p>如下，是官方文档列出来的四种商品类型的一些主要属性。</p><table><thead><tr><th>Product type</th><th>Non-consumable</th><th>Consumable</th><th>Auto-renewable</th><th>Non-renewing</th></tr></thead><tbody><tr><td>Users can buy</td><td>Once</td><td>Multiple times</td><td>Multiple times</td><td>Multiple times</td></tr><tr><td>Appears in the receipt</td><td>Always</td><td>Once</td><td>Always</td><td>Always</td></tr><tr><td>Synced across devices</td><td>By the system</td><td>Not synced</td><td>By the system</td><td>By your app</td></tr><tr><td>Restored</td><td>By the system</td><td>Not restored</td><td>By the system</td><td>By your app</td></tr></tbody></table><p>其中需要注意的几点：</p><ol><li>除了非消耗型商品只能买一次，再次购买应该会失败以外，其他三种类型（消耗型，连续/非连续订阅）都是可以无限制购买的，Apple 是不会报错的；</li><li>除了消耗型商品在 Receipt 中出现一次，其他三种类型（非消耗型，连续/非连续订阅）是始终都会在其中的，这也是后续做订阅校验至关重要的一环；</li></ol><h3 id="获取-Product-ID"><a href="#获取-Product-ID" class="headerlink" title="获取 Product ID"></a>获取 Product ID</h3><p>这里获取的是 Product 在 iTunes Connect 后台注册商品时填写的 Product ID</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-09-03-15359854946888.png" alt="Product ID"></p><p>In-App Identifier 为每个可销售的商品提供一个唯一标识。在客户端上，我们可以直接写死代码:</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> identifiers = [<span class="string">"com.myCompany.myApp.product1"</span>, <span class="string">"com.myCompany.myApp.product2"</span> ]</span><br></pre></td></tr></table></figure><p>或者从 Server 端获取。</p><h3 id="获取商品信息"><a href="#获取商品信息" class="headerlink" title="获取商品信息"></a>获取商品信息</h3><p>然后通过上一步取得的 Product ID 来获取具体某个 Product 的详细信息，包括价格，描述等等。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取一批商品的信息</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> request = <span class="type">SKProductsRequest</span>(productIdentifiers: identifierSet) </span><br><span class="line"></span><br><span class="line">request.delegate = <span class="keyword">self</span></span><br><span class="line"></span><br><span class="line">request.start()</span><br></pre></td></tr></table></figure><p>在代理回调中处理获取的结果，进行展示或者 Cache。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">productRequest</span><span class="params">(<span class="number">_</span> request: SKProductsRequest, didReceive response: SKProductsResponse)</span></span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> product <span class="keyword">in</span> response.products &#123;</span><br><span class="line">    </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>官方建议是不要进行缓存，因为 Product 的信息有可能会更新，比如用户切换 AppStore 的区域，价格信息也会因为相应的汇率发生变化。不过！在国内，被牺牲最大的用户体验不是这种边界情况，而是获取 Product 信息是需要和 Apple Server 打交道的，这就有个问题 —— 慢！</p><p>我们会在开机的时候提前预取所有 Product 信息，并且缓存起来。下次弹出充值面板的时候就直接列出商品信息即可。</p><h2 id="展示支付-UI"><a href="#展示支付-UI" class="headerlink" title="展示支付 UI"></a>展示支付 UI</h2><p>接下来就看什么时候进行购买了，展示商品面板进行购买。用户在面板上进行选择之后，就进入了下一阶段 ── 支付</p><h2 id="发出购买请求"><a href="#发出购买请求" class="headerlink" title="发出购买请求"></a>发出购买请求</h2><p>这⼀步也很简单， 两⾏行代码就可以搞定。只需要把之前拿到的商品对象传到 <code>SKPayment</code> 的初始化方法中，构造一个 <code>SKPayment</code> 实例，再把这个实例加⼊到购买队列中即可:</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> payment = <span class="type">SKPayment</span>(product: product) <span class="type">SKPaymentQueue</span>.<span class="keyword">default</span>().add(payment)</span><br></pre></td></tr></table></figure><p>之后就交给 Apple 了，此时应用内会弹出苹果设计的购买窗口，用户只要使⽤指纹或者输入密码即可同意付款。</p><h2 id="处理购买结果"><a href="#处理购买结果" class="headerlink" title="处理购买结果"></a>处理购买结果</h2><p>当⽤户的购买请求经过 <code>StoreKit</code> 和苹果服务器的验证后，开发者可以在回调函数中接收到。Apple 具体的回调会通过 <code>SKPaymentQueue</code> 来进行，如下，我们需要在支付之前就向 <code>SKPaymentQueue</code> 中加入代理监听。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">SKPaymentQueue</span>.<span class="keyword">default</span>().add(<span class="keyword">self</span>)</span><br></pre></td></tr></table></figure><p>你可以把具体的监听放在一个独立的类中完成。在该 Observer 中，实现代理方法：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 处理理 SKPaymentQueueObserver 事件</span></span><br><span class="line"><span class="comment">// MARK: - SKPaymentTransactionObserver</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">paymentQueue</span><span class="params">(<span class="number">_</span> queue: SKPaymentQueue, updatedTransactions transactions:[SK PaymentTransaction])</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> transaction <span class="keyword">in</span> transactions &#123;</span><br><span class="line">        <span class="keyword">switch</span> transaction.transactionState &#123; </span><br><span class="line">            <span class="keyword">case</span> .purchased:</span><br><span class="line">            <span class="comment">// Validate the purchase</span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">// Locate the file</span></span><br><span class="line">            <span class="keyword">guard</span> <span class="keyword">let</span> url = <span class="type">Bundle</span>.main.appStoreReceiptURL <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// Handle failure</span></span><br><span class="line">                <span class="keyword">return</span>    </span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Read the contents</span></span><br><span class="line">            <span class="keyword">let</span> receipt = <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">        &#125; </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于每一个你提交的 Payment，就一定会有一个相应的 Transaction 生成以便其后续处理。通过检查 transaction 的状态，我们可以指定每种状态下的处理逻辑。如果状态显示已购买， 我们还是应该和⾃己的服务器器进行一次校验，确保交易真实有效⽽而不是通过越狱后的某些插件完成的。被检验的，是一种被苹果称之为收据(receipt)的凭证，就像我们在超市购物或者饭店就餐后拿到的收据一样，每⼀个购买的商品都有⾃己的收据。这个收据由苹果签发，保存在客户端本地。</p><p>目前 Apple 官方提供两种方式，一种是本机校验，一种是 Server 端校验。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-09-03-E9DC4D03-7F18-49CC-B21D-835904E2B39F.png" alt="Receipt Validation Ways"></p><h2 id="Final，投递商品"><a href="#Final，投递商品" class="headerlink" title="Final，投递商品"></a>Final，投递商品</h2><p>当 Server 端检验完成之后，说明本地交易已经成功完成，因此将用户购买的内容提供给用户使用。当然，对于业务方，购买完成不代表业务完成，因此大多数情况下还有一步进行商品业务校验的过程。</p><p>需要注意的一点是：</p><p>Apple 也明确告诉开发者有几个关键的路径节点需要注意：</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/2018-09-03-C1BB8495-728D-43E7-9C40-8EC9B3A22905.png" alt="关键路径"></p><p>监听交易状态的代码一定要越早越好，比如在 App 完成启动之后进行注册，确保整个生命周期内都有代码逻辑来处理监听到的交易，根据监听到的 Transaction 的状态来分发处理逻辑。因为很有可能你监听的时机太晚，导致 Apple 通知有一些之前未完成的交易的时候你无法捕获。说白了就是用户的购买流程跟 App 生命周期不挂钩导致的，典型的有以下几个场景</p><ol><li>用户杀死了 App</li><li>用户需要更新帐号中的付费信息（此时已跳出 App ）</li><li>App 闪退</li><li>用户进行了订阅续期</li><li>用户进入了推介促销价的流程</li><li>用户跳出 App 输入推广码</li></ol><h2 id="结束交易"><a href="#结束交易" class="headerlink" title="结束交易"></a>结束交易</h2><p>在最后一步中，如果是正常的交易，那么简单的结束它们就⾏了。但对于其他异常的交易易，⽐比如出现了一些 error，也要妥善的结束它们。如果是⾃动续期的订阅，也会经过这一步。如果 交易没有被正确的结束，它们会一直停留留在上文提到的队列中，每次回调函数被调用时，队列 中都会有这些没有被结束的交易。也就是为什么需要提前监听的原因了。</p><p>结束交易的代码⾮常简单，只有一行:</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">SKPaymentQueue</span>.<span class="keyword">default</span>().finishTransaction(transaction)</span><br></pre></td></tr></table></figure><h3 id="关于-Transaction-和-Receipt-的区别和联系"><a href="#关于-Transaction-和-Receipt-的区别和联系" class="headerlink" title="关于 Transaction 和 Receipt 的区别和联系"></a>关于 Transaction 和 Receipt 的区别和联系</h3><p>很多同学在进行 IAP 开发的时候常常有一个很大的困惑就是 Transaction 和 Receipt 傻傻分不清楚，这里重新梳理了一下。</p><h4 id="关于-Transaction"><a href="#关于-Transaction" class="headerlink" title="关于 Transaction"></a>关于 Transaction</h4><p>其实前面讲 Payment 的操作的时候已经表明了， Transaction 是和具体的一次 Payment 对应的，也就是发生一次 Payment 就会生成 Transaction，但是，并不是 1 对 1 的关系，比如本次 Payment 对应的 Transaction 已经被 finish 了，用户换了手机，登陆同样 AppleID 的时候使用 Restore 功能，Apple 会生成 Transaction 给你，本次的 Transaction 就对应的之前的 Payment，只是状态标记为 .restored。</p><p>Transaction 的状态主要有以下这么几种：</p><table><thead><tr><th style="text-align:left">状态</th><th style="text-align:left">含义</th></tr></thead><tbody><tr><td style="text-align:left">.purchasing</td><td style="text-align:left">不需要做什么，继续等待 SKPaymentTransactionState 的状态流转</td></tr><tr><td style="text-align:left">.purchased</td><td style="text-align:left">用户已完成付费，处理付费后的流程并调用 finishTransaction 方法</td></tr><tr><td style="text-align:left">.failed</td><td style="text-align:left">用户付费失败，处理付费失败的流程并调用 finishTransaction 方法</td></tr><tr><td style="text-align:left">.restored</td><td style="text-align:left">用户付费成功，处理付费后的流程并调用 finishTransaction 方法</td></tr><tr><td style="text-align:left">.deferred</td><td style="text-align:left">不需要做什么，继续等待 SKPaymentTransactionState 的状态流转</td></tr></tbody></table><p><strong>Note</strong>: 对于 <code>finishTransaction</code> 的几点说明：</p><ol><li>对于那些依赖苹果下载服务的，比如存储在 iTunes Connect 上的付费内容要下载到本地，如果下载完成之前就进行了 finish 动作会导致 Apple 阻断所有的下载流程，并且无法重新下载；</li><li>Transaction 需要配合 Receipt 验证是否合法，单纯靠前端是不靠谱的，因此交由业务后端验证 Receipt 更安全合理，业务后端来决定是否需要 finish</li><li>文档里说：Your app needs to finish every transaction, regardless of whether the transaction succeeded or failed. 所以我们需要将 Transaction Finish 掉，否则会始终出现在 PaymentQueue 里，BUT，中间态的交易是不需要 finish 的，比如 .purchasing 和 .deferred</li></ol><h4 id="关于-Receipt"><a href="#关于-Receipt" class="headerlink" title="关于 Receipt"></a>关于 Receipt</h4><p>Receipt 是 App Store 签发的，是你的 App 上和该 App 上发生的支付行为记录。它存储在设备上的某个固定地址，StoreKit 并不会生成它，它是从 App Store 拉取下来的一个文件。如图所示：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">guard</span> <span class="keyword">let</span> url = <span class="type">Bundle</span>.main.appStoreReceiptURL <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// handle failure</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// read the contents</span></span><br><span class="line"><span class="keyword">let</span> receipt = <span class="type">Data</span>(contentsOf: url)</span><br></pre></td></tr></table></figure><p>如果本地该地址里没有，我们还可以进行刷新，实质上就是从 App Store 请求 Receipt 数据。请求如下：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> request = <span class="type">SKReceiptRefreshRequest</span>()</span><br><span class="line"></span><br><span class="line">request.delegate = <span class="keyword">self</span></span><br><span class="line"></span><br><span class="line">request.start()</span><br></pre></td></tr></table></figure><h2 id="这里有几个需要注意的点："><a href="#这里有几个需要注意的点：" class="headerlink" title="这里有几个需要注意的点："></a>这里有几个需要注意的点：</h2><h3 id="恢复已经购买的商品"><a href="#恢复已经购买的商品" class="headerlink" title="恢复已经购买的商品"></a>恢复已经购买的商品</h3><p>这里有一个需要注意的事情就是：如果一个用户试图去购买一个之前购买过的商品，而不是使用你App 提供的 Restore 功能恢复的话，App Store 会依然创建一个正常的新交易。用户是不会被再次收费的，这里的问题是，Transaction 的状态可不是 restored，而是完全新的 Transaction，你可以按照之前的正常流程走，当然对于自带账户体系的 App 来讲，需要自行判断和计算。</p><h2 id="参考资料："><a href="#参考资料：" class="headerlink" title="参考资料："></a>参考资料：</h2><ol><li><a href="https://developer.apple.com/in-app-purchase/" target="_blank" rel="noopener">In App Purchase</a></li><li><a href="https://developer.apple.com/library/archive/documentation/NetworkingInternet/Conceptual/StoreKitGuide/Introduction.html" target="_blank" rel="noopener">In-App Purchase Programming Guide</a></li><li><a href="https://developer.apple.com/library/archive/releasenotes/General/ValidateAppStoreReceipt/Introduction.html" target="_blank" rel="noopener">Receipt Validation Programming Guide</a></li><li><a href="https://forums.developer.apple.com/thread/46737" target="_blank" rel="noopener">How to detect refunded IAPs from receipts?</a></li><li><a href="https://stackoverflow.com/questions/10771240/check-if-a-non-renewable-subscription-was-refunded-by-apple" target="_blank" rel="noopener">Check if a non-renewable subscription was refunded by Apple?</a></li><li><a href="https://stackoverflow.com/questions/6439482/how-does-apple-notify-ios-apps-of-refunds-of-in-app-purchases-iap" target="_blank" rel="noopener">How does Apple notify iOS apps of refunds of in-app purchases (IAP)?</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;对于在 App Store 中上架的应⽤来说，应⽤内购买(In-App Purchase，简称 IAP) 应该是一个避不开的话题，尤其是去年微信打赏和 Apple 之间的争执更让 IAP 火了一把，不仅仅大公司，作为个人开发者来讲，IAP 也是非常重要的，说不定就是你养家糊口的工具呢。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://hechen.xyz/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="IAP" scheme="http://hechen.xyz/tags/IAP/"/>
    
      <category term="Receipt" scheme="http://hechen.xyz/tags/Receipt/"/>
    
  </entry>
  
  <entry>
    <title>模块化 Xcode 工程</title>
    <link href="http://hechen.xyz/2017/11/19/Modular-Xcode-projects/"/>
    <id>http://hechen.xyz/2017/11/19/Modular-Xcode-projects/</id>
    <published>2017-11-19T10:17:15.000Z</published>
    <updated>2018-10-07T11:19:32.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://ppinera.es/2017/09/29/modular-xcode-projects.html" target="_blank" rel="noopener">Modular Xcode projects</a><br>原作者 &amp; Copyright <a href="http://twitter.com/pepibumur" target="_blank" rel="noopener">@pepibumur</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>使用 Xcode 构建模块化的工程就需要对工程结构以及其基础概念有很好的理解才行。<a id="more"></a> 我们平时不怎么关注工程结构本身，只有在工程逐渐变大，要添加更多依赖的时候才会注意的到。而即使到了这个时候，我们大多数的工程都会使用  <a href="https://cocoapods.org/" target="_blank" rel="noopener">CocoaPods</a> 来设置那些依赖项，或者 <a href="https://github.com/carthage" target="_blank" rel="noopener">Carthage</a>, 后者虽然没有帮我们做依赖性的设置，但是使得我们会更容易的，通过在工程的 build phase 选项中添加一些内容，达到同样的目的。当配置项越来越复杂，我们就很容易产生困惑，这是因为我们并没有完全掌握 Xcode 工程中所涉及的所有元素。我经常被问到的问题如下：</p><ul><li>我能不能在工程里同时使用 Carthage，Cocoapods 以及自己个人的依赖设置？</li><li>我添加了依赖，但是当模拟器打开 App 的时候 Crash 了。</li><li>为什么我需要只在某些 targets 里嵌入 framework？</li><li>我的 framework 应该是静态的还是动态的？</li></ul><p>在这篇博文中，我会引导你遍历 Xcode Project 中的各个元素，指导你如何通过改变这些元素来模块化你的设置项。我希望下次你遇到上面这些问题的时候，你不需要再花大量时间取 Stack Overflow 上查这些确定的答案。</p><h3 id="Elements-⚒"><a href="#Elements-⚒" class="headerlink" title="Elements ⚒"></a>Elements ⚒</h3><h4 id="Target"><a href="#Target" class="headerlink" title="Target"></a>Target</h4><p>工程（Projects）都是由多个更小的叫做 target 的单元组成的。这些 target 包含编译特定平台产品，比如 frameworks, libraries, apps, testing bundles, extensions 等所需要的配置。 你可以在<a href="https://github.com/xcodeswift/xcproj/blob/master/Sources/xcproj/PBXProductType.swift" target="_blank" rel="noopener">这里</a>看到 target 所有可用的类型。 Target 可以相互依赖，当一个 target A 依赖另外一个 target B 的时候，target B 就会被先构建出来以便 target A 使用其产出。而 target 的配置项会涉及以下几个地方：</p><ul><li><strong>Info.plist 文件</strong>: 该文件包含产出特定的设置项，比如 版本、App 的名字或者 App 的类型，你可以在<a href="https://developer.apple.com/library/content/documentation/General/Reference/InfoPlistKeyReference/Introduction/Introduction.html" target="_blank" rel="noopener">这里</a>详细了解这个文件。</li><li><strong>Entitlements</strong>: 其指定了应用的能力。如果在授权文件中指定的能力和开发者平台上设置的无法匹配，签名过程就会出错。</li><li><strong>Build settings</strong>: 如其名字所描述的那样，这些都是构建 target 所必要的设置项。构建设置项要么在 target 自身定义或者在 <code>xcconfig</code> 文件中定义。一个 target 的配置项可以继承而来，首先是配置文件本身，其次是 target 的配置项，最后是 project 配置项。</li><li><strong>Build phases</strong>: 构建流水线由 build phase 定义。当一个 target 被创建出来之后，其包含默认的构建阶段（包含 <em>构建源码、拷贝资源等</em>），但是你可以自行添加你需要的。举个例子，这些阶段里，有个 shell script phase 允许你在构建过程中执行一些脚本。这些脚本可以读取 Xcode 暴露出来的那些<a href="http://ppinera.es/2017/09/15/modular-xcode-projects.html" target="_blank" rel="noopener">构建参数</a>。</li></ul><p>基于<code>.xcconfig</code>文件的可组合性以及其可重用性的考虑，强烈建议你在这些文件中定义你那些编译设置。Target 的配置，比如 build setting 、build phase 等的变更都体现在 <code>.pbxproj</code> 文件中，这个文件是一种特殊的plist 文件，当我们使用 Git 管理我们的工程的时候，这个文件很容易出现冲突。当然，更新 <code>pbxproj</code> 文件中配置的最简单方式就是使用 Xcode，其了解如何从这些文件中读取配置和向其中写入配置。如果你对不使用 Xcode 更新 <code>pbxproj</code> 文件感兴趣的话，你可以试试 <a href="https://github.com/cocoapods/xcodeproj" target="_blank" rel="noopener">Xcodeproj</a> 或者 <a href="https://github.com/swift-xcode/xcodeproj" target="_blank" rel="noopener">xcproj</a>。</p><p>构建这些 target 的输出要么是比如 app，extension 或者测试文件等 <strong>bundles</strong>，要么就是<strong>intermediate products</strong> ，例如 library 或者那些封装了代码和资源文件用来给别的 target 使用的 framework。这些 Target 的输出内容你可以在工程文件中的 <code>Products</code> 的 Group 下找到，如果有红色的文件引用表示没有 product 输出，很大可能是你还没有构建过这个 target。</p><h4 id="Scheme"><a href="#Scheme" class="headerlink" title="Scheme"></a>Scheme</h4><p>Xcode 工程中另外一个要素是 scheme。 一个工程可以包含多个 scheme，他们可以被共享，作为工程的一部分被人们使用。这些 scheme 指定了 Xcode 中每个具体动作的配置，这些动作包括：<strong>run</strong>，<strong>test</strong>，<strong>profile</strong>，<strong>analyze</strong> 以及 <strong>archive</strong>。 细的来讲，可以指定哪些 target 需要被构建，以什么顺序构建甚至针对每一种动作指定不同的配置。</p><p>关于 scheme 的编译配置有一些东西要讲。当我们指定针对哪些动作构建哪些 target 的时候，在下面两种情况下，我们不需要指定每个 target 的依赖项：</p><ol><li>如果依赖项是是相同 project 中的一部分，并且已经在 <code>Target dependencies</code> 的 <code>Build Phases</code> 中定义过；</li><li>开启了 <code>Find implicit dependencies</code>。</li></ol><p>第 2 点中开启的标识，构建过程必须找到 target 的依赖项，并且先行构建。另外，如果你开启了<code>Parallelize build</code> 的话，一旦 target 相互之间没有依赖的话，就能够并行构建，因而会节省一部分时间。</p><p>一个有问题的构建配置会导致你编译 target 的时候出现错误，比如 <code>Framework XXX not found</code>。如果你曾经或者当前遇到了类似的报错，检查一下在构建每个 scheme 的时候，你的 target 的所有依赖是否已经被构建。</p><p>scheme 文件定义是存储在 <code>Project.xcodeproj/xcshareddata/xcodeproj.xcscheme</code> 路径下的一个标准的 XML 文本，因此你可以很容易的使用任意 XML 编辑器来修改它。</p><h4 id="Workspace"><a href="#Workspace" class="headerlink" title="Workspace"></a>Workspace</h4><p>多个 project 文件被组合成一个 workspace。当 project 被添加到一个 workspace 的时候：</p><ol><li>其 schemes 会出现在 workspace 的 scheme 列表中；</li><li>project 彼此可以产生依赖关系，后文会讲到。</li></ol><p>和 scheme 类似，workspace 也是普通的 XML 文件，修改起来很方便。</p><p><img src="https://i.imgur.com/wGXI5fy.png" alt="工程构建组合中的每一个基础元素： target、scheme、project 以及 workspace 是如何被组织起来的"></p><h4 id="Dependencies-🌱"><a href="#Dependencies-🌱" class="headerlink" title="Dependencies 🌱"></a>Dependencies 🌱</h4><p>每个 target 都可以有依赖，这些依赖是 target 需要链接的那些 framework 以及 library等，其包含了能够被 target 共享的源代码以及资源。这些依赖项可以被静态或者动态的链接。</p><p><strong>静态链接：</strong></p><ul><li>发生在编译阶段；</li><li>库（Library）中的代码文件会被包含到应用的二进制文件中（会增大应用的二进制大小）；</li><li>库使用 .a 作为文件后缀，其来自 (ar)chive file3 type；</li><li>如果相同的库被多次链接，编译器会由于 duplicated symbols 编译失败。</li></ul><p><strong>动态链接：</strong></p><ul><li>模块在应用启动或者运行过程中被加载；</li><li>应用或者扩展的 target 都可以共享相同的动态库（仅被复制一份）</li></ul><p>关于 framework 和 library（无论是静态链接还是动态链接）的区别在于前者可以在相同的 bundle 中包含多个版本，还可以包含额外的资源文件。</p><blockquote><p>一个 Library 是一个 .a 文件，其来源于 归档（archive）文件类型。一个单一的归档文件仅支持单一的架构。如果需要打包多个架构，则需要将其打包成胖Match-O二进制（<strong>fat Match-O binary</strong>），该二进制文件是一种容器格式，其将支持不同架构的Mach-O打包在一起。如果我们想生成、修改一个胖二进制文件或者从其中提取某个特定架构的库的话，可以使用命令行工具<code>lipo</code>。</p></blockquote><p>你可以在<a href="https://pewpewthespells.com/blog/static_and_dynamic_libraries.html" target="_blank" rel="noopener">这里</a>了解更多关于 frameworks/libraries 以及 static/dynamic 的内容。</p><p><img src="https://i.imgur.com/qKHdwhp.png" alt="动态链接和静态链接的区别"></p><p>应用的依赖项分为预编译过的（<strong>precompiled</strong>）和未经编译过（<strong>not-compiled</strong>）两类。</p><h5 id="Precompiled-dependencies"><a href="#Precompiled-dependencies" class="headerlink" title="Precompiled dependencies"></a>Precompiled dependencies</h5><p><a href="https://github.com/carthage" target="_blank" rel="noopener">Carthage</a> 是这类型依赖的典型代表。某些 SDK，比如 <a href="https://github.com/CocoaPods/Specs/blob/master/Specs/0/3/5/Firebase/4.2.0/Firebase.podspec.json#L23" target="_blank" rel="noopener">Firebase</a> 就是作为编译过的依赖来发布的。当预编译过的依赖是库（library）的时候，这些依赖就包含 <code>.a</code> 的库及一个公共头文件，包含了该库所暴露出的公共接口。当这些依赖是 framework 的时候，这些依赖就以包含了库和资源文件的 <code>.framework</code> 文件发布。</p><p>当我们的 app 依赖的是预编译依赖的时候，很重要的一点是，这些依赖也是依照我们 app 所构建架构来构建出来的。一旦其中缺失某个架构的代码，我们就会在编译 app 的时候收到编译错误。一会儿后文会看到，Carthage 使用 lipo 工具生成那些包含模拟器或者真机所必须的架构的 framework 的，同时根据构建配置来剔除掉那些不需要的 framework。</p><h5 id="Non-compiled-dependencies"><a href="#Non-compiled-dependencies" class="headerlink" title="Non-compiled dependencies"></a>Non-compiled dependencies</h5><p><a href="https://cocoapods.org/" target="_blank" rel="noopener">CocoaPods</a> 是该种类型的典型代表。依赖项被定义在我们要链接的 frameworks/libraries 的 target 中。这里有多种方式在 Xcode 中指定我们的 target 依赖其他 target 的输出。</p><ul><li><strong>如果这些 target 分布在同一个 project 中</strong>，你可以在 Build Phase 的<em>Target dependencies</em> 中指定依赖。 Xcode 会在编译该 target 的时候首先编译这些指定的依赖项；</li><li><strong>如果这些 target 分布在不同的 project 中</strong>，我们就可以使用<em>Scheme</em>来定义这些 target 之间的依赖关系。在 scheme 的 Build 部分，我们可以定义要被构建的 target 以及以什么顺序构建（基于他们之间的依赖关系）。如果你开启了<em>Find implicit dependencies</em>标识，Xcode 能够根据每个 target 的输入输出来猜测依赖。如果 scheme 中有错误配置，你就会得到类似<code>xxxx.framework not found</code>的错误。如果在 framework 之间出现了循环依赖也会报类似的错误。</li></ul><blockquote><p>关于依赖项和配置项有个需要注意的地方：所有依赖项的配置一定要完全匹配。如果你在使用 Alpha 配置项构建你的 app，但是其依赖项中但凡出现了不包含这种配置，编译过程都会因为找不到某个 framework 而失败。当这种情况发生的时候，Xcode 不会编译该 framework 但是不报任何错误。</p></blockquote><p><img src="https://i.imgur.com/TrXKUTJ.png" alt="各个依赖项是如何基于 project 的配置得到编译的"></p><h3 id="Linking-with-Xcode"><a href="#Linking-with-Xcode" class="headerlink" title="Linking with Xcode"></a>Linking with Xcode</h3><p>Target 本身可以链接其他 target 的输出，我们可以使用 Xcode 中的工具，比如 scheme 或者 target dependencies 来指定依赖，但是，我们是如何通过定义这些依赖的链接关系来将它们融为一体的？</p><h4 id="1-动态或者静态链接-libraries-和-frameworks"><a href="#1-动态或者静态链接-libraries-和-frameworks" class="headerlink" title="1. 动态或者静态链接 libraries 和 frameworks"></a>1. 动态或者静态链接 libraries 和 frameworks</h4><p>我们可以通过以下的方式定义链接：</p><ul><li><strong>一个构建阶段（build phase）：</strong>，在所有可用的 build phase 中，有一个是定义链接的，<em>Link Binary With Libraries</em>。你可以在这里添加某个 target 的依赖项，这些依赖项可以来自于同一个 project，也可以来自同一个 workspace 中的其他 project。这个 build phase 被 Xcode 用来识别 target 被构建时所需的依赖项；</li><li><strong>编译器构建设置：</strong>一个 build phase 中所定义的内容会被转换成编译器参数。其中某些内容你也可以通过定义编译设置项做到：</li><li><code>FRAMEWORK_SEARCH_PATHS</code>：定义编译器所链接的 framework 所在路径</li><li><code>LIBRARY_SEARCH_PATHS</code>：定义编译器所链接的 library 所在路径</li><li><code>OTHER_LDFLAGS</code> <em>(Other Linker Flags)</em>：我们可以使用<code>-l</code>参数指定链接的 library，比如<code>-l&quot;1PasswordExtension&quot; -l&quot;Adjust&quot;</code>。如果需要链接一个 framework，就需要使用<code>-framework</code>参数，比如：<code>-framework &quot;GoogleSignIn&quot; -framework &quot;HockeySDK&quot;</code>。如果我们尝试链接一个无法在上方指定路径中找到的 framework 或者 library 的话，编译过程就会失败。</li></ul><h4 id="2-暴露库的头文件"><a href="#2-暴露库的头文件" class="headerlink" title="2. 暴露库的头文件"></a>2. 暴露库的头文件</h4><p>Library 的头文件需要暴露给依赖该库的 targe。为了做到这个，有一个编译设置项：<code>HEADER_SEARCH_PATHS</code>用来指定头文件所在路径。如果我们链接某个库，但是忘记暴露该库的头文件，编译过程就会因为找不到其头文件而失败。</p><h4 id="3-将-Framework-嵌入到应用中"><a href="#3-将-Framework-嵌入到应用中" class="headerlink" title="3. 将 Framework 嵌入到应用中"></a>3. 将 Framework 嵌入到应用中</h4><p>App 的 target 链接动态 framework，需要把这些依赖项复制到应用的 bundle 中。这个过程被称作 <strong>framework embedding</strong>。为了达到这个目的，我们需要使用 Xcode 的<strong>Copy Files Phase</strong>，其拷贝 这些 framework 到 <code>Frameworks</code>目录中。不仅仅需要把这些直接依赖项嵌入应用中，还包括直接依赖所依赖的项目。如果缺少任意的 framework，当我们尝试打开 app 的时候都会抛出错误。</p><hr><h3 id="案例学习-👨‍💻"><a href="#案例学习-👨‍💻" class="headerlink" title="案例学习 👨‍💻"></a>案例学习 👨‍💻</h3><p>在这个部分，我们会分析以下 Cocopods 和 Carthage 是如何贯彻上面这些概念来管理你的工程依赖的。</p><h4 id="CocoaPods"><a href="#CocoaPods" class="headerlink" title="CocoaPods"></a>CocoaPods</h4><p><img src="https://i.imgur.com/yYLLsbQ.png" alt="CocoaPods"></p><p>Cocoapods 解析你的工程依赖，并将它们融合到你的工程中。虽然直接修改你的工程配置是不太推荐的，但是它从最初的版本已经有了很大的提升，用这种方式，我们几乎不需要对 project 做很多改变。那么它底层到底是怎么做到的？</p><ul><li>它创建一个工程（project）<em>(</em><code>*Pods.xcodeproj*</code><em>)</em> ，其包含了所有的依赖项，每个依赖项以 target 的形式存在。每个 target 各自编译需要被链接到 app 中的依赖项；</li><li>它创建一个额外的 target，其依赖于其他所有的依赖项。该 target 是一个 umbrella target，用来触发其他 target 的编译。这样做也最小程度的减少了你的 project 中所需要的改变。通过链接这个 target，Xcode 会先编译其所有依赖项，然后是你的 app；</li><li>它创建了一个 workspace，包含了你的 project 以及 Pods project；</li><li>Frameworks 和 libraries 使用<code>.xcconfig</code>文件链接。这些文件被加到了你的 project 群组中，并且被设置为你 project 中 target 的配置项；</li><li>嵌入过程是通过一个构建阶段脚本（build phase script）来做到的。类似的，所有的 framework 所需要的资源也通过一个构建阶段（build phase）来完成。</li></ul><p>下面这张图展示了整个设置过程：</p><p><img src="https://i.imgur.com/UARMUhl.png" alt="CocoaPods 如何将依赖项融合到整个 Project 中"></p><h4 id="Carthage"><a href="#Carthage" class="headerlink" title="Carthage"></a>Carthage</h4><p><img src="https://i.imgur.com/IUXCxhQ.png" alt="Carthage"></p><p>Carthage 的方式和 CocoaPods 比起来大不同。除了依赖项的解析，该工具是还一种去中心化的模式，其生成那些需要被链接或者嵌入到 app 的依赖项的预编译版本。</p><ul><li>Carthage 解析依赖项，并且编译它们生成你能够链接到 app 中的动态 framework，或者为了调试所需要的符号。这些 framework 是 fat framework，支持模拟器和真机的架构；</li><li>这些 framework 被用户使用 <em>Link Binary With Libraries</em> 的构建阶段（build phase）手动的链接；</li><li>嵌入过程使用 Carthage 提供的脚本完成。这个脚本会剔除那些我们正在构建目标所不必要的架构版本；</li><li>使用同样的脚本，复制符号到合适的文件夹，使得调试能够正常进行。</li></ul><p><img src="https://i.imgur.com/HXqtoDl.png" alt="Carthage 是如何生成依赖项的 framework 和 symbol"></p><h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><ul><li><a href="http://www.knowstack.com/framework-vs-library-cocoa-ios/" target="_blank" rel="noopener">Framework vs Library</a></li><li><a href="https://pewpewthespells.com/blog/static_and_dynamic_libraries.html" target="_blank" rel="noopener">Static and dynamic libraries</a></li><li><a href="https://pewpewthespells.com/blog/buildsettings.html" target="_blank" rel="noopener">Xcode Build Settings Reference</a></li><li><a href="https://developer.apple.com/library/content/technotes/tn2435/_index.html" target="_blank" rel="noopener">Embedding Frameworks in an App</a></li><li><a href="https://developer.apple.com/library/content/documentation/MacOSX/Conceptual/BPFrameworks/Frameworks.html" target="_blank" rel="noopener">Introduction to Framework Programming Guide</a></li><li><a href="https://www.cocoanetics.com/2015/04/skipping-copy-phase-strip/" target="_blank" rel="noopener">Skippy Copy Phase Strip</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://ppinera.es/2017/09/29/modular-xcode-projects.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Modular Xcode projects&lt;/a&gt;&lt;br&gt;原作者 &amp;amp; Copyright &lt;a href=&quot;http://twitter.com/pepibumur&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@pepibumur&lt;/a&gt;&lt;br&gt;翻译：&lt;a href=&quot;https://twitter.com/OgreMergO&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;@OgreMergO&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;使用 Xcode 构建模块化的工程就需要对工程结构以及其基础概念有很好的理解才行。
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="XCode" scheme="http://hechen.xyz/tags/XCode/"/>
    
      <category term="CocoaPods" scheme="http://hechen.xyz/tags/CocoaPods/"/>
    
      <category term="Carthage" scheme="http://hechen.xyz/tags/Carthage/"/>
    
  </entry>
  
  <entry>
    <title>在 Swift 中使用工厂模式进行依赖注入</title>
    <link href="http://hechen.xyz/2017/11/19/Dependency-injection-using-factories-in-Swift/"/>
    <id>http://hechen.xyz/2017/11/19/Dependency-injection-using-factories-in-Swift/</id>
    <published>2017-11-19T10:17:15.000Z</published>
    <updated>2017-12-23T15:45:01.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.swiftbysundell.com/posts/dependency-injection-using-factories-in-swift" target="_blank" rel="noopener">Dependency injection using factories in Swift</a><br>原作者 <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">@johnsundell</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>依赖注入是一项使得代码更加可测试的关键工具。我们不需要持有某些对象，或者创建这些对象的自有依赖，或者通过单例来获取他们，而是尽可能使那些对象能够正常运转所必须的一切内容（其他对象）通过外界传入，这样做的好处在于，一方面能清晰的看得到某个对象的所有依赖项，另一方便也使得测试工作变得更为简单（因为我们可以模拟这些依赖项来捕获、验证状态以及值。）</p><p>然而，尽管依赖注入确实很有用，但是当在工程中广泛使用的时候还是会有一些痛点。随着某个对象的依赖项越来越多，初始化该对象就变得越来越蹩脚。虽然使得代码可测没毛病，但是如果像下面这种每次需要这样来写初始化方法，也太不爽了。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class UserManager &#123;</span><br><span class="line">    init(dataLoader: DataLoader, database: Database, cache: Cache,</span><br><span class="line">        keychain: Keychain, tokenManager: TokenManager) &#123;</span><br><span class="line">        // ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以，这周咱们来深入了解一下某种依赖注入的技巧，使得我们的代码不失去可测性，我们也不需要再强迫自己去写一团初始化方法或者复杂的依赖管理的代码。</p><h2 id="传递依赖项"><a href="#传递依赖项" class="headerlink" title="传递依赖项"></a>传递依赖项</h2><p>我们遇到上面代码 demo 中的问题，最主要的原因是我们需要把这么多依赖项传递给某个对象，以便之后来使用。举例来说，我们在构建一个消息收发的 App，这里有一个 view controller 需要展示某个用户所有的消息：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class MessageListViewController: UITableViewController &#123;</span><br><span class="line">    private let loader: MessageLoader</span><br><span class="line"></span><br><span class="line">    init(loader: MessageLoader) &#123;</span><br><span class="line">        self.loader = loader</span><br><span class="line">        super.init(nibName: nil, bundle: nil)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    override func viewWillAppear(_ animated: Bool) &#123;</span><br><span class="line">        super.viewWillAppear(animated)</span><br><span class="line"></span><br><span class="line">        loader.load &#123; [weak self]() messages in</span><br><span class="line">            self?.reloadTableView(with: messages)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上代码能看到，我们给 <code>MessageListViewController</code> 传入某个依赖项 <code>MessageLoader</code>，之后其被用来加载数据。这里其实并没有太大的问题，因为仅仅只有一个依赖而已。然而，我们的列表视图并不是一个死气沉沉的展示而已，某些状态下还需要们进行导航到某视图控制器的工作。</p><p>具体来讲，我们想让用户在点击消息列表中某个 cell 的时候，导航到一个新的视图中。我们为这个新的视图创建一个视图控制器 <code>MessageViewController</code>，使得用户能够单独查看某条消息，并且能够回复该消息。为了该功能，我们实现了 <code>MessageSender</code> 类，当创建该类的时候，我们将前面那个新的视图控制器传递给他，代码类似下面这样：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">override func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) &#123;</span><br><span class="line">    let message = messages[indexPath.row]()</span><br><span class="line">    let viewController = MessageViewController(message: message, sender: sender)</span><br><span class="line">    navigationController?.pushViewController(viewController, animated: true)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>问题来了，<code>MessageViewController</code> 需要有一个 <code>MessageSender</code> 实例，我们也需要使得 <code>MessageListViewController</code> 看到该类。一种办法就是简单的，将 sender 加入到 列表视图控制器的初始化方法中，如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class MessageListViewController: UITableViewController &#123;</span><br><span class="line">    init(loader: MessageLoader, sender: MessageSender) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一旦如上面这样开始写代码，我们就逐步的进入庞大初始化方法的不归路上咯，然后使得 <code>MessageListViewController</code> 会变得越来越难用（也会让调用这很困惑，为什么一个列表视图控制器还需要关心某个发送消息的人？）。</p><p>另外一个可能的解决方案（也是一个很常用的解决方案），就是把 MessageSender 做成一个单例，这样的话，我们可以很容易在任何地方取到他的值，也可以随时将单例对象注入<code>MessageViewController</code> 中：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let viewController = MessageViewController(</span><br><span class="line">    message: message,</span><br><span class="line">    sender: MessageSender.shared</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>然而，就如 <a href="https://www.swiftbysundell.com/posts/avoiding-singletons-in-swift" target="_blank" rel="noopener">Avoiding singletons in Swift</a> 这篇文章中讲的，单例这种方式会伴随一些明显的缺陷，导致我们会难以看清依赖关系，从而对整个框架都难以理解。</p><h2 id="工厂模式来救场"><a href="#工厂模式来救场" class="headerlink" title="工厂模式来救场"></a>工厂模式来救场</h2><p>Wouldn’t it be nice if we could just skip all of the above, and enable MessageListViewController to be completely unaware of MessageSender, and all other dependencies that any subsequent view controllers might need?</p><p>如果我们能够避免掉上面这些问题，能够使得 <code>MessageListViewController</code> 完全不关心 <code>MessageSender</code>，甚至是后续的视图控制器的其他依赖，岂不是很爽？</p><p>如果我们有某种形式的工厂，我们可以给其传入指定的 message，然后很方便的产出一个 <code>MessageViewController</code> 出来，类似下面这样，就能够很方便并且简洁的实现上面的理想：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let viewController = factory.makeMessageViewController(for: message)</span><br></pre></td></tr></table></figure><p>如 <a href="https://www.swiftbysundell.com/posts/using-the-factory-pattern-to-avoid-shared-state-in-swift?rq=factories" target="_blank" rel="noopener">Using the factory pattern to avoid shared state in Swift</a> 这篇文章中我们看到的，关于工厂模式中，我最喜欢的一点就是，他能够使得你将某个对象的创建和使用两者解耦，也能使得许多对象和这些对象的依赖之间有一个相对解耦的关系，进而能使得我们想重构代码或者修改某些部分的时候相对更容易一些。</p><h3 id="那我们该怎么做呢？"><a href="#那我们该怎么做呢？" class="headerlink" title="那我们该怎么做呢？"></a>那我们该怎么做呢？</h3><p>首先，我们定义一个工厂协议，该协议使得我们能够在并不知道某个视图控制器的依赖项或者其初始化方法的前提下，很容易的在我们的应用中创建出我们需要的任意的视图控制器。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol ViewControllerFactory &#123;</span><br><span class="line">    func makeMessageListViewController() -&gt; MessageListViewController</span><br><span class="line">    func makeMessageViewController(for message: Message) -&gt; MessageViewController</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>到这里我们还不能停止。我们同样为工厂添加一些附件的协议用来创建视图控制器的依赖，比如下面这个协议，使得我们可以为某个列表视图控制器生成一个 <code>MessageLoader</code> 出来：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">protocol MessageLoaderFactory &#123;</span><br><span class="line">    func makeMessageLoader() -&gt; MessageLoader</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="单例依赖"><a href="#单例依赖" class="headerlink" title="单例依赖"></a>单例依赖</h2><p>一旦我们准备好这些工厂协议之后，回到上面 <code>MessageListViewController</code> 的地方，重构这段代码，无需使用其依赖项的实例而是简单的引入一个工厂实例即可。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class MessageListViewController: UITableViewController &#123;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    typealias Factory = MessageLoaderFactory &amp; ViewControllerFactory</span><br><span class="line"></span><br><span class="line">    private let factory: Factory</span><br><span class="line">    </span><br><span class="line">    private lazy var loader = factory.makeMessageLoader()</span><br><span class="line"></span><br><span class="line">    init(factory: Factory) &#123;</span><br><span class="line">        self.factory = factory</span><br><span class="line">        super.init(nibName: nil, bundle: nil)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过上面这么做，我们可以做到两点：</p><ol><li>我们将一堆依赖项简化成了一个单一的工厂；</li><li><code>MessageListViewController</code> 无需再需再关心 <code>MessageViewController</code> 的依赖项了</li></ol><h2 id="一个使用-Container-的例子"><a href="#一个使用-Container-的例子" class="headerlink" title="一个使用 Container 的例子"></a>一个使用 Container 的例子</h2><p>接下来，我们该实现工厂协议了。首先，我们需要定义一个 <code>DependencyContainer</code>，该对象会包含我们应用中那些正常情况下会被直接用来作为依赖的核心工具对象们。这些不仅仅包括类似之前 <code>MessageSender</code>，也包括更加底层的业务逻辑上的类，比如我们可能会用到 <code>NetworkManager</code>。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class DependencyContainer &#123;</span><br><span class="line">    private lazy var messageSender = MessageSender(networkManager: networkManager)</span><br><span class="line">    private lazy var networkManager = NetworkManager(urlSession: .shared)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从上面这段代码，你能看到，我们使用了懒加载属性以便能够在初始化该对象的时候能够引用相同类中的其他属性。这是设置你依赖关系的一种非常方便而且优雅的方式，你可以利用编译器帮助你避免比如<a href="https://en.wikipedia.org/wiki/Circular_dependency" target="_blank" rel="noopener">引用循环</a>等问题。</p><p>最后，我们为 <code>DependencyContainer</code> 实现我们的工厂协议，使得我们能够将该工厂注入各种视图控制器或其他对象中：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">extension DependencyContainer: ViewControllerFactory &#123;</span><br><span class="line">    func makeMessageListViewController() -&gt; MessageListViewController &#123;</span><br><span class="line">        return MessageListViewController(factory: self)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func makeMessageViewController(for message: Message) -&gt; MessageViewController &#123;</span><br><span class="line">        return MessageViewController(message: message, sender: messageSender)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">extension DependencyContainer: MessageLoaderFactory &#123;</span><br><span class="line">    func makeMessageLoader() -&gt; MessageLoader &#123;</span><br><span class="line">        return MessageLoader(networkManager: networkManager)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="分布式的所有权"><a href="#分布式的所有权" class="headerlink" title="分布式的所有权"></a>分布式的所有权</h2><p>最后一步了，我们在哪里实际储存依赖存储器，谁应该拥有它？它应该在哪里设置？这里有些比较 cool 的事情就是，由于我们把依赖容器作为对象们所需要的工厂的一种实现，而对象们强持有其工厂，所以，我们其实无需在任何地方储存该依赖容器。</p><p>举例来说，如果 <code>MessageListViewController</code> 是我们应用的初始化视图控制器，我们可以很简单的创建一个 <code>DependencyContainer</code> 的实例传入：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let container = DependencyContainer()</span><br><span class="line">let listViewController = container.makeMessageListViewController()</span><br><span class="line"></span><br><span class="line">window.rootViewController = UINavigationController(</span><br><span class="line">    rootViewController: listViewController</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>无需保留任何全局的变量或者在 app delegate 中使用可选属性。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>使用工厂协议和容器配置依赖注入是一种很好的方式，其可以避免需要传递大量依赖而创建很复杂的初始化方法。它可以使得依赖注入使用起来更加方便，使得你能够对自己创建的对象实际的依赖关系有很明晰的判断，也使得测试更加简单。</p><p>因为我们能够把工厂定义为协议，因此可以很容易的在测试中通过给定不同测试指定版本的具体实现来模拟输出。未来我会写大量关于模拟数据以及如何在测试中充分利用依赖注入的博文。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.swiftbysundell.com/posts/dependency-injection-using-factories-in-swift&quot; target=&quot;_blank&quot; rel=&quot;noopene
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="XCode" scheme="http://hechen.xyz/tags/XCode/"/>
    
  </entry>
  
  <entry>
    <title>Initializer in Swift</title>
    <link href="http://hechen.xyz/2017/11/15/Initializer-in-Swift/"/>
    <id>http://hechen.xyz/2017/11/15/Initializer-in-Swift/</id>
    <published>2017-11-15T05:09:27.000Z</published>
    <updated>2018-10-07T11:36:34.000Z</updated>
    
    <content type="html"><![CDATA[<p>我想 <code>[[xxx alloc] init]</code> 应该是自己接触 iOS 开发以来写的最多的语句了吧。一个对象初始化是其得以存在的前提，无论你是使用 new 关键字与否，根本上都会执行到 init 方法。</p><p>转入 swift 语言之后，在刚接触的时候经常会因为自建初始化方法不合适，编译器报一堆错误，很是痛苦，但是当理解 Swift 语言为了保障安全的初始化行为所做的工作之后，你就会明白，Swift 初始化方法是有一定的规范要求的。 </p><p>这篇文章是想做个记录。</p><h3 id="初始化方法"><a href="#初始化方法" class="headerlink" title="初始化方法"></a>初始化方法</h3><p>顺序很重要！</p><p>与 <code>Objective-C</code> 中不同，Swift 语言会保证某个类的所有属性均被初始化才行。因此初始化方法会非常严格，那第一点需要保障的就是<strong>顺序</strong>。</p><p>首先，祭出这张官方镇宅图：</p><p><img src="media/initializerDelegation02_2x.png" alt="initializerDelegation02_2x"></p><p>在某个类的子类中，初始化方法语句的顺序并不是随意的，我们需要保证在当前子类实例的成员初始化完成后才能调用父类的初始化方法。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;我想 &lt;code&gt;[[xxx alloc] init]&lt;/code&gt; 应该是自己接触 iOS 开发以来写的最多的语句了吧。一个对象初始化是其得以存在的前提，无论你是使用 new 关键字与否，根本上都会执行到 init 方法。&lt;/p&gt;
&lt;p&gt;转入 swift 语言之后，在刚接
      
    
    </summary>
    
      <category term="Swift" scheme="http://hechen.xyz/categories/Swift/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="Initializer" scheme="http://hechen.xyz/tags/Initializer/"/>
    
      <category term="Designated Initializer" scheme="http://hechen.xyz/tags/Designated-Initializer/"/>
    
      <category term="Convenience Initializers" scheme="http://hechen.xyz/tags/Convenience-Initializers/"/>
    
  </entry>
  
  <entry>
    <title>处理 Swift 中非可选的可选值类型</title>
    <link href="http://hechen.xyz/2017/11/14/Handling-non-optional-optionals-in-Swift/"/>
    <id>http://hechen.xyz/2017/11/14/Handling-non-optional-optionals-in-Swift/</id>
    <published>2017-11-14T13:53:52.000Z</published>
    <updated>2017-11-14T15:43:52.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.swiftbysundell.com/posts/handling-non-optional-optionals-in-swift" target="_blank" rel="noopener">Handling non-optional optionals in Swift</a><br>原作者 <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">@johnsundell</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>可选值（optionals）无可争议的是 <code>swift</code> 语言中最重要的特性之一，也是和其他语言，例如 <code>Objective-C</code> 的最大区别。通过强制处理那些有可能出现 <code>nil</code> 的地方，我们就能写出更有预测性的以及更健壮的代码。</p><p>然而，有些时候可选值可能会致你于尴尬的境地，尤其是你作为开发者了解（甚至是有些猜测的成分在），有的特定变量始终是非空（non-nil）的，即使它是一个可选类型。例如，我们在一个视图控制器中处理视图的时候：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class TableViewController: UIViewController &#123;</span><br><span class="line">    var tableView: UITableView?</span><br><span class="line"></span><br><span class="line">    override func viewDidLoad() &#123;</span><br><span class="line">        super.viewDidLoad()</span><br><span class="line">        tableView = UITableView(frame: view.bounds)</span><br><span class="line">        view.addSubview(tableView!)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func viewModelDidUpdate(_ viewModel: ViewModel) &#123;</span><br><span class="line">        tableView?.reloadData()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这也是对于很多 Swift 程序员争论比较激烈的地方，程度不亚于讨论 tabs 和 spaces 的用法。有的人会说：</p><blockquote><p>既然它是一个可选值，你就应该时刻使用 <code>if let</code> 或者 <code>guard let</code> 的方式进行解包。</p></blockquote><p>然而另外一些人则采用完全相反，说：</p><blockquote><p>既然你知道这个变量在使用的时候不会为 <code>nil</code>，使用 <code>!</code> 强制解包多好。崩溃也要比让你的程序处于一个未知状态要好吧。</p></blockquote><p>本质上来讲，我们这里讨论的是要不要采用防御性编程（<a href="https://en.wikipedia.org/wiki/Defensive_programming" target="_blank" rel="noopener">defensive programming</a>）的问题。我们是试图让程序从一个未知状态恢复还是简单的放弃，然后让它崩溃掉？</p><p>如果非得让我对这个问题给出一个答案的话，我更倾向于后者。未知状态真的很难追踪 bug，会导致执行很多不想执行的逻辑，采用防御性编程就会使得你的代码很难追踪，出现问题很难追踪。</p><p>但是，我不太喜欢给出一个二选一的答案。相反，我们可以寻找一些技术手法，用更精妙的方式的解决上面提到的问题。</p><h2 id="它真的可选的吗？"><a href="#它真的可选的吗？" class="headerlink" title="它真的可选的吗？"></a>它真的可选的吗？</h2><p>那些可选类型的，但是被代码逻辑真实需要的变量和属性，实际上是架构瑕疵的一个体现。如果在某些地方确实需要它，但是它又不在，就会使得你的代码逻辑处于未知状态，那么它就不应该是可选类型的。</p><p>当然，在某些特定场景下，可选值确实很难避免（尤其是和特定的系统 API 交互的时候），那对于大部分这种情况，我们有一些技术来处理从而避免可选值。</p><h3 id="lazy-要比非可选的可选值更好"><a href="#lazy-要比非可选的可选值更好" class="headerlink" title="lazy 要比非可选的可选值更好"></a>lazy 要比非可选的可选值更好</h3><p>某些属性的值需要在其父类创建之后再生成（比如视图控制器中的那些视图，应该在 <code>loadView()</code>或者 <code>viewDidLoad()</code>方法中被创建），对于这种属性要避免其可选类型的方法就是使用 <code>lazy</code> 属性。一个<code>lazy</code>属性是可以是非可选类型的，同时也不在其父类的初始化方法里被需要，它会在其第一次被获取的时候创建出来。</p><p>让我们改一下上面的代码，使用 lazy 来改造 tableView 属性：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class TableViewController: UIViewController &#123;</span><br><span class="line">    lazy var tableView = UITableView()</span><br><span class="line"></span><br><span class="line">    override func viewDidLoad() &#123;</span><br><span class="line">        super.viewDidLoad()</span><br><span class="line">        tableView.frame = view.bounds</span><br><span class="line">        view.addSubview(tableView)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func viewModelDidUpdate(_ viewModel: ViewModel) &#123;</span><br><span class="line">        tableView.reloadData()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，没有可选值了，也不会有未知状态咯🎉</p><h3 id="适当的依赖管理要比非可选的可选值要好"><a href="#适当的依赖管理要比非可选的可选值要好" class="headerlink" title="适当的依赖管理要比非可选的可选值要好"></a>适当的依赖管理要比非可选的可选值要好</h3><p>可选值类型另外一种常用的场景就是用来打破循环依赖（<a href="https://en.wikipedia.org/wiki/Circular_dependency" target="_blank" rel="noopener">circular dependencies</a>）。有的时候，你就陷入 A 依赖 B，B 又依赖 A 的情况，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class UserManager &#123;</span><br><span class="line">    private weak var commentManager: CommentManager?</span><br><span class="line"></span><br><span class="line">    func userDidPostComment(_ comment: Comment) &#123;</span><br><span class="line">        user.totalNumberOfComments += 1</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func logOutCurrentUser() &#123;</span><br><span class="line">        user.logOut()</span><br><span class="line">        commentManager?.clearCache()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class CommentManager &#123;</span><br><span class="line">    private weak var userManager: UserManager?</span><br><span class="line"></span><br><span class="line">    func composer(_ composer: CommentComposer</span><br><span class="line">                  didPostComment comment: Comment) &#123;</span><br><span class="line">        userManager?.userDidPostComment(comment)</span><br><span class="line">        handle(comment)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func clearCache() &#123;</span><br><span class="line">        cache.clear()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从上面的代码，我们可以看到，<code>UserManager</code> 和 <code>CommentManager</code> 之间有一个循环依赖的问题，它们二者都没法假设自己拥有对方，但是它们都在各自的代码逻辑里依赖彼此。这里就很容易产生 bug。</p><p>那要解决上面的问题，我们创建一个 <code>CommentComposer</code> 来做一个协调者，负责通知<code>UserManager</code> 和 <code>CommentManager</code>二人一个评论产生了。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class CommentComposer &#123;</span><br><span class="line">    private let commentManager: CommentManager</span><br><span class="line">    private let userManager: UserManager</span><br><span class="line">    private lazy var textView = UITextView()</span><br><span class="line"></span><br><span class="line">    init(commentManager: CommentManager,</span><br><span class="line">         userManager: UserManager) &#123;</span><br><span class="line">        self.commentManager = commentManager</span><br><span class="line">        self.userManager = userManager</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func postComment() &#123;</span><br><span class="line">        let comment = Comment(text: textView.text)</span><br><span class="line">        commentManager.handle(comment)</span><br><span class="line">        userManager.userDidPostComment(comment)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过这种形式，UserManager 可以强持有 CommentManager 也不产生任何依赖循环。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class UserManager &#123;</span><br><span class="line">    private let commentManager: CommentManager</span><br><span class="line"></span><br><span class="line">    init(commentManager: CommentManager) &#123;</span><br><span class="line">        self.commentManager = commentManager</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    func userDidPostComment(_ comment: Comment) &#123;</span><br><span class="line">        user.totalNumberOfComments += 1</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们又一次的移除了所有的可选类型，代码也更好预测了🎉。</p><h3 id="优雅的崩溃（Crashing-gracefully）"><a href="#优雅的崩溃（Crashing-gracefully）" class="headerlink" title="优雅的崩溃（Crashing gracefully）"></a>优雅的崩溃（Crashing gracefully）</h3><p>通过上面几个例子，我们通过对代码做一些调整，移除了可选类型从而排除了不确定性。然而，有的时候，移除可选类型是不可能的。让我们举个例子，比如你在加载一个本地的包含针对你 App 的配置项的 JSON 文件，这个操作本身一定会存在失败的情况，我们就需要添加错误处理。</p><p>继续上面这个场景，加载配置文件失败的时候继续执行代码就会使得你的 app 进入一个未知状态，在这种情况下，最好的方式让它崩溃。这样，我们会得到一个崩溃日志，希望这个问题能够在用户感知之前早早的被我们的测试人员以及 QA 处理掉。</p><p>所以，我们如何崩溃。。。最简单的方式就是添加 <code>!</code> 操作符，针对这个可选值强制解包，就会在其是 nil 的时候发生崩溃：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let configuration = loadConfiguration()!</span><br></pre></td></tr></table></figure><p>虽然这个方法比较简单，但是它有个比较大的问题，就是一旦这段代码崩溃，我们能得到的只有一个错误信息：</p><blockquote><p>fatal error: unexpectedly found nil while unwrapping an Optional value</p></blockquote><p>这个错误信息并不告诉我们为什么发生这个错误，在哪里发生的，给不了我们什么线索来解决它。这个时候，我们可以使用 guard 关键字，结合 <code>preconditionFailure()</code> 函数，在程序退出的时候给出定制消息。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">guard let configuration = loadConfiguration() else &#123;</span><br><span class="line">    preconditionFailure(&quot;Configuration couldn&apos;t be loaded. &quot; +</span><br><span class="line">                        &quot;Verify that Config.JSON is valid.&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面这段代码发生崩溃的时候，我们就能获得更多更有效的错误信息：</p><blockquote><p>fatal error: Configuration couldn’t be loaded. Verify that Config.JSON is valid.: file /Users/John/AmazingApp/Sources/AppDelegate.swift, line 17</p></blockquote><p>这样，我们现在有了一个更清晰的解决问题的办法，能够准确的知道这个问题在我们代码里的哪个未知发生的。</p><h3 id="引入-Require-库"><a href="#引入-Require-库" class="headerlink" title="引入 Require 库"></a>引入 Require 库</h3><p>使用上面的 guard-let-preconditionFailure 的方案还是有一些冗长，确实让我们呃代码更难驾驭。我们也确实不希望在我们的代码里占很多篇幅去些这种代码，我们想更专注于我们的代码逻辑上。</p><p>我的解决方案就是使用 <code>Require</code>。它只是简单的在可选值添加简单的 <code>require()</code> 方法，但能够使得调用的地方更简洁。用这种方法来处理上面加载 <code>JSON</code> 文件的代码就可以这样写：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let configuration = loadConfiguration().require(hint: &quot;Verify that Config.JSON is valid&quot;)</span><br></pre></td></tr></table></figure><p>当出现异常的时候，会给出下面的错误信息：</p><blockquote><p>fatal error: Required value was nil. Debugging hint: Verify that Config.JSON is valid: file /Users/John/AmazingApp/Sources/AppDelegate.swift, line 17</p></blockquote><p><code>Require</code> 的另一个优势就是它和调用 <code>preconditionFailure()</code> 方法一样也会抛异常 <code>NSException</code>，就能使得那些异常上报工具能够捕获异常发生时候的元数据。</p><p>你如果想在自己代码中使用的话，<a href="https://github.com/JohnSundell/Require.git" target="_blank" rel="noopener">Require 现在在 Github 上开源了</a></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>所以，总结来看，在 <code>Swift</code> 语言里处理那些非可选的可选值，我有几点自己的贴心小提示给大家：</p><ol><li><code>lazy</code> 属性要比非可选的可选值要更好</li><li>适当的依赖管理要比非可选的可选值要好</li><li>当你使用非可选的可选值的时候，优雅的崩溃</li></ol><p>如果有任何问题、建议或者反馈，都欢迎随时在 <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">Twitter</a> 上联系我，我非常乐意听到你们希望我在接下来的文章里谈论哪些主题哦。</p><p>谢谢阅读。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.swiftbysundell.com/posts/handling-non-optional-optionals-in-swift&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Ha
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="Optional" scheme="http://hechen.xyz/tags/Optional/"/>
    
  </entry>
  
  <entry>
    <title>理解响应者和响应链</title>
    <link href="http://hechen.xyz/2017/11/13/Understanding-Responders-and-the-Responder-Chain/"/>
    <id>http://hechen.xyz/2017/11/13/Understanding-Responders-and-the-Responder-Chain/</id>
    <published>2017-11-13T13:22:44.000Z</published>
    <updated>2018-05-02T10:46:15.000Z</updated>
    
    <content type="html"><![CDATA[<p>Apps 是通过响应者（responder）对象来接收和处理事件的。一个响应者对象是 UIResponder 类的一个实例，我们常见的 UIView，UIViewController 以及 UIApplication 都是 UIResponder 的子类。 UIKit 自动帮你管理着这些 responder 相关的行为，包括事件是如何从一个 responder 传递给另一个 responder 的等等。当然，你也可以修改你的 app 中事件传递的默认行为。</p><p>UIKit 会把大部分的事件都传递给最适合的 responder 对象来处理。如果该 responder 无法处理该事件，UIKit 就会继续把该事件沿着当前的响应者链传递到下一个 responder。响应者链就是你的 App 中所有响应者的动态配置，也因为其是动态的，你的 App 中不可能只存在单一的响应者链。由于事件总是从特定的响应者那里流转到更通用的响应者那里，因此很容易确定某响应者链中下一个响应者是谁。举个例子，一个 view 的下一个响应者是其 superview 或者负责管理它的 view controller。事件就是这样在响应者链中传递直到其被处理掉。</p><p>下图1 表明一个界面中包含了 一个 Label，一个 text field，一个 button 以及两个 background view 的 App 中响应链是什么样子的。如果 text field 不处理某个事件，UIKit 就会把该事件发送给 text field 的父级 UIView 对象，同样的，如果该对象依然处理不了，就会传递给该 view 对象的 window。如果 window 对象也依然无法处理该事件，UIKit 最终会把该事件传递给 UIApplication 对象，一般上该 UIApplication 对象是 App 的 delegate 对象并且是 UIResponder 实例，当然这个时候已经脱离了响应者链了。</p><p><img src="https://ws4.sinaimg.cn/large/006tKfTcly1flgs9djns2j30zp0lyac6.jpg" alt="图1 - 一个响应者链的例子"></p><p>针对每一个事件，UIKit 都会指定一个第一响应者（first responder），然后把该事件首先发送给对象处理。这个第一响应者基于事件类型而不同。</p><ol><li><strong>Touch events.</strong> 第一响应者是 Touch 发生所在的 view。</li><li><strong>Press events.</strong> 第一响应者是当前焦点所在的响应者。</li><li><strong>Motion event.</strong> 第一响应者是你显式指定用以处理事件的对象。 Core Motion 处理所有的和加速器、气压仪以及磁力计相关的事件。Motion events 不随响应者链流动。</li><li><strong>Shake-motion events.</strong> 第一响应者是你或者 UIKit 框架指定的对象。</li><li><strong>Remote-control events.</strong> 第一响应者是你或者 UIKit 框架指定的对象。</li><li><strong>Editing-menu messages.</strong> 第一响应者是你或者 UIKit 框架指定的对象。</li></ol><p>Controls 向其关联的对象发送动作消息本身不是事件，但是依然能够享受到响应者链的好处。当一个 Control 的 target object 是 nil 的时候，UIKit 就会在该 target object 的响应者链上寻找合适的对象用以妥善处理动作消息。例如，UIKit 编辑按钮使用这种行为来寻找响应者对象来执行<code>cut:</code>, <code>copy:</code> 或者 <code>paste:</code> 等方法。</p><p>如果一个 view 自身还有附加的手势识别器的话，该手势识别器会延迟针对该 view 的 touch 和 press 事件的传递。<code>delaysTouchesBegan</code>, <code>delaysTouchesEnded</code> 以及 <code>UIGestureRecognizer</code> 的 <code>cancelsTouchesInView</code> 属性都是用来决定这些 touches 什么时间以及以什么方式被延迟处理。</p><h3 id="识别包含-Touch-事件的响应者"><a href="#识别包含-Touch-事件的响应者" class="headerlink" title="识别包含 Touch 事件的响应者"></a>识别包含 Touch 事件的响应者</h3><p>UIKit 使用基于视图的碰撞检测（hit-testing）来决定 touch 事件发生的地点。具体而言，UIKit  拿该 touch 的位置和视图层级中所有的视图对象的 bounds 进行比较。UIView 的 <code>hitTest:withEvent:</code> 方法会游历整个视图层级，找到该 touch 事件发生所在的视图树最底端的视图，其也就是处理该 touch 事件的第一响应者。</p><blockquote><p>如果一个 touch 的位置发生在某个视图的外围，<code>hitTest:withEvent:</code>方法就会忽略该视图和其所有子视图。所以，如果你将 view 的<code>clipsToBounds</code>属性设置为 NO 的化，即使其子视图将该 touch 包含在自己领域也是无效的。</p></blockquote><p>就这样，UIKit 持续不断的把每个 touch 指派给包含该 touch 的视图。当 touch 发生的时候，UIKit 创建一个 <code>UITouch</code> 对象，直到 touch 结束该对象才会被释放。当 touch 位置或者其他参数发生变化的时候，UIKit 就更新这个 UITouch 对象的信息。当然，其中有的属性当然是不会变化的，比如该 touch 附属的 view，甚至当 touch 位置已经超出原始 view 的外围的时候，UITouch 对象中的 view 属性依然保持和之前一样。</p><h3 id="变更响应者链"><a href="#变更响应者链" class="headerlink" title="变更响应者链"></a>变更响应者链</h3><p>你可以通过覆写响应者的 nextResponder 属性来改变响应者链。许多 UIKit 的类已经覆写了该方法并且返回了特定的对象。</p><ol><li>如果你覆写了任意类的 <code>nextResponder</code> 属性，那该对象的下一个响应者就是你返回的那个；</li><li><code>UIView</code><br> 2.1 如果该视图是某个视图控制器的根视图（root view），其下一个响应者就是该视图控制器；<br> 2.2 如果该视图不是某个视图控制器的根视图，其下一个响应者就是该视图的父视图；</li><li><code>UIViewController</code><br> 3.1 如果该视图控制器的视图是某个 window 的根视图（root view），其下一个响应者就是 window；<br> 3.2 如果该视图控制器是由另一个视图控制器展示出来的，其下一个响应者就是那个视图控制器（presenting view controller）；</li><li><code>UIWindow</code>  window 的下一个响应者就是 <code>UIApplication</code> 对象</li><li><code>UIApplication</code>  UIApplication 对象的下一个响应者是 App delegate，而且要求该 delegate 必须是 <code>UIRepsponder</code> 的实例并且不能是一个视图、视图控制器或者 <code>UIApplication</code> 对象自己。</li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Apps 是通过响应者（responder）对象来接收和处理事件的。一个响应者对象是 UIResponder 类的一个实例，我们常见的 UIView，UIViewController 以及 UIApplication 都是 UIResponder 的子类。 UIKit 自动
      
    
    </summary>
    
      <category term="iOS" scheme="http://hechen.xyz/categories/iOS/"/>
    
    
      <category term="UIResponder" scheme="http://hechen.xyz/tags/UIResponder/"/>
    
      <category term="UIGestureRecognizer" scheme="http://hechen.xyz/tags/UIGestureRecognizer/"/>
    
  </entry>
  
  <entry>
    <title>Capturing objects in Swift closures</title>
    <link href="http://hechen.xyz/2017/11/12/Capturing-objects-in-Swift-closures/"/>
    <id>http://hechen.xyz/2017/11/12/Capturing-objects-in-Swift-closures/</id>
    <published>2017-11-12T12:56:50.000Z</published>
    <updated>2017-11-13T01:32:26.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文：<a href="https://www.swiftbysundell.com/posts/capturing-objects-in-swift-closures" target="_blank" rel="noopener">Capturing objects in Swift closures</a><br>原作者 <a href="https://twitter.com/johnsundell" target="_blank" rel="noopener">@johnsundell</a><br>翻译：<a href="https://twitter.com/OgreMergO" target="_blank" rel="noopener">@OgreMergO</a></p></blockquote><p>自从 Block 在 iOS4 被引入 Objective-C 的世界之后就成为了 Apple 各平台上最时髦的 API 的重要组成部分了。当 Swift 语言出现的时候，blocks 的概念就摇身一变通过 closure 的形式引入，成为了目前我们可能每一天都在用的语言特性之一了。</p><p>Closure 目前已经被我们广泛的使用了，即使如此，我们在使用它的时候还是需要有很多需要注意的点，并且需要做很多额外的操作。这篇文章，我们来近距离的了解 closure，主要是了解其捕获变量的机制以及那些能够更好的让我们来处理变量捕获的技术。</p><h2 id="伟大的-escape"><a href="#伟大的-escape" class="headerlink" title="伟大的 escape"></a>伟大的 escape</h2><p>Closure 有两种类型：escaping 和 non-escaping。当一个 closure 是 escaping（使用 <code>@escaping</code> 修饰闭包参数）的，也就意味着其会被以各种形式存储下来（无论是通过 property 还是被其他 closure 捕获）。相反，Non-Escaping 的　closure 意味着其不能被存储，而且在使用它的地方就必须直接被执行。</p><blockquote><p>译者注： 可以参见 <a href="http://hechen.info/2017/11/12/autoclosure-escape/" target="_blank" rel="noopener">《@autoclosure &amp;&amp; @escape》</a> 一文。</p></blockquote><p>一个显而易见的例子就是当你在一个集合类型上使用函数式操作的时候，例如 <code>forEach</code>：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[1, 2, 3].forEach &#123; number in</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上代码所示，closure 都直接作用于集合的每一个元素上，也就无需把该闭包变为 escaping 的。</p><p>而 escaping 的 closures 最常见的就是在那些异步 API 中，例如 <code>DispatchQueue</code>。例如，当你异步执行某 closure 的时候，这个 closure 就会 escape。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">DispatchQueue.main.async &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以，这两种的区别在哪里呢？ 由于 escaping 的 closure 会被以某种形式存储下来，因此，这些 closure 会同时存储当前其所处的上下文，同时就会把上下文中用到的值或者对象都捕获（capture）到，以至于当该 closure 被执行的时候，所需用到的内容没有丢失。实践中最常见的就是在 closure 中使用 self 的 API，此时，我们就需要某种办法显式的捕获 self。</p><h2 id="捕获-amp-引用循环"><a href="#捕获-amp-引用循环" class="headerlink" title="捕获 &amp; 引用循环"></a>捕获 &amp; 引用循环</h2><p>因为 escaping 的 closures 会自助捕获在其内部使用的任何值和对象，因此很容易发生引用循环。举个例子，下面描述的是一个 view controller 被其存储的 viewModel 的 closure 捕获的情况：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class ListViewController: UITableViewController &#123;</span><br><span class="line">    private let viewModel: ListViewModel</span><br><span class="line"></span><br><span class="line">    init(viewModel: ListViewModel) &#123;</span><br><span class="line">        self.viewModel = viewModel</span><br><span class="line"></span><br><span class="line">        super.init(nibName: nil, bundle: nil)</span><br><span class="line"></span><br><span class="line">        viewModel.observeNumberOfItemsChanged &#123;</span><br><span class="line">            // This will cause a retain cycle, since our view controller</span><br><span class="line">            // retains its view model, which in turn retains the view</span><br><span class="line">            // controller by capturing it in an escaping closure.</span><br><span class="line">            self.tableView.reloadData()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最常见的方式，也就是你们大部分人也都会用的解决方式，通过弱引用的方式打破这个循环引用。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">viewModel.observeNumberOfItemsChanged &#123; [weak self] in</span><br><span class="line">    self?.tableView.reloadData()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="捕获-context-而不是捕获-self"><a href="#捕获-context-而不是捕获-self" class="headerlink" title="捕获 context 而不是捕获 self"></a>捕获 context 而不是捕获 self</h2><p>上面提到的 [weak self] 的解决方案已经是你希望避免引用循环的最常用，也常常是最有效的解决方案了。但是这种方式也有一些问题：</p><ol><li>很容易忘掉写，尤其是编译器又没检查出来潜在的引用循环的时候；</li><li>当你希望从 weak self 中强持有 self 的时候还需要写一堆代码（weak strong dance），例如下面这段代码所示：</li></ol><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dataLoader.loadData(from: url) &#123; [weak self] data in</span><br><span class="line">    guard let strongSelf = self else &#123;</span><br><span class="line">        return</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    let model = try strongSelf.parser.parse(data, using: strongSelf.schema)</span><br><span class="line">    strongSelf.titleLabel.text = model.title</span><br><span class="line">    strongSelf.textLabel.text = model.text</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里其实有一个可选的解决方案，也就是不要捕获 self，而去捕获那些闭包中所需要的对象即可。例如上面例子中的 labels 和 schema 等，我们可以直接捕获它们而不至于引发引用循环（因为其也并不持有 closure 本身），下面是个解决方案，通过使用 context 的 tuple 来解决。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// We define a context tuple that contains all of our closure&apos;s dependencies</span><br><span class="line">let context = (</span><br><span class="line">    parser: parser,</span><br><span class="line">    schema: schema,</span><br><span class="line">    titleLabel: titleLabel,</span><br><span class="line">    textLabel: textLabel</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">dataLoader.loadData(from: url) &#123; data in</span><br><span class="line">    // We can now use the context instead of having to capture &apos;self&apos;</span><br><span class="line">    let model = try context.parser.parse(data, using: context.schema)</span><br><span class="line">    context.titleLabel.text = model.title</span><br><span class="line">    context.textLabel.text = model.text</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="通过显式传递参数而不是隐式的捕获"><a href="#通过显式传递参数而不是隐式的捕获" class="headerlink" title="通过显式传递参数而不是隐式的捕获"></a>通过显式传递参数而不是隐式的捕获</h2><p>这里，还有另外一种捕获对象的方式，就是显式的把这些对象通过参数传入。这种手法我在设计我的 <a href="">Image Engine</a> 项目中的 Event API 的时候用到了，这个 API 就是当使用 closure 来监听 event 的时候，需要你传递一个 observer 给它。如下所示，你把 self 传入进来的同时也使得其被传递到了 event 的 closure 中了，这也使得 self 被隐式的带入，你也无需手动的捕获它了。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">actor.events.moved.addObserver(self) &#123; scene in</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们回到之前的 ListViewController 的例子中，看一看当我们要监听其 viewModel 的时候，我们是如何通过上面这种手法来实现同样的 API 的。这种方式正好使得我们可以将要 reload 的 tableView 作为观测者传递，实现一个很优雅的调用：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">viewModel.numberOfItemsChanged.addObserver(tableView) &#123; tableView in</span><br><span class="line">    tableView.reloadData()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，需要实现上面这段代码，我们还需要做一些事情，就像 Image Engine 的事件系统如何工作类似。我们首先定义一个简单的 Event 类型，其可以记录那些观测闭包。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Event &#123;</span><br><span class="line">    private var observers = [() -&gt; Void]()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后，我们添加一个方法，该方法会传两个参数进来，一个是引用类型的观测者，另外一个是一个闭包，当观察动作一旦触发，该闭包就会被调用。核心就在这里，我们会封装该闭包，并且在内部闭包中弱捕获该观测者：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func addObserver&lt;T: AnyObject&gt;(_ observer: T, using closure: @escaping (T) -&gt; Void) &#123;</span><br><span class="line">    observers.append &#123; [weak observer] in</span><br><span class="line">        observer.map(closure)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样就使得我们只需要做这么一次 weak/string 的操作，也不影响其调用的地方。</p><p>最后，我们添加一个 trigger 方法来使得我们能够触发事件本身。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func trigger() &#123;</span><br><span class="line">    for observer in observers &#123;</span><br><span class="line">        observer()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后回到 <code>ListViewModel</code>，为 <code>numberOfItemsChanged</code> 方法添加事件，当某个条件满足的时候，就会触发该事件。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class ListViewModel &#123;</span><br><span class="line">    let numberOfItemsChanged = Event()</span><br><span class="line">    var items: [Item] &#123; didSet &#123; itemsDidChange(from: oldValue) &#125; &#125;</span><br><span class="line"></span><br><span class="line">    private func itemsDidChange(from previousItems: [Item]) &#123;</span><br><span class="line">        if previousItems.count != items.count &#123;</span><br><span class="line">            numberOfItemsChanged.trigger()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上面所看到的，基于 event 的 API 的最大优势就是最大程度上避免了引用循环的发生。我们也可以在我们的代码中为任意类型的观察事件重用相同的执行代码。当然，上面的 demo 中 Event 实现非常简单，缺乏一些高级特性，比如针对观察者的移除等等，但是对于简单使用已经足够了。</p><p>我们会在之后的博文中详细的讲述事件驱动的编程范式，你也可以详细看下在 Image Engine 项目中的 <a href="https://github.com/JohnSundell/ImagineEngine/blob/master/Sources/Core/API/Event.swift" target="_blank" rel="noopener">Event 类型的完整实现</a>。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Closure 自动捕获其内部所使用的对象和值本身是一个非常棒的特色，它使得 closure 本身变得非常好用。但是，捕获同时也引入了一些 bug 和引用循环的问题，甚至最后使得代码变得复杂和难以理解。</p><p>当然，我并不是建议大家在所有的场景下去避免捕获发生，而是想通过这篇文章提供给大家一些捕获 self 的选择。在某些场景下，使用经典的 [weak self] 是最有效的解决方案，另外一些场景则可以使用某些手法来帮助你把自己的闭包代码写的更容易使用，也更容易理解吧。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;原文：&lt;a href=&quot;https://www.swiftbysundell.com/posts/capturing-objects-in-swift-closures&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Capturi
      
    
    </summary>
    
      <category term="翻译" scheme="http://hechen.xyz/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="Closure" scheme="http://hechen.xyz/tags/Closure/"/>
    
  </entry>
  
  <entry>
    <title>@autoclosure &amp;&amp; @escape</title>
    <link href="http://hechen.xyz/2017/11/12/autoclosure-escape/"/>
    <id>http://hechen.xyz/2017/11/12/autoclosure-escape/</id>
    <published>2017-11-12T12:01:54.000Z</published>
    <updated>2019-01-23T04:16:40.185Z</updated>
    
    <content type="html"><![CDATA[<p>我们知道在 <code>swift</code> 中，闭包（closure）是一等公民，因此可以被当作参数传递，在学习 swift 的过程中经常会看到某些关键字修饰该闭包，<code>@autoclosure</code>， <code>@escape</code> 就是其中比较常见的两种关键字。</p><h3 id="escape-和-nonescape"><a href="#escape-和-nonescape" class="headerlink" title="@escape 和 @nonescape"></a>@escape 和 @nonescape</h3><p>当一个闭包被当作参数传递给一个函数，但是当该函数内容执行完毕返回之后，该闭包才会被执行，我们就称该闭包要 escape 某个函数，那 <code>@escape</code> 关键字就是用来表示该闭包是允许在函数返回之后被调用的。</p><p>我们用 swift 官方文档的例子来看，如下所示</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var completionHandlers: [() -&gt; Void] = []</span><br><span class="line"></span><br><span class="line">func someFunctionWithEscapingClosure(completionHandler: @escaping () -&gt; Void) &#123;</span><br><span class="line">    completionHandlers.append(completionHandler)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>someFunctionWithEscapingClosure(_:)</code> 的参数是一个闭包，函数内部会把传入的闭包存到之前声明的数组里以便之后进行调用，可以看到，在函数参数的声明部分添加了 <code>@escaping</code> 关键字，如果这里不添加的话，就会在编译的时候报错：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">error: passing non-escaping parameter &apos;completionHandler&apos; to function expecting an @escaping closure</span><br><span class="line">    completionHandlers.append(completionHandler)</span><br></pre></td></tr></table></figure><p>针对标记了 @escaping 关键字含义代表你必须在该闭包内部显式的使用 self 关键字，官方文档中又列举了另外一个例子，如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func someFunctionWithNonescapingClosure(closure: () -&gt; Void) &#123;</span><br><span class="line">    closure()</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">class SomeClass &#123;</span><br><span class="line">    var x = 10</span><br><span class="line">    func doSomething() &#123;</span><br><span class="line">        someFunctionWithEscapingClosure &#123; self.x = 100 &#125;</span><br><span class="line">        someFunctionWithNonescapingClosure &#123; x = 200 &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">let instance = SomeClass()</span><br><span class="line">instance.doSomething()</span><br><span class="line">print(instance.x)</span><br><span class="line">// Prints &quot;200&quot;</span><br><span class="line"> </span><br><span class="line">completionHandlers.first?()</span><br><span class="line">print(instance.x)</span><br><span class="line">// Prints &quot;100”</span><br></pre></td></tr></table></figure><p><code>someFunctionWithEscapingClosure(_:)</code> 是一个可逃逸的闭包，意味着你需要显示的调用 self 关键字， 而 <code>someFunctionWithNonescapingClosure(_:)</code> 是非逃逸的闭包，意味着你可以隐式的调用 self。</p><h3 id="autoclosure"><a href="#autoclosure" class="headerlink" title="@autoclosure"></a>@autoclosure</h3><h4 id="例子讲解"><a href="#例子讲解" class="headerlink" title="例子讲解"></a>例子讲解</h4><p>通过一个🌰来说明 <code>@autoclosure</code> 关键字到底起到什么作用。</p><p>考虑下面这个函数 f，其需传入一个参数，类型是 <code>()-&gt; Bool</code> 的闭包。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func f(predicate: () -&gt; Bool) &#123;</span><br><span class="line">    if predicate() &#123;</span><br><span class="line">        print(&quot;It&apos;s true&quot;)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后通过传入符合此类型的闭包进行调用</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">f(predicate: &#123;2 &gt; 1&#125;)</span><br><span class="line">// &quot;It&apos;s true&quot;</span><br></pre></td></tr></table></figure><p>但是，如果我们忽略传入闭包的 <code>{</code> 和 <code>}</code> ，编译就会错误。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">f(predicate: 2 &gt; 1)</span><br><span class="line">// error: &apos;&gt;&apos; produces &apos;Bool&apos;, not the expected contextual result type &apos;() -&gt; Bool&apos;</span><br></pre></td></tr></table></figure><p>如文档中所说，一个 autoclosure (自主闭包？)是这样一种闭包: 当某个表达式被当做参数传递给一个函数的时候会被 wrap 成一个闭包，该闭包没有任何参数，其被调用的时候，返回的是其 wrap 的表达式的值。swift 提供这种语法糖就能够让你省略 {} 而直接写一个表达式。</p><p>结合上面的例子来看，当你写个表达式类似 2 &gt; 1 传给函数 f 的时候，该表达式会被自动包裹到一个闭包中，会自动处理为 { 2 &gt; 1 } 而传递给函数 f。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func f(predicate: @autoclosure () -&gt; Bool) &#123;</span><br><span class="line">    if predicate() &#123;</span><br><span class="line">        print(&quot;It&apos;s true&quot;)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">f(predicate: 2 &gt; 1)</span><br><span class="line">// It&apos;s true</span><br></pre></td></tr></table></figure><p>⚠️ @autoclosure 并不支持带有输入参数的写法，也就是说只有形如 () -&gt; T 的参数才能简化</p><h4 id="Delay-Evaluation"><a href="#Delay-Evaluation" class="headerlink" title="Delay Evaluation"></a>Delay Evaluation</h4><p>swift 提供了 ?? 操作符，如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let nickName: String? = nil</span><br><span class="line">let fullName: String = &quot;John Appleseed&quot;</span><br><span class="line">let informalGreeting = &quot;Hi \(nickName ?? fullName)</span><br></pre></td></tr></table></figure><p>如果某 Optional 存在就会返回其值，如果没有就会返回后面的默认值，当我们去看 ?? 的实现的时候能看到如下定义：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func ??&lt;T&gt;(optional: T?, defaultValue: @autoclosure () -&gt; T?) -&gt; T?</span><br><span class="line"></span><br><span class="line">func ??&lt;T&gt;(optional: T?, defaultValue: @autoclosure () -&gt; T) -&gt; T</span><br></pre></td></tr></table></figure><p>看得出来 <code>??</code> 是一个二元操作符，optional 指代 ?? 前面的输入，defaultValue 指代 <code>??</code> 后面的参数，那我们就会想，我们上面的例子中 <code>fullName</code> 只是一个 <code>String</code>，怎么变成 <code>() -&gt; T</code> 类型的呢？ 这个就看前面的 <code>@autoclosure</code> 的威力了，前面讲过了，该关键字把表达式的值封装成闭包并且返回该表达式的值了。 其实传入该方法的第二个参数是 <code>{ fullName }</code>。</p><p>所以可以想到该方法的实现应该如下所示，（当然 fullName 为 String 类型，应该会重载第二个函数实现）</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func ??&lt;T&gt;(optional: T?, defaultValue: @autoclosure () -&gt; T) -&gt; T &#123;</span><br><span class="line">    switch optional &#123;</span><br><span class="line">        case .Some(let value):</span><br><span class="line">            return value</span><br><span class="line">        case .None:</span><br><span class="line">            return defaultValue()</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里我们还需要注意一点的是，使用 <code>@autoclosure</code> 来修饰的表达式可以实现延迟计算，也就是说直到该闭包被调用之前，闭包里所被包裹的表达式都不会进行取值计算，也就避免了一定的开销，尤其是上面默认值是复杂计算得到的话。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;我们知道在 &lt;code&gt;swift&lt;/code&gt; 中，闭包（closure）是一等公民，因此可以被当作参数传递，在学习 swift 的过程中经常会看到某些关键字修饰该闭包，&lt;code&gt;@autoclosure&lt;/code&gt;， &lt;code&gt;@escape&lt;/code&gt; 就是其
      
    
    </summary>
    
      <category term="Swift" scheme="http://hechen.xyz/categories/Swift/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
  </entry>
  
  <entry>
    <title>阅读 《Thread Programming Guide》</title>
    <link href="http://hechen.xyz/2017/10/17/Thread-Programming-Guide/"/>
    <id>http://hechen.xyz/2017/10/17/Thread-Programming-Guide/</id>
    <published>2017-10-17T12:06:46.000Z</published>
    <updated>2017-10-17T12:18:26.000Z</updated>
    
    <content type="html"><![CDATA[<p>花了两天时间阅读了官方的 Thread Programming Guide，下面是该文档的脑图。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/Thread Programming Guide-1.png" alt="Thread Programming Guide"></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;花了两天时间阅读了官方的 Thread Programming Guide，下面是该文档的脑图。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;http://7xilk1.com1.z0.glb.clouddn.com/Thread Programming Guide-1.png&quot; a
      
    
    </summary>
    
      <category term="Apple Documentation" scheme="http://hechen.xyz/categories/Apple-Documentation/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Thread" scheme="http://hechen.xyz/tags/Thread/"/>
    
  </entry>
  
  <entry>
    <title>关于 iOS10 Notification 的那些事儿</title>
    <link href="http://hechen.xyz/2017/05/20/iOS-Notification/"/>
    <id>http://hechen.xyz/2017/05/20/iOS-Notification/</id>
    <published>2017-05-20T02:10:54.000Z</published>
    <updated>2018-05-02T12:56:13.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h3><p>推送通知我们大家都不陌生，可以说几乎每个使用智能手机的人每天都会被不同的通知 <em>打扰</em> 到，正式因为合适的推送是吸引用户注意力的利器，其成为了各 App 吸引用户，将用户带回到 App 本身，提升用户的活跃度的一种必要的方式。当然要注意的是，推送本身是一件对用户影响特别大的事情，毕竟注意力被打断，因此合适的推送时机也是各个 App 开发者所要注意的，否则就会成为用户勿扰名单里的一员了。</p><p>之前刚开始学习 iOS 开发的时候还整理了下当时部署 iOS 远程推送的流程，详见：<a href="http://hechen.info/2015/07/30/iOS-Push-Notification/" target="_blank" rel="noopener">iOS 远端推送部署详解</a></p><p>接下来，我们大致回顾一下 iOS 平台关于推送都有哪些历程？</p><a id="more"></a><h3 id="历史"><a href="#历史" class="headerlink" title="历史"></a>历史</h3><ul><li><p>&lt;= iOS 6 </p><ul><li>远程推送通知 （iOS 3）</li><li>本地通知 （iOS 4）</li></ul></li><li><p>iOS 7  参考 </p><ul><li>引入 Silent Remote Notifications</li></ul></li><li><p>iOS 8  参考 <a href="https://developer.apple.com/videos/play/wwdc2014/713/" target="_blank" rel="noopener">WWDC 2014 Session 713</a></p><ul><li>引入 Actionable Notifications</li><li>修改 Notification 的权限请求</li></ul></li><li><p>iOS 9  参考 <a href="https://developer.apple.com/videos/play/wwdc2015/720/" target="_blank" rel="noopener">WWDC 2015 Session 720</a></p><ul><li>引入 Text Input Action</li><li>UIUserNotificationActionBehavior</li></ul></li><li><p>iOS 10 参考 <a href="https://developer.apple.com/videos/play/wwdc2016/707" target="_blank" rel="noopener">WWDC 707 Introduction to Notifications</a> &amp;&amp; <a href="https://developer.apple.com/videos/play/wwdc2016/708" target="_blank" rel="noopener">WWDC 708 Advanced Notifications</a></p><ul><li>UserNotification Framework</li><li>Extensions</li></ul></li></ul><p>WWDC 2016 大会上，Apple 在 iOS 10 上引入了 <strong>UserNotification</strong> 框架，可以说是对之前的各种代码做了一次重构。该框架统一了通知的行为，尤其是针对远程推送和本地推送不再有两套完全不同的使用方式了。</p><h3 id="变化"><a href="#变化" class="headerlink" title="变化"></a>变化</h3><p>关于 iOS 10 上 UserNotification 框架的变化，主要从几个方面来讲述：</p><ol><li>权限申请</li><li>推送内容变更</li><li>推送管理</li><li>Extensions</li></ol><h4 id="权限申请"><a href="#权限申请" class="headerlink" title="权限申请"></a>权限申请</h4><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">UNUserNotificationCenter</span>.current().requestAuthorization(options: [.alert, .sound, .badge]) &#123;</span><br><span class="line">    granted, error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> granted &#123;</span><br><span class="line">        <span class="comment">// 用户允许进行通知</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="远程推送"><a href="#远程推送" class="headerlink" title="远程推送"></a>远程推送</h4><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 向 APNs 请求 token：</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// iOS 10 support</span></span><br><span class="line"><span class="keyword">if</span> #available(iOS <span class="number">10</span>, *) &#123;  </span><br><span class="line">    <span class="type">UNUserNotificationCenter</span>.current().requestAuthorization(options:[.badge, .alert, .sound])&#123; (granted, error) <span class="keyword">in</span> &#125;</span><br><span class="line">    application.registerForRemoteNotifications()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// iOS 9 support</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> #available(iOS <span class="number">9</span>, *) &#123;  </span><br><span class="line"></span><br><span class="line"><span class="type">UIApplication</span>.shared.registerUserNotificationSettings(<span class="type">UIUserNotificationSettings</span>(types: [.badge, .sound, .alert], categories: <span class="literal">nil</span>))</span><br><span class="line">    <span class="type">UIApplication</span>.shared.registerForRemoteNotifications()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关于注册远程通知的回调方法一致，</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// AppDelegate.swift</span></span><br><span class="line"> <span class="function"><span class="keyword">func</span> <span class="title">application</span><span class="params">(<span class="number">_</span> application: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data)</span></span> &#123;</span><br><span class="line">    <span class="keyword">let</span> tokenString = deviceToken.hexString</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"Get Push token: <span class="subst">\(tokenString)</span>"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="Payloads"><a href="#Payloads" class="headerlink" title="Payloads"></a>Payloads</h4><p>&lt;  iOS 10</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"aps"</span>:&#123;</span><br><span class="line">    <span class="attr">"alert"</span>:<span class="string">"Test"</span>,</span><br><span class="line">    <span class="attr">"sound"</span>:<span class="string">"default"</span>,</span><br><span class="line">    <span class="attr">"badge"</span>:<span class="number">1</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>iOS 10 系统提供了更为丰富的结构，比如可以指定 Title，Subtitle 等</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">   <span class="attr">"aps"</span>:&#123;</span><br><span class="line">     <span class="attr">"alert"</span>:&#123;</span><br><span class="line">       <span class="attr">"title"</span>:<span class="string">"This is a title"</span>,</span><br><span class="line">       <span class="attr">"subtitle"</span>:<span class="string">"This is a subtitle"</span>,</span><br><span class="line">       <span class="attr">"body"</span>:<span class="string">"This is body"</span></span><br><span class="line">     &#125;,</span><br><span class="line">     <span class="attr">"sound"</span>:<span class="string">"default"</span>,</span><br><span class="line">     <span class="attr">"badge"</span>:<span class="number">1</span></span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure><p>甚至，现在可以支持多媒体的展示了，在 payload 上也有相应的体现，例如下面几例：</p><ol><li><p>支持图片</p> <figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"aps"</span>:&#123;</span><br><span class="line">        <span class="attr">"alert"</span>: &#123;</span><br><span class="line">            <span class="attr">"title"</span>: <span class="string">"Title: Notification Demo"</span>, </span><br><span class="line">            <span class="attr">"subtitle"</span>: <span class="string">"Subtitle: show iOS 10 support!"</span>,</span><br><span class="line">            <span class="attr">"body"</span>: <span class="string">"The Main Body For Notification!"</span></span><br><span class="line">            &#125;,</span><br><span class="line">            <span class="attr">"mutable-content"</span>: <span class="number">1</span></span><br><span class="line">        &#125;,</span><br><span class="line">    <span class="attr">"image"</span> : <span class="string">"https://pic1.zhimg.com/v2-0314056e4f13141b6ca2277078ec067c.jpg"</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>支持音频</p> <figure class="highlight json"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="attr">"aps"</span>:&#123;</span><br><span class="line">        <span class="attr">"alert"</span>: &#123;</span><br><span class="line">        <span class="attr">"title"</span>: <span class="string">"Title: Notification Demo"</span>, </span><br><span class="line">        <span class="attr">"subtitle"</span>: <span class="string">"Subtitle: show iOS 10 support!"</span>, </span><br><span class="line">        <span class="attr">"body"</span>: <span class="string">"The Main Body For Notification!"</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="attr">"mutable-content"</span>: <span class="number">1</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">"audio"</span> : <span class="string">"http://hao.1015600.com/upload/ring/000/982/d9924a7f4e4ab06e52a11dfdd32ffae1.mp3"</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><p>具体所有的 Key 可以参考官方文档 <a href="https://developer.apple.com/library/content/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/PayloadKeyReference.html" target="_blank" rel="noopener">Payload Key Reference</a></p><blockquote><p>注意到有个 key:  launch-image ，可以指定用户点击通知启动 App 的时候的 Launch Image</p></blockquote><h4 id="可以撤销和更新通知了！"><a href="#可以撤销和更新通知了！" class="headerlink" title="可以撤销和更新通知了！"></a>可以撤销和更新通知了！</h4><p>UserNotification 框架 API 提供了通知的更新和撤销的接口。具体功能主要包含几个部分：</p><ol><li><p>删除未展示的通知</p> <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> trigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">3</span>, repeats: <span class="literal">false</span>)</span><br><span class="line"><span class="keyword">let</span> identifier = <span class="type">Constants</span>.pendingRemoveNotificationIdentifier</span><br><span class="line"><span class="keyword">let</span> request = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: title1Content, trigger: trigger)</span><br><span class="line"></span><br><span class="line"><span class="type">UNUserNotificationCenter</span>.current().add(request) &#123; error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"remove pending notification error: <span class="subst">\(error)</span>"</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"Notification request added: <span class="subst">\(identifier)</span>"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">delay(<span class="number">2</span>) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">"Notification request removed: <span class="subst">\(identifier)</span>"</span>)</span><br><span class="line">    <span class="type">UNUserNotificationCenter</span>.current().removePendingNotificationRequests(withIdentifiers: [identifier])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>更新未展示的通知</p> <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> trigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">3</span>, repeats: <span class="literal">false</span>)</span><br><span class="line"><span class="keyword">let</span> identifier = <span class="type">Constants</span>.pendingUpdateNotificationIdentifier</span><br><span class="line"><span class="keyword">let</span> request = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: title1Content, trigger: trigger)</span><br><span class="line"></span><br><span class="line"><span class="type">UNUserNotificationCenter</span>.current().add(request) &#123; error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"update pending notification error: <span class="subst">\(error)</span>"</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"Notification request added: <span class="subst">\(identifier)</span> with title1"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">delay(<span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> newTrigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">1</span>, repeats: <span class="literal">false</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Add new request with the same identifier to update a notification.</span></span><br><span class="line">    <span class="keyword">let</span> newRequest = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: <span class="keyword">self</span>.title2Content, trigger: newTrigger)</span><br><span class="line">    <span class="type">UNUserNotificationCenter</span>.current().add(newRequest) &#123; error <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"update delivered notification error: <span class="subst">\(error)</span>"</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"Notification request updated: <span class="subst">\(identifier)</span> with title2"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>删除已经展示的通知</p> <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> trigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">3</span>, repeats: <span class="literal">false</span>)</span><br><span class="line"><span class="keyword">let</span> identifier = <span class="type">Constants</span>.pendingUpdateNotificationIdentifier</span><br><span class="line"><span class="keyword">let</span> request = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: title1Content, trigger: trigger)</span><br><span class="line"></span><br><span class="line"><span class="type">UNUserNotificationCenter</span>.current().add(request) &#123; error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"update pending notification error: <span class="subst">\(error)</span>"</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"Notification request added: <span class="subst">\(identifier)</span> with title1"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">delay(<span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> newTrigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">1</span>, repeats: <span class="literal">false</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Add new request with the same identifier to update a notification.</span></span><br><span class="line">    <span class="keyword">let</span> newRequest = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: <span class="keyword">self</span>.title2Content, trigger: newTrigger)</span><br><span class="line">    <span class="type">UNUserNotificationCenter</span>.current().add(newRequest) &#123; error <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"update delivered notification error: <span class="subst">\(error)</span>"</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"Notification request updated: <span class="subst">\(identifier)</span> with title2"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>更新已经展示的通知</p> <figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> trigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">3</span>, repeats: <span class="literal">false</span>)</span><br><span class="line">    <span class="keyword">let</span> identifier = <span class="type">Constants</span>.deliveredUpdateNotificationIdentifier</span><br><span class="line">    <span class="keyword">let</span> request = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: title1Content, trigger: trigger)</span><br><span class="line">    </span><br><span class="line">    <span class="type">UNUserNotificationCenter</span>.current().add(request) &#123; error <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">            </span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">"Notification request added: <span class="subst">\(identifier)</span> with title1"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    delay(<span class="number">4</span>) &#123;</span><br><span class="line">        <span class="keyword">let</span> newTrigger = <span class="type">UNTimeIntervalNotificationTrigger</span>(timeInterval: <span class="number">1</span>, repeats: <span class="literal">false</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// Add new request with the same identifier to update a notification.</span></span><br><span class="line">        <span class="keyword">let</span> newRequest = <span class="type">UNNotificationRequest</span>(identifier: identifier, content: <span class="keyword">self</span>.title2Content, trigger: newTrigger)</span><br><span class="line">        <span class="type">UNUserNotificationCenter</span>.current().add(newRequest) &#123; error <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> error = error &#123;</span><br><span class="line">                </span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="built_in">print</span>(<span class="string">"Notification request updated: <span class="subst">\(identifier)</span> with title2"</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li></ol><p>当然，上述均是针对本地通知的操作，关于远程推送通知，目前只支持更新通知，远程推送可以进行通知的更新，在使用 Provider API 向 APNs 提交请求时，在 HTTP/2 的 header 中 apns-collapse-id key 的内容将被作为该推送的标识符进行使用。多次推送同一标识符的通知即可进行更新。</p><h4 id="Notification-Extension"><a href="#Notification-Extension" class="headerlink" title="Notification Extension"></a>Notification Extension</h4><p>iOS 10 中最重要的一个变化就是 Extension，从 iMessage Extension 到 SiriKit 中提供的  Intent Extension 等，那对于 UserNotification 来讲就是下面这两种 Extension：</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/18D62919-DA73-4D3D-9B05-6071E6945764-1.png" alt="18D62919-DA73-4D3D-9B05-6071E6945764"></p><ol><li>Service Extension </li></ol><p>具体而言，就是我们可以在收到通知之后，在展示给用户之前 ，也就是以Banner 或者 Alert 的形式 或者 进入通知中心之前，给我们一次截取并处理的机会，这样就给开发者提供了针对推送通知再加工的手段，并且远程推送多媒体也是通过 Service Extension 来实现的。</p><blockquote><p>使用在本机截取推送并替换内容的方式，可以完成端到端 (end-to-end) 的推送加密。你在服务器推送 payload 中加入加密过的文本，在客户端接到通知后使用预先定义或者获取过的密钥进行解密之后再显示。这样一来，即使推送信道被第三方截取，其中所传递的内容也还是安全的。使用这种方式来发送密码或者敏感信息，对于一些金融业务应用和聊天应用来说，应该是必备的特性。</p></blockquote><p>生成 Extension 之后，系统已经为我们提供了模板，代码如下：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NotificationService</span>: <span class="title">UNNotificationServiceExtension</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> contentHandler: ((<span class="type">UNNotificationContent</span>) -&gt; <span class="type">Void</span>)?</span><br><span class="line">    <span class="keyword">var</span> bestAttemptContent: <span class="type">UNMutableNotificationContent</span>?</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">didReceive</span><span class="params">(<span class="number">_</span> request: UNNotificationRequest, withContentHandler contentHandler: @escaping <span class="params">(UNNotificationContent)</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.contentHandler = contentHandler</span><br><span class="line">        bestAttemptContent = (request.content.mutableCopy() <span class="keyword">as</span>? <span class="type">UNMutableNotificationContent</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> bestAttemptContent = bestAttemptContent &#123;</span><br><span class="line">            contentHandler(bestAttemptContent)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">serviceExtensionTimeWillExpire</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// Called just before the extension will be terminated by the system.</span></span><br><span class="line">        <span class="comment">// Use this as an opportunity to deliver your "best attempt" at modified content, otherwise the original push payload will be used.</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> contentHandler = contentHandler, <span class="keyword">let</span> bestAttemptContent =  bestAttemptContent &#123;</span><br><span class="line">            contentHandler(bestAttemptContent)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="2"><li>Content Extension<br>iOS 10 SDK 新加的另一个 Content Extension 可以用来自定义通知的详细页面的视图。 下图中就是几款市面上目前已经实现过自定义 Content Extension 的 App 截图。需要注意的是：</li></ol><p>第一个 PriceTag App 的通知样式，实际上是默认的系统显示行为。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/IMG_AD4882D0ECDE-1.jpeg" alt="IMG_AD4882D0ECDE-1"></p><p>其中需要注意的一点是。如果不想默认显示通知内容，需要在 Content Extension 的 info.plist 文件中添加 UNNotificationExtensionDefaultContentHidden  并设置为 YES</p><h4 id="支持多媒体资源在通知中心的展示"><a href="#支持多媒体资源在通知中心的展示" class="headerlink" title="支持多媒体资源在通知中心的展示"></a>支持多媒体资源在通知中心的展示</h4><p>iOS 10 中另一个比较显著的特点就是支持了多媒体的推送和展示。开发者现在可以在通知中嵌入图片、音频甚至视频，这极大丰富了推送内容的可读性和趣味性。</p><ol><li>本地推送支持<br>本地通知添加多媒体比较简单一些，只需要通过本地磁盘上的文件 URL 创建一个 UNNotificationAttachment 对象，然后将这个对象放到数组中赋值给 content 的 attachments 属性就行了：</li></ol><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> content = <span class="type">UNMutableNotificationContent</span>()</span><br><span class="line">content.title = <span class="string">"Image Notification"</span></span><br><span class="line">content.body = <span class="string">"Show me an image!"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> imageURL = <span class="type">Bundle</span>.main.url(forResource: <span class="string">"image"</span>, withExtension: <span class="string">"jpg"</span>),</span><br><span class="line">   <span class="keyword">let</span> attachment = <span class="keyword">try</span>? <span class="type">UNNotificationAttachment</span>(identifier: <span class="string">"imageAttachment"</span>, url: imageURL, options: <span class="literal">nil</span>) &#123;</span><br><span class="line">    content.attachments = [attachment]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="2"><li>远程推送支持</li></ol><p>首先需要在 payloads 结构中添加对富媒体的支持，aps 字典中添加字段<code>mutable-content</code>并置为 1 来标识该远程通知是需要支持。 然后可以在 payloads 中添加资源地址，可以是本地的资源，也可以是需要 App 下载的。</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"aps"</span>:&#123;</span><br><span class="line">    <span class="attr">"alert"</span>:&#123;</span><br><span class="line">      <span class="attr">"title"</span>:<span class="string">"Image Notification"</span>,</span><br><span class="line">      <span class="attr">"body"</span>:<span class="string">"Show me an image from web!"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">"mutable-content"</span>:<span class="number">1</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"zh_image"</span>: <span class="string">"https://pic1.zhimg.com/v2-0314056e4f13141b6ca2277078ec067c.jpg"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>具体支持多媒体文件的富媒体类型以及每一种类型所支持的大小详官方文档  <a href="https://developer.apple.com/reference/usernotifications/unnotificationattachment" target="_blank" rel="noopener">UNNotificationAttachment</a></p><p>一旦远程推送在 aps 中添加 mutable-content 的key 并设置为 1 之后，iOS 系统接收到该推送之后就会唤起我们配套的 Service Extension 来做进一步处理，我们可以在其中下载对应 zh_image 链接的图片，然后生成 Attachment 再丢给系统处理即可。 详细做法就是在上一节讲 Service Extension 中的 <code>didReceive</code> 中写相应逻辑即可，具体代码如下所示：</p><p>当然，这里的处理时间是存在时间限制的，如果处理超时系统就会回收该 Extension，并且调用 <code>serviceExtensionTimeWillExpire</code> 方法，因此每个人收到带有附件的推送之后不一定展示相同，例如下图所示的情况，第一张和第二张就属于正常设置了附件的情况，而第三张就是没有在有限时间内正确设置附件的情况。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/14953507334611.png" alt="未成功设置附件的情况"></p><h4 id="推送证书的配置"><a href="#推送证书的配置" class="headerlink" title="推送证书的配置"></a>推送证书的配置</h4><p>下图是获取推送通知证书并将其注册到 Leancloud 的流程，最关键的地方其实就是需要在本机生成 CSR 文件提交到 Apple Developer Website 生成 Push Certification 文件。</p><p>主要有几个步骤：</p><ol><li>AppID 关于 Remote Push 的 注册</li><li>生成推送证书</li><li>转换证书为 P12 文件，并提供给 Leancloud</li></ol><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/02.png" alt="02"></p><p>XCode 8 的 Auto Signing 已经省去了难以名状的复杂，但是还是有一点小事情我们需要做。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/iOS App IDs - Apple Developer Google Chrome, 今天 at 下午5.01.22.png" alt="iOS App IDs - Apple Developer Google Chrome, 今天 at 下午5.01.22"></p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/0CB09A98-0D9B-4E76-BA7C-9A1766CFEC42.png" alt="0CB09A98-0D9B-4E76-BA7C-9A1766CFE"></p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.07.47.png" alt="Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.07.47"></p><p>我们需要生成 CSR 文件 ，按照指定步骤按部就班来就行，最后会生成 默认文件 <em>CertificateSigningRequest.certSigningRequest</em></p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/Finder Finder, 今天 at 下午4.59.55.png" alt="Finder Finder, 今天 at 下午4.59.55"><br><img src="http://7xilk1.com1.z0.glb.clouddn.com/证书助理 证书助理, 今天 at 下午5.05.42.png" alt="证书助理 证书助理, 今天 at 下午5.05.42"></p><p>接着之前的步骤，选择 CSR 文件上传。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.09.07.png" alt="Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.09.07"></p><p>上传成功之后就会生成 Push 证书，下载到本机安装上。</p><p><img src="http://7xilk1.com1.z0.glb.clouddn.com/Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.09.58.png" alt="Add - iOS Certificates - Apple Developer Google Chrome, 今天 at 下午5.09.58"></p><h4 id="Demo-演示-Knuff"><a href="#Demo-演示-Knuff" class="headerlink" title="Demo 演示  Knuff"></a>Demo 演示  <a href="https://github.com/KnuffApp/Knuff" target="_blank" rel="noopener">Knuff</a></h4><p>参考资料</p><ol><li><a href="https://developer.apple.com/library/content/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/index.html#//apple_ref/doc/uid/TP40008194-CH3-SW1" target="_blank" rel="noopener">Local and Remote Notification Programming Guide</a></li><li><a href="https://developer.apple.com/reference/usernotifications" target="_blank" rel="noopener">UserNotifications 官方文档</a></li><li><a href="http://wereadteam.github.io/2017/03/13/Signature/" target="_blank" rel="noopener">iOS App 签名的原理</a></li><li><a href="https://medium.com/@scomper/探究-iphone-的后台刷新-a7a96cb426d4" target="_blank" rel="noopener">iPhone 的后台刷新</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;概览&quot;&gt;&lt;a href=&quot;#概览&quot; class=&quot;headerlink&quot; title=&quot;概览&quot;&gt;&lt;/a&gt;概览&lt;/h3&gt;&lt;p&gt;推送通知我们大家都不陌生，可以说几乎每个使用智能手机的人每天都会被不同的通知 &lt;em&gt;打扰&lt;/em&gt; 到，正式因为合适的推送是吸引用户注意力的利器，其成为了各 App 吸引用户，将用户带回到 App 本身，提升用户的活跃度的一种必要的方式。当然要注意的是，推送本身是一件对用户影响特别大的事情，毕竟注意力被打断，因此合适的推送时机也是各个 App 开发者所要注意的，否则就会成为用户勿扰名单里的一员了。&lt;/p&gt;
&lt;p&gt;之前刚开始学习 iOS 开发的时候还整理了下当时部署 iOS 远程推送的流程，详见：&lt;a href=&quot;http://hechen.info/2015/07/30/iOS-Push-Notification/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;iOS 远端推送部署详解&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;接下来，我们大致回顾一下 iOS 平台关于推送都有哪些历程？&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://hechen.xyz/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://hechen.xyz/tags/iOS/"/>
    
      <category term="Swift" scheme="http://hechen.xyz/tags/Swift/"/>
    
      <category term="Notification" scheme="http://hechen.xyz/tags/Notification/"/>
    
  </entry>
  
</feed>
